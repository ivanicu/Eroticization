# CALIBER — 同一个量在不同轮次给出不同值的那些小选择

**这不是文档,是一张可查的清单。** 下一次两轮对不上时,**第一件事是查它**。
每一行都由 `E01…/A80…/R317_caliber_list_and_table_note/run.py` **实测**其影响,
并由**守卫 14**(`could_have_come_out_otherwise`)验证它**真的会改变**一个已发表的数;
**不会改变的旋钮不进这张表**(`#241a` 的教训:一张让人安心的长表比没有表更糟)。

基准量:**六个人层量对 29 个结局的联合 R² 中位**,基线 **1.37%**(旧 S + 最小二乘,`#264a` 口径)。

| # | 旋钮 | 取值 | 实测 | 最大变动 |
|---|---|---|---:|---:|
| ① | **块覆盖阈值** `cov >= n` | 8 / 6 / 10 | 1.37% · 1.38% · 1.54% | **0.17pp** |
| ② | **剖面清晰度的最小块数** `need` | 8 / 6 / 10 | 1.37% · 1.37% · 1.55% | **0.18pp** |
| ③ | **位置分 S 是否随块子集变化** | 否 / 是 | 1.37% · 1.21% | **0.17pp** |
| ④ | **选项劈分种子** | 500 / 900 / 1200 | 1.37% · 1.35% · 1.35% | 0.02pp |
| ~~⑤~~ | ~~阈值重抽样次数~~ | — | 1.37% × 3,**逐位相同** | **0.00pp,已删** |

**三个主旋钮同时取另一端**(`cov>=10, need=10, 新 S`):**1.52%** —— 只比基线高 **+0.15pp**,
**它们部分相消。**

## ⚠ 这张表**不完整**,而这一点必须写在最上面
`#314` 报的 **2.23%** 与本口径的 **1.37%** 差 **+0.85pp**。
**这五个旋钮里,单个最大 0.18pp,三个叠加 +0.15pp —— 都解释不了它。**

> **至少还有一个我没有识别出来的选择。**
> `#271b`:一个数在两轮之间变了,而我能想到的原因都不是它,
> **说明还有第三个我没记下来的选择** —— 而这张表现在证明了它确实存在,只是还没被找到。

## 用法
- 报告一个可能被复现的数时,**注明这四个旋钮的取值**;
- 两轮对不上时,**先逐个旋钮复算**,再怀疑别的;
- 发现新旋钮时,**先用守卫 14 验证它真的会动**,再加进这张表。

## ⚠ 为什么「劈分种子」这一格只有 0.02pp(`D5`,推论不是测量)
`#270a` 实测:六个量**共享测量误差**(c1↔c2 |0.416| 等)—— 因为它们由**同一次**劈分构造。
**推论**:换一个劈分种子时,六个量**一起**移动,所以联合 R² 几乎不动(0.02pp)。
> **这意味着「跨种子展布」**低估**了真实的不确定性** ——
> 它量的是「同一次抽签重来一遍」,不是「六个量各自独立地重来一遍」。
⚠ 标 `D5`:这是从 `#270a` 的测量推出来的,**本身没有被直接测过**。

## ⑤ 合并样本 vs 按性别分组(`#286`,守卫 14 已验)
| 口径 | 联合 R² 中位 |
|---|---:|
| 合并样本 | **1.39%** |
| `biomale = 0`(n=3,232) | **1.85%** |
| `biomale = 1`(n=3,485) | **1.67%** |

守卫 14:**2/2 个扰动让它变了**(变动 0.28–0.46pp),所以这是一个真的旋钮。
⚠ 而它与前四个不同:前四个是**分析选择**,这一个是**样本选择** ——
`#286a` 已证六个坐标的预测在两组间可分辨地不同(**12.8×**),
**所以「合并」不是一个中性的默认值,它是一个会改变结论的选择。**
