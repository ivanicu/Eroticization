# `E02·A13·R650` — 那两个维度,有名字吗?(而命名必须是可失败的)

**仪器**:GHN 2009 JPSP Study 3(MFQ),n=5,453。

**§3 识别检查先于一切**:PC1/PC2 只有在特征值有间隔时才各自可识别。
实测 λ 间隔 41.35%,bootstrap |cos| 0.9996 / 0.9992 ⇒ **轴可识别,命名有资格被问。**

**L21 被执行**:禁止套用文献里的现成名字;判据只允许名字从数据里长出来。

**结果:UNVERIFIED** —— 观察到的 `max−次大` 低于随机方向的零臂 ⇒ 安慰剂 FAIL ⇒
**证不了名字,也没有任何东西支持名字。** 失败的形状才是结果:每条轴是一**捆**域。

⚠ **这一段是对仪器的验证,不是发现** —— 两捆结构是该问卷自己的文献所报告的(P14 `prior_art`)。

**结构上做不到**:非因果 · **用作者的域命名作者的主成分是循环的,本轮没有打开** ·
非概率样本。数字只住在顶层 `README.md` 与 `RETRACTIONS.md` `#614`。
