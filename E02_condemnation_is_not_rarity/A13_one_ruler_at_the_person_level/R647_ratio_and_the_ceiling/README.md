# `E02·A13·R647` — 把 `#610c` 的两个事后量正式预注册,然后用天花板去打它们

**仪器**:GHN 2009 JPSP Study 3(MFQ),30 题 + `Religion_attend_num` 完整 n=6,438。
**判据逐字取自 `#610` 的 NEXT,本轮只执行,不改判据。**

**先做攻击阶梯第 2 级(代数,最便宜)**:`ρ_obs = ρ_true·√(r_ii·r_jj)` ⇒
`ratio = within/out` 里 `r_ii` 约掉 ⇒ 这道题自己的天花板消掉。
⚠ 这是 **DERIVATION,不是证据**;序数题 + 秩相关未必满足经典衰减 ⇒ 仍实测(E3 人为造天花板)。

**估计量**:`ratio(i)=within(i)/out(i)` · 每题边际分布 · 天花板 Δ。
**控制**:正对照 = 纯域内 vs 总括合成题(g=0 时必须不通过)· 安慰剂 = 非纯洁题造天花板 ·
**offset** = 随机打乱域指派(域大小相同也会产生的域内>域外)。

**结果:严格判据不通过,`#610c①` 已撤回。** 见 `RETRACTIONS.md` `#611`。
数字与区间只住在顶层 `README.md`。

**结构上做不到**:非因果 · 非概率样本 · 人为天花板是模拟不是真实题目改写。
