# `E02·A13·R648` — 纯洁是上游,还是和别人共享一个上游?

**仪器**:GHN 2009 JPSP Study 3(MFQ),30 题 + `politics` + `Age` 完整 n=5,453。

**硬规则①救了一次**:`#611` 点名的 `libcon_soc` 实测 **n=19**,弃用;改 `politics`(n=6,619,1–7)。

**估计量**:`R_D` = 拿掉域 D 的共同因子后,**不含 D 的那四个域之间**(216 对)还剩多少跨域相关。
**纯洁的题一道也不在这个对集里。**

**§3 梯度检查**:偏掉任何宽合成量都会机械压低跨域相关 ⇒ offset 必须是其余四域各自 PC1,对称计算。

**控制**:正对照 = 全 30 题 PC1(g=0 纯噪声时必须不通过)· 安慰剂 = 年龄 ·
offset = 偏掉任意一个六题域 PC1 都会有的机械衰减。

**结果:W-CAUSE 死,按判据报区间。** 数字只住在顶层 `README.md` 与 `RETRACTIONS.md` `#612`。

**结构上做不到**:非因果 · 「上游」只能是统计意义的 · 单时点无法定序 · 非概率样本 ·
**最强反驳(政治可能只是道德判断的摘要)本轮没有分开** —— 已写成下一轮的判据。
