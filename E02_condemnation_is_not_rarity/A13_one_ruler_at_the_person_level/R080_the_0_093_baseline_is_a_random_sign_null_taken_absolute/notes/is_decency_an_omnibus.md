# `E02·A13·R646` — `decency` 的载重,是纯洁内部的,还是一道措辞最泛的总括题?

**仪器**:GHN 2009 JPSP Study 3(MFQ),30 题 + `Religion_attend_num` 完整 n=6,438。

**BASIN RULE**:连续第三轮确认「纯洁特殊」,所以本轮**就是去攻击它** ——
W-A(总括题)的成立会把 `#609c` 那行降级,而那是我不希望的。

**§3 梯度检查(踩上去之前)**:总括题会同时抬高本域一致性与跨域相关 ⇒
「删掉它领先下降」的符号**不是代数强制的**。两个领先口径都量。

**估计量**:`out(i)` = 与域外全部题的 |ρ| 中位;辅助 `ratio(i)=within(i)/out(i)`。

**控制**:正对照 = 合成真总括题(g=0 时必须不通过)· 安慰剂 = 纯域内合成题 ·
offset = 每题都带的一般共性(`out` 从来不是 0)。

**结果:W-C(预注册的统计量未定)。** 把方向定住的两个量**是事后的**,已写成下一轮的预注册。
数字与区间只住在顶层 `README.md` 与 `RETRACTIONS.md` `#610`。

**结构上做不到**:非因果 · 非概率样本 · `out` 不量「为什么泛」——
要分开措辞与概念需要改写题干重测,那是一次新的数据采集。
