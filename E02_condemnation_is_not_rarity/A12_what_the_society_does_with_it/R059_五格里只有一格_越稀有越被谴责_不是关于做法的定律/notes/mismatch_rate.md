# `E02·A12·R668` — 那个配对启发式,错配率是多少?

**行动类型:PRODUCTION**,结论 **UNVERIFIED**(正对照没踩中陷阱)+ 一个实测到的硬缺陷。

⚠ **一处标注的偏离**:预注册写「临时分支伪造」,本轮走**纯文本支**(`#173c` 的规则),刻意且标注。
**§3 救了这一轮**:「无关段落」必须携带一个**新来的量**,否则错配率会假地是 0。

**⛔ 三档的实测词重合全部是 0.167,36 组全 `UNMATCHED`** ⇒ 档位没生效 ⇒ 错配率不可采信。

**★ 诊断即结果**:`ov` 用 `\w+` 找词,**把一整串汉字当成一个词** ——
两段共享大半内容的中文 `ov = 0.000`(英文同义对 0.727);
真实页面上中文版 **16.3%** 的段落被切成 ≤2 个词(英文 6.6%)。
**`#626c` 的警告,在我紧随其后发出的代码里应验了。**

**规则不改**:`#620` 要求先回测。见 `RETRACTIONS.md` `#632`。
