# `E02·A12·R663` — 每条规则,在每一版页面上开得了火吗?

**行动类型:PRODUCTION + FIX**。§3 定的操作化:基线非零 ⇒ **「开火」= 计数上升**;
**g=0 = 注入一个该规则不该抓的东西**(「不注入」是个不可能失败的对照)。

**结果**:可测 6 格,**6 个都开火,0 个失明**;g=0 六格全干净;**4 格 `UNCOMPUTED`**。

⛔ **更正 `#622`**:「结构性开不了火」是错的措辞 —— 注入里带一行不含中文的字,它立刻开火。
**那一页实际含有的每一行都带中文,所以第二侧是空的 —— 不是规则坏了,是页面上没有那样一行。**

⚠ **g=0 对照自己先失败了一次**,原因是 object dtype 上 `~True = -2`,六个干净格被算成 −12。

见 `RETRACTIONS.md` `#627`。
