# `E02·A12·R667` — 一段里同时走两个、来两个:它会不会编一个一一对应出来?

**行动类型:PRODUCTION + FIX**。我在写这一轮之前就知道答案(`cand[0]`),**而我仍然先量**。

**缺陷比预期更糟**:两个离开的量被指派了**同一个**替代者,而第三个候选**一次也没被提起** ——
那不是随便挑一个,是一个在任何方向上都不成立的对应,**而它的自信和正确答案一模一样**。

**修法**(写在跑之前):第四值 **`多候选`**,列出全部。
验证走 `#173c` 的**纯文本支**(不穿过 git)。回测 32 版**零缺失**,接入前后判定**完全一致**。

⚠ 守卫又拦了我一次,拦得对;而**仓库里早就写好了纯文本支这条出路**。

见 `RETRACTIONS.md` `#631`。
