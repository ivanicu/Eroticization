# `E02·A12·R665` — 「改写」和「删除」,在它的输出里分得开吗?

**行动类型:PRODUCTION**。⚠ **§3 改掉了判据的读法**:`for tk in sorted(o-n)` ⇒
「它对改写半盲」是**代数强制的 DERIVATION,不是发现**;真正可测的是「两者分不分得开」。

**结果:两版都分不开** —— 改写臂与删除臂的 token 集合逐字节相同 `['+0.7373']`。
g=0(原样写回)与安慰剂(只新增)**四格全空** ⇒ 那条 DERIVATION 得到实测支持。

**代价**:读者分不出「限定语被抹掉」与「数字被更正」,而这两件事要求的反应正好相反。
**闸门原样未动**(`#620` 回测规矩);变的是这处含糊被写了下来。

收尾核对:分支/HEAD/工作区/临时分支残留 **四项全 ✅**。见 `RETRACTIONS.md` `#629`。
