# `E02·A12·R655` — 还页面欠着的账

**行动类型:CLOSURE**(如实标注)。它不分离任何世界。
判据逐字沿用 `#604c`:**每条改完立即重跑,必须降 2,降 1 或不降都回滚。**

**结果**:`uncited_numbers` **15 → 1**,七条主张各自找到出处并单独通过降-2 判据。

**★ 真正的产物**:最后一条查出一个**孤儿** —— 页面上的 `rape (−0.519, n=17)`
在它的出处表(`#528`,五格)里根本没有。重算 `SCCS173×SCCS174` = n 17,ρ −0.5187,**完全正确**。
**它不是错的,是一个算对了却从没进过账本的数。**

⚠ 我的编辑把 `internal_consistency` 从 3 抬到 4(退化命中),尝试的修法没有改善 ⇒ 按判据回滚,如实记账。

见 `RETRACTIONS.md` `#619`。
