# `E02·A12·R664` — 伪造一次提交,把最便宜的那个 `UNCOMPUTED` 关掉

**行动类型:PRODUCTION**。`numbers_that_left` 的缺陷住在**两次提交之间的差**里,单版注入够不到。

**安全边界先写死**:只在 `tmp/r664` 上做 · `--no-verify` 是**一次性分支上被标注的刻意绕过** ·
`finally` 必须回 `main`、删分支、验干净,**并把收尾核对打印出来**。

**★ 第一次尝试被我自己的守卫拦住** —— 唯一「不干净」的是本轮自己的脚本。
**一个那么严的守卫迟早会被人关掉**,所以收紧到 `-uno`(只看已跟踪,那才是 `reset --hard` 会毁掉的)。

**结果**:两版正对照 **2/2 命中**,g=0 **0/2** ⇒ **`UNCOMPUTED` 4 → 3**。
⛔ 只验了「整段删掉」这一种形状;「改写成另一个数」**本轮没有验**。

见 `RETRACTIONS.md` `#628`。
