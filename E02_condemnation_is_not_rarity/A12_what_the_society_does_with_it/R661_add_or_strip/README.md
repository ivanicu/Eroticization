# `E02·A12·R661` — 那三十次措辞更正,是在补还是在删?

**行动类型:CLOSURE**。⚠ **§3 改掉了 `#624` 的做法**:「由我逐条归类」本身是一台判断仪器
(无正对照、不可复现、而我是被测对象的作者)⇒ 改成机械口径(`_QUAL` 的增删行数之差)。
「换范围词」机械上分不开 ⇒ **单独标 `UNCOMPUTED` 并说明它需要什么**,不许标 planned。

**结果**:(b) 补 4 · **(c) 删 1** · **(a/d) 分不开 25** · 不涉及 1 ⇒ **分辨率 5/30**。
预注册判据要求 (c)≤2 **且** (a/d)≤2 ⇒ **条件不成立 ⇒ 报区间,不下判决**。
`#623d` 的缺口**仍开着**,而且现在带着一个测得的分辨率。

⚠ 那唯一一次删限定语**只在中文页**;只数英文页时是 **0** —— 本次会话第三次「计数取决于看哪一版」。

见 `RETRACTIONS.md` `#625`。
