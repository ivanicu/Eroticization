# `E02·A12·R659` — 回测:这条规则在历史上抓到过什么?

**行动类型:CLOSURE**。`#620` 立的规矩「改规则必须先回测」,本轮跑掉。

**§3 梯度检查决定了判据**:现规则当前四格全是伪影 ⇒ 「候选必须抓到现规则抓到的每一个」是**空真**。
所以「真缺陷」的定义**取自账本**,不取自现规则。

**回测总体**:36 条账本记过的页面更正 —— **29 条是措辞更正(数字规则结构上抓不到)**、
6 条映射不到提交、**可测 1 条**。

**判决**:唯一可测的 `#299` 上,现规则 ✅ · 候选① ✅ · **候选② ⛔ 漏报** ⇒ **不换规则**。
⚠ **分辨率 n = 1,与判决写在一起。**

**★ 真正的产物**:五分之四的页面更正是措辞不是算术 ⇒ **数字闸门对多数真错误结构性失明**。

见 `RETRACTIONS.md` `#623`。
