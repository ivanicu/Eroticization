# `E02·A12·R673` — 这个项目在 SCCS 上还能问什么:按来源论文画的边界图

**行动类型:普查(ENUMERATION)+ PRODUCTION**。`#636c` 立的边界 ⇒ 「还能问什么」的单位是 **source**。

**§3 两条写在前面**:O(k²) ⇒ 抽样上限 300 并标出被抽样的 20 篇;`k==1` 的 source 连一对都没有,
**那是 g=0 该落的地方**。

**结果:58 篇里 44 篇合格**(k≥3 且对内中位 n≥30)。正对照 `ross1983political` ✅(对内中位 82);
g=0:4 个 `k==1` 的 source 无一合格 ✅。

**★ 而本项目用过的那几篇一查就把整条弧解释掉了**:
`broude1976cross`(二十条性态度)**对内中位 n = 24,不合格**;`ross1983political`(暴力)**= 82**。
**记录性态度的那一篇内部是稀的;记录暴力的那一篇内部是密的。**

见 `RETRACTIONS.md` `#637`。
