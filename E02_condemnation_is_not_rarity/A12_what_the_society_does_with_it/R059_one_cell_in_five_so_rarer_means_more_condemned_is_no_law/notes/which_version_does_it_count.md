# `E02·A12·R662` — 每条规则,它数的是哪一版?

**行动类型:PRODUCTION**。同型三次之后,把它变成规则的一个属性。

**关键设计**:**量出来,不是声明出来** —— 拦截 `pathlib.Path.read_text`,记录实际打开的 `README*.md`。
探针对照:只读一版 -> 1 · 两版 -> 2 · 不读 -> 0。

**结果**:七条规则**全部读两版**;加拦 `open` 的第二档探针,覆盖数变化 **0 条**。
已接入 `readme_gate`,接入前后所有判定计数**完全一致**。

⛔ **而造出来的属性不是需要的那个**:读了两版是**必要不充分**的 ——
`#622` 已测 `internal_consistency` 读了中文页却在它上面**结构性开不了火**。
所以闸门输出里,覆盖数旁边**永远印着这一句**。

见 `RETRACTIONS.md` `#626`。
