# `E02·A12·R671` — 禁忌铺得越宽,罚得越狠吗?(两个死因,不计算任何相关)

**行动类型:FRONTIER**,结论:**两个互相独立的死因**。

**死因一(计算之前)**:`SCCS959` 是**名义类型学**(母系不比交表「更远」)——
把它当序数正是 `#529a` 杀过的那个错。
**死因二**:可辩护的二值退路里,**16 个有延伸、1 个没有 —— 没有对照组**。

**★ 本轮真正的产物**:`#634` 预注册的停止规则是「n < 15 就停」,而 **n = 17,判据不触发** ——
**样本量守的是功率,这里死的是识别。一条只盯着 n 的停止规则,会让一个无法识别的问题走到计算那一步。**

⚠ 同样这 17 个里藏着一个几乎完美平衡的对照(双边 7 对单系 8),**但它回答的是另一个问题,
而在看到预注册的那个已死之后去挑它,正是本项目要拒绝的那一步** ⇒ 登记为候选,本轮不做。

见 `RETRACTIONS.md` `#635`。
