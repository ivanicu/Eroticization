# `E02·A12·R660` — 那 30 次措辞更正里,有多少次是「限定语被删而数字留下」?

**行动类型:PRODUCTION**(如实标注)。

**★ P4 救了一次**:我预注册的判据(删句含限定词且无小数)**比仓库里已有的
`qualifiers_stripped` 更差** —— 后者要求**被遗弃的量**,那才是缺陷形状。改用它。
⚠ 而它**从没被接进 `readme_gate`** —— 「造好没被调用」第三次。

**⛔ 第一版正对照失败在代码里**:`_MAGNUM` 只认带符号小数/百分数/倍数,我用了裸 `0.432`。
改成 `+0.432` 后开火。**修好对照之前,主测的零不可采信(P5★)。**

**结果**:30 次措辞更正,**开火 0 次**,管道全通;正对照 1、g=0 两支各 0
⇒ **零是测量,不是沉默** ⇒ **不接进闸门**(门槛 50%)。

见 `RETRACTIONS.md` `#624`。
