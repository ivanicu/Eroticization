# `E02·A12·R657` — 那条规则的四格,是真不一致,还是退化命中?

**行动类型:CLOSURE**(如实标注)。判据在 `#620` 已写死:**只有四格全是退化才允许改规则**。

**读源码不读名字**:`internal_consistency` 是**行级**的,按「行含不含 CJK」分两侧比较数字并集。

**三个实测后果**:① 中文页 **0 格,永远** —— 它从没检查过中文版;
② 英文页只有 **4** 个标记出现在两侧,**全部已在报警**,两侧相等的 **0 个**
⇒ **没有阴性样本 ⇒ 正对照无法搭建**(三次尝试全败);
③ 单位是**行** ⇒ 一个标记继承整行的数字,两格在比 **26 对 333**。

**判决:退化 1 · 真不一致 1 · 判不了 2 ⇒ 条件不成立 ⇒ 规则原样未动。**
见 `RETRACTIONS.md` `#621`。
