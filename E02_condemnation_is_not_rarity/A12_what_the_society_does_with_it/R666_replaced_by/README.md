# `E02·A12·R666` — 给闸门加一列:那个数是被拿走了,还是被换掉了?

**行动类型:PRODUCTION**,**已接入**。§3 决定输出必须**三值**:`替换` / `拿走` / **`UNMATCHED`**。
**把配对失败折进「拿走」会印出一个自信的错答案;让它拥有自己的名字,印出的才是诚实的答案。**

**结果**:改写臂 → `替换 +0.8484`(两版)· 最小删除 → `拿走`(两版)·
**整句改写的删除 → `UNMATCHED`**(两版,启发式的代价被标价)· 安慰剂 → 空表。
⚠ 「删除(最小)」是**看到第一次结果之后加的**,如实标注,两臂都报。

**回测**(`#620` 硬要求):32 个历史版本,**漏报 0** ⇒ 接受并接入;接入前后判定计数**完全一致**。

⚠ **本次会话第三次「对照因 dtype 而假失败」**(`None` → `NaN`)。见 `RETRACTIONS.md` `#630`。
