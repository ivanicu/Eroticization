# additivity

**Rounds r50–r52, 3 of the project's 52.**

Ivan's model B is v = w^T phi(s), which requires additivity. The fluid option template instantiates one feature contrast across seven substances.

| round | directory |
|---|---|
| `r50` | [`r50_additivity`](r50_additivity) |
| `r51` | [`r51_source_or_orientation`](r51_source_or_orientation) |
| `r52` | [`r52_which_pairs_transfer`](r52_which_pairs_transfer) |

Findings, intervals and caveats for every round live in the top-level [`README.md`](../README.md); this file is a table of contents, not a second account of the results — one home per fact.
