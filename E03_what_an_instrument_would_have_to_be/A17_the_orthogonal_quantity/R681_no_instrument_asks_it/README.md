# R681 · 那个正交的量,五个仪器里没有一个问过它

**类型:CLOSURE**(如实标注 —— 它**关掉一条路**,不开新世界;它保护的是 `#643`,给它一个边界)。

`#644` 的 NEXT:`#643` 留下一个形状 —— **一个测得极稳(自身 +0.9215)、却与周围一切都正交的量**
(以身作则:与其余四种管教手段 +0.0450,与四个结构变量 +0.0459)。
**而它在 SCCS 里已经问不动了** —— 结构变量也是同一个团队编的,`#643` 的 ② 判不了正因如此。
**⇒ 换仪器(硬规则④)。**

## 硬规则①:先打印,再下结论。而每一个零都必须先有正对照(P5★)

| 仪器 | 变量 / 标签 | 正对照 | 目标命中 |
|---|---|---|---|
| **GSS** (1972–2024) | 6,943 变量 / 75,699 行 | 6/6 ✅ (`premarsx` `homosex` `xmarsex` `spanking` `obey` `fefam`) | **1 真命中** |
| **BRFSS 2023** | 350 | 4/4 ✅ | **0** |
| **NSFG** | 2,603 标签 | 327 ✅ | **0** |
| **YRBS (SADC 2023)** | 269 标签 | 18 ✅ | **0** |
| **Open Psychometrics** | 12,461 行(拆 zip 后) | 30 ✅ | **2,全假阳性** |

**那唯一的真命中撑不起来**:`prntmdl`「MOTHER VS FATHER ROLE MODEL」 ——
**n = 1107,只问过 2022 一年**,而且它问的是**哪一位家长**,不是**有多少榜样教育**。
**它不是 SCCS 那个量,连近似都不是。**

Open Psychometrics 的两个命中是 `HFair6 I admire a really clever scam`(HEXACO 公平题)与
`Q33 I tend to be admired by others` —— **子串,不是构念。**

## ⚠ 两次「一个 grep 是一件测量仪器」,都在本轮内

**①** 第一遍扫的是**文本文件**,而 GSS 只有 1 个可扫文本文件、BRFSS 有 0 个 ——
**变量标签住在 `.dta`/`.XPT` 里面**,文本扫描根本看不见。**扫错了层,零毫无意义。**
**②** `imitat` 这个模式抓到了 `hlthdays`「activity **limitat**ion」。四个「命中」里三个是子串假阳性。
**③** Open Psychometrics 第一遍**正对照返回 0**(标签 0 条)—— 文件是 `.zip`,扫描进不去。
**按 P5★ 那个零是不可采信的**,拆开 zip 后正对照 30 命中,零才成为一次测量。

## 结论(预注册②:一个也没有 ⇒ 这就是答案,写进页面并停)

> **「让孩子看着大人做事来学」这件事,在这份工作能拿到的五个仪器里,没有一个问过。**
> 它在 SCCS 里被问过、被测得极稳,而**它在别处不存在,所以 `#643` 那个形状无法被跨仪器复制。**

**不许从 SCCS 内部再找第四个候选**(硬规则③)—— 这一格进「这份数据做不到什么」。

## 顺带查到的一条真路(记下,不在本轮用)

**GSS `spanking`「Favor spanking to discipline child」:n = 33,936,22 个年份(1986–2024)。**
不是那个正交量,**但它是 A12 主题在**人**这个单位上的一条真正的换仪器路** ——
A12 全部结论都在**社会**这个单位上。

## 本轮结构上做不到的(不写 planned)

只扫了变量**标签**,没扫题干全文(GSS 的题干在 PDF 码本里)· 五个仪器不是全部现存仪器 ·
`[unchallenged]`
