# R109 · 「换对象几乎不改变」是不是单项目伪影

**类型:FRONTIER**。**判决:W1 —— 不是。在另一套码、另一份出版物上重现。我第六次下注下反。**

**跨仪器:换不了仪器,只此一具**(`#664` 穷举 D-PLACE 五库;`#665`:跨编码项目 ≠ 跨仪器)。

## ⑤ 的预言应验了一半,而穷举的 bug 差点让我看错另一半

**`lang1998conan` 只编了一个对象**(`SCCS1766`;`SCCS620` 是配偶不是儿童)
⇒ **体罚上的跨项目「换对象」构不成。** 可证伪形式:
*若出现 `source ∉ {barry1977agents}` 且按 ≥2 个对象分列的儿童体罚变量、真实 n ≥ 60,即被推翻。*

## ⚠ 而我在「先穷举再说话」这一步上犯了 `#617` 记过的同一个缺陷

**第一次穷举把每个变量的 n 都印成 186 —— 因为我数的是 `data.csv` 的行数,不是非缺失值。**
按真实覆盖重算:**75 个命中里 21 个行数 ≥150 而真实 <60。**
`rohner1981parental` 按性别分列的温暖变量真实 n = **5 / 6**,联合 **4** ——
**我差点拿它当第二个项目跑一整轮。**

> **一个为「先查再说话」而设的步骤,自己带着一个假覆盖的 bug。**

## 而真实覆盖翻出了一条真的路

**`barry1976traits` 的 `SCCS322–325`「Obedience:幼年/晚童年 男/女」,真实 n = 160–162** ——
**完整的四对象切分,出自与 `barry1977agents` 不同的出版物。**

## 结果

| 四件套 | 6 对中位 | 归一中位 |
|---|---|---|
| **体罚(Barry 1977)** | **+0.8392** | +0.8679 |
| **服从(Barry 1976)** | **+0.7905** | +0.8319 |

**✅ 正对照** 体罚复现 `#640` 的 +0.8392,分毫不差。
**✅ 独立性检查(不假定,实测)** 服从 × 体罚 的 16 对跨构念 |ρ| 中位 = **0.2951 < 0.90**
⇒ **是两套不同的码,不是一套。**
**⚠ 安慰剂** 服从 × |纬度| = **0.2897 = 效应的 37%** —— 过了 50% 的线,**但偏高,如实记。**

## 站得住的那一句,和它的边界

> **一个社会对男孩和对女孩、对幼儿和对大孩子做的事,几乎是同一件事** ——
> **而这在两套彼此只相关 0.295 的码上都成立**(体罚 +0.839 · 服从 +0.791)。

**⇒ 这正是 `#642` 那个形状在一对新构念上的重现:同一套码内换对象几乎不动,跨构念几乎无关。**

⚠ **边界**:`barry1976traits` 与 `barry1977agents` **第一作者同为 Barry**
⇒ **跨出版物,不是跨独立研究组。这是这一轮拿不到的东西,写进「做不到」。**

## 本轮结构上做不到的(不写 planned)

**同一第一作者 ⇒ 非独立团队** · **体罚上的跨项目换对象构不成**(Lang 只有一个对象)·
安慰剂 37% 偏高 · **换不了仪器** · `[unchallenged]`
