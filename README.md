# The Eroticization Operator

**"Sexual" — is it a content category the brain detects, or a value the brain assigns to ordinary
world-representations?** 329 self-attacking rounds against one public dataset, asking what can be
settled before anyone collects anything new. **283 numbered ledger entries** — [RETRACTIONS.md](RETRACTIONS.md)
carries the history; this page carries only what currently stands. **中文版:[`README_zh.md`](README_zh.md)** — English is canonical; corrections land here first.

Private curiosity project. Not written for publication and not seeking any.

Object: the **Big Kink Survey** public subsample — [Zenodo 10.5281/zenodo.18625141](https://zenodo.org/records/18625141),
15,503 respondents × 365 columns, aggressively binned, demographically stripped, noise-injected.
Data is not committed — neither the raw release nor the person-level derived tables; `E01…/A01…/R001_schema/run.py` re-downloads and rebuilds them. **[CORRECTED before publication]** 24 derived `data/derived/*.csv` files, two of them with one row per respondent, were tracked until now, so this sentence was false; they are untracked and gitignored as of this commit. They remain in the commit history.

> ### How big is any of this? (`#264`, read this before anything else)
> Every person-level dimension this project established, **entered together** against the same 29
> outcomes, explains a **median of 1.4%** of the variance — **7.8% at its best** (`animated`), **2.8%**
> for shame. The degrees-of-freedom floor is **0.14%**, and the same pipeline reaches **49%** when a
> deliberately strong predictor is added, so the machinery is not blind and the dimensions are not
> noise: they are real, replicable, cross-instrument, controlled — **and they are small**.
> **Two readings this does not license.** It does *not* mean the findings are empty (the floor is
> 0.14%, and every dimension clears its own family-wise threshold). It does *not* mean sexual
> preference is mostly random — **1.4% is the reach of *these six coordinates*, not a ceiling on
> explainability**. ⚠ And note the ordering: the *least reliable* standing dimension (position, 0.432)
> contributes the *largest* incremental R² (0.715%). **Reliability and explanatory power are different
> things — a quantity can stably measure something that hardly moves anything.**
> ⚠ **`#301` `#302`:**in-sample 1.4% / out-of-sample 0.87%**. Held out — 5-fold person-level cross-validation with the six coordinates **re-estimated inside every fold** — the same pipeline delivers a median of **0.869%**; with the coordinates held fixed it is 1.087%, and ridge-CV 1.056%. On 2 of the 29 outcomes the held-out R² is **negative**. **Over a third of the published number is fit.** The estimator is the largest single knob in [`CALIBER.md`](CALIBER.md) (**0.52pp**, against 0.18pp for every analysis knob). Both numbers are legitimate and they answer different questions — 1.390% is what these coordinates explain **in the people they were built from**, 0.869% is what they deliver **on people they have never seen**. ⚠ **`#356`: and part of that 1.4% is co-linear with how much a person reported.** Adding "how many onset categories you filled in" to the six coordinates and doing a commonality decomposition across all 29 outcomes, the shared component is positive for **22 of 29** outcomes, median **0.345pp** — **18.2% of the median outcome's explained variance** — and it grows with how well the outcome is explained (`r` = **+0.609**). Swapping the breadth proxy for block coverage keeps it (25/29 positive; the two shared vectors correlate **+0.892**). ⚠ Commonality is a bookkeeping term, not a causal one: it says this part is shared, not which side carries it.

## The three models being separated

Ivan's framing, kept verbatim because the whole repository is scoped by it:

| | |
|---|---|
| **A** dedicated sexual-content system | `Y_i(s) = f_i(h_sex(s))` — "sexual" is a content category like *face* in vision |
| **B** erotic valuation of ordinary representation | `v_i(s,c,t) = w_i(c,t)^T h(s)` — ordinary scene, a readout applied to it |
| **C** recursive mix | value re-enters and reshapes the representation it acted on |
> ### Where the three models stand (`#234c`)
> **B is dead** (`#179`: position is not a stance-free coordinate — it comes with shame).
> **C's one distinguishing prediction is unsupported** (`#234a`: stratify by shame, match pick count,
> and the breadth profile's split-half replication is **0.4672 / 0.4611 / 0.4718** — flat, in a design
> whose positive control fires at a gradient **7.4× smaller** than the one C predicts).
> **But the honest reading is not "A wins."** The most reliable person-level structure in this release —
> split-half **0.56–0.72**, against the position score's 0.432 — is *which domains a person opens in and
> in what shape* (`#229`–`#232`). **None of A, B or C predicts it**, because all three ask what makes a
> thing sexual, and this is a fact about the **person**, not the stimulus.
> **The A/B/C decomposition does not cut this data at its joints.** That is a claim about the
> decomposition's completeness, not a refutation of A.


---

## The person-level dimensions this release actually supports

`#234c` found that the A/B/C decomposition does not cut this data at its joints: all three ask what makes a
*thing* sexual, while the most reliable structure here is about the *person*. Those dimensions were scattered
across a dozen ledger entries; this is all of them in one place. **Every number below appears verbatim in
`RETRACTIONS.md` — the round that built this table (`#251`) fails if one does not.** Empty cells say
`UNCOMPUTED`; they are the honest part.

| Dimension | split-half | instrument | cross-instrument | ↔ shame | outcomes cleared | strongest rival |
|---|---:|---|---|---:|---:|---|
| **Position S** — how uncommon the options you pick are | **+0.432** | blocks (32 multi-select) | untested | **+0.1185** | 9/20 | pick count controlled (+0.1155) · sex · content score |
| **Between-block contrast D** — which domains you open in | **+0.6785** | blocks | **verified** (onset instrument, +0.1384) | +0.0830 | ~8–10/31  ⚠ **`#259`: this row and the breadth-type c3 row overlap heavily** — their outcome profiles correlate **+0.7826** against an attenuation ceiling of 0.9142 ± 0.0171, i.e. **86% of ceiling**, resolvably below it (7.7× the spread) but by only 0.13 in absolute terms. They are kept as two rows because the residual is resolvable, not because they are independent. | pick count near-absent on the onset instrument (+0.0428) |
| **Breadth profile** — the shape you open in | **+0.4290** | blocks | **verified** (≥2 dimensions; onset k=2 replicates at 0.9379) | its per-person **clarity** gives +0.0148 ± 0.0116 (1.3×, one seed) | **8 of 29, read as 3–13** across six option-split seeds (`#255` — ⚠ `#254`'s "predicts nothing" is **withdrawn**: that reading came from one unlucky seed) | pick count controlled · block-level property ruled out (`#228b`) |
| Breadth type c1 | **+0.6429** | blocks | untested | `UNCOMPUTED` | 13 → **9 after control, read as 6–12** (`#257`) | **pick count · sex · position S all controlled** (`#252`) |
| Breadth type c2 | **+0.5778** | blocks | untested | `UNCOMPUTED` | 11 → **6 after control, read as 4–8** (`#257`) | **pick count · sex · position S all controlled** (`#252`) |
| **Breadth type c3 — *where you spend your openness*** | **+0.5631** | blocks | **name verified** on the disjoint onset instrument (`#258`: +0.1815 with its transgressive half, **−0.1086** with its ordinary half, gap 18.6×) | **+0.1286** | 21 → **19 after control, read as 17–21** (`#257`) | correlates only +0.2036 with position; both survive jointly; total category count controlled — the gap **grows** to +0.4905 ⚠ **`#366`: and this route is not as clean as the other two.** Against a panel of **20** non-coordinate person-level variables (age, sex, all five Big Five traits, powerlessness, mental-illness and childhood-adversity flags, upbringing, relationship style, gender tolerance, politics, straightness, BMI, partner count, pick count, porn habit, self-reported honesty), a commonality decomposition on shame leaves c3⁻ with **59.6%** of its own R² (unique 0.996pp of 1.671pp; shared **+0.674pp** against a row-permutation null of +0.007 ± 0.017). **13 of the 20 exceed their own permutation threshold individually**, though no single one takes more than **18.9%** (partner count), then extraversion 14.9%, age 8.8%, neuroticism 8.3%, powerlessness 7.3%. **All five biggest bites point the same way**: younger, fewer partners, less extraverted, more neurotic, less agency, together carrying about 40% of this route's shame. The other 60% none of the twenty can see. ⚠ **`#367`: that 40% is ONE dimension, not twenty — but half of the sentence above was rhetoric.** Sign-aligned, all 10 off-diagonal correlations among the five are positive (median +0.092) and PC1 takes **28.9%** against an offset null of **20.88% ± 0.23%** (+35 sd) — the null is not zero because sign-aligning on a common target *induces* a floor, so it is built by synthesising five variables with the observed \|r\| to c3⁻ and nothing else in common. **A real single factor gives 59.6%, so the common direction is genuine but thin.** And that single PC1 takes **37.1%** of c3⁻'s shame where all twenty take 40.4% — **92% of the work of twenty variables done by one dimension** — against random 5-subsets taking **8.3% ± 7.7%** (beyond all 300 draws, +3.8 sd), which is the control the selected-on-the-same-data five require. ⚠ **What is withdrawn: the sign agreement itself carries almost no information.** 16 of the 20 variables are same-signed, so a random 5-subset agrees **28.2%** of the time. ⚠ **`#369`: and the sharing is uniform along that line — the two numbers above are not in tension.** A quintile split had no power to say so (MDE +0.20 against a meaningful +0.05, `#368`), so the question was re-estimated as a single interaction on all 6,609 people: `c3⁻ × PC1` = **−0.00042** (se 0.0120, 95% CI [−0.024, +0.023]), **−0.04 sd** from an offset null built by permuting **only** PC1 — the null is not zero because the product term is collinear with both main effects (corr(c3⁻, PC1) = +0.1632). **MDE 0.030 for linear moderation and 0.150 for a step confined to the top quintile**, and the quintile bound already excludes steps ≥0.20, so the two shapes leave no gap. **Weak covariance and strong sharing coexist: sharing needs the direction to be right, not the magnitude to be large.** In plain terms — this line sets how much shame a person carries, not whether their taste will shame them. ⚠ For scale, not as a test (k differs): a variable that genuinely **is** c3⁻ plus equal-magnitude noise leaves 54.8%. ⚠ c3⁻ is estimated in-sample (`#347`), so the absolute pp is an upper bound; the retained fraction is the transferable quantity. |
| **rho_i** — whether your rare interests come first or last | **+0.5316** | onset ages (31 categories) | n/a (blocks carry no time) | `UNCOMPUTED` | 11/31 | current age controlled (+0.0226) · recall bias controlled (`#114` — **`#452` corrects this citation, which read `#289`; that entry is about measurement invariance by sex**) · 56% survives a cross-half split |
| **`form_i` — what "opened by drawn things" and "opened by written things" share** | **+0.3382** (two items) | blocks (the residual of `animated`/`written` after all six coordinates) | **verified** — +0.0799 on the disjoint onset instrument, 3.9× its permutation spread (`#277`) | **+0.0454**, but its increment over the six is **+0.21 ± 0.13pp — indistinguishable** (`#278`) | **9–11 of 27** (its own two items excluded) | the six coordinates are removed by construction; its correlation with `c3⊥D` is an **identity**, not evidence (`#277`); it shares only +0.1212 of profile with `rate⊥rho` on the disjoint instrument (`#283`) |
| ~~Accumulation rate~~ | +0.6931 | onset ages | n/a | `UNCOMPUTED` | 6/31 | **withdrawn as a dimension** — its profile sits at the ceiling against rho_i (−0.8499 vs 0.8779) |

**Read the reliability column first.** The three breadth types (0.56–0.72) are the most reliable person-level
quantities in this release, and they were the last to be found. The position score (0.432), which carries the
project's oldest and most-cited claim, is the least reliable of the ones still standing.
---

> ⚠ **This table is built on the pooled sample, and the six coordinates are not the same thing in the two ⚠ **`#288`: dropping every item that mentions male/female/gender halves this but does not remove it** — on the 24 non-gender-referential outcomes the gap is **−0.1229**, and on the 22 that remain after excluding the two cells already seen it is **−0.1172, still 3.0× its own spread**. **Roughly half the non-invariance comes from a few gendered items; the other half does not.** ⚠ The gender-referential subset itself is only 5 outcomes — too few for a correlation, so it is `UNCOMPUTED`, not zero, and the "half" is a subtraction (`D5`), not a measurement. ⚠ **`#293`–`#294`: the two groups do not even agree on what counts as uncommon.** The rarity vector `rar = −log(option prevalence)` is computed once on the pooled sample and applied to everyone; median within-block prevalence correlation is **+0.9039** across the sex split against **+0.9973 ± 0.0003** across random splits of the same sizes (**148.6× the spread**). ⚠ **But giving each group its own ruler does not remove the non-invariance**: rebuilt with within-group rarity, the gap goes from −0.2061 to **−0.2270** (**110.2% retained**), with a `SEX × S` interaction sweep flat at g=0. **Both facts are true and neither causes the other** — the difference is not in the ruler, it is in the thing being measured. ⚠ **`#296`: the outcome side is not yet controlled.** A group difference in the joint-R² curve can also come from the outcomes themselves (range restriction, response style), and partialling a response-style reference curve leaves **84.3%** of the gap — but that arm's positive control **fails** (planting noise in six outcomes for one group, the partialling makes the artificial gap **larger**, recovery **−18.5%**), so it is UNVERIFIED, not a confirmation. **The sex non-invariance is reported here at D5, not D6. ⚠ **`#381`: and it cannot be localised to the position score S — not because it isn't there, but because this dataset cannot ask.** With shame rank-transformed within each group, S's slope difference is +0.033 (|t| 1.16) against a family-wise threshold of 2.38 — but in the same model and the same run, **`EARLY`, which `#380` established as genuinely different, reaches only |t| 1.73 and also fails.** An instrument that cannot detect a difference it is known to have says nothing by its silence, so this is UNVERIFIED. **A synthetic positive control planted at 0.25 passes the same test at |t| 6.82** — it would have licensed the claim that S shows no sex difference, and the real-data reference is what stopped it. **Settling S would need the wide caliber's power, and S's definition needs ≥8 blocks; the two requirements exclude each other.** So this is a named wall, not an undone task. ⚠ **`#414`: and there is a second wall, of a different kind — the category level tops out at n = 31.** The onset instrument has exactly 31 categories, so any category-level correlation is estimated on 31 points: the offset null for such a correlation already has an sd of **0.146** and a 95th percentile of **0.292**, giving an MDE of about **0.45**. **Nothing smaller than that is visible at the category level here, and no amount of data adds categories.** The two walls differ in kind: S's sex difference needs power the wide caliber cannot supply because S itself requires ≥8 blocks; the category level needs more categories, and there are none. ⚠ A worked illustration of what that costs: ordering the 31 categories by how much earlier or later they arrive than their rarity predicts, the two latest have mean shame 0.591 and 0.756 against 0.370 and 0.467 for the two earliest — which reads like a clean positive relation while the actual correlation across all 31 is **+0.074**. **The two ends of a 31-point ordering are its noise, not its shape.**** ⚠ **`#298`–`#299`: the two readings cannot be separated on this data.** Rarity recomputed inside each person's own reference group (sex × age) separates content stigma from deviation-from-people-like-me — but only where the two rarity definitions disagree, and there they behave the same. Over the option-subset grid (keep the most reference-group-divergent 10/25/50/100% per block) the two definitions decorrelate from **+0.8865 to +0.6722**, and both routes **strengthen together** (+41.6% and +62% among people readable both ways) while their difference stays at **+0.0113 against a detectable +0.0918**, with the sign flipping in one of four cells. **The earlier reading that only content carries shame held on the full option set, where the two definitions are 90% the same quantity — it does not survive the grid.** ⚠ **`#304`: and the two routes are not equally solid objects.** Split the respondents in half twelve times and estimate each direction independently: `c3` points at itself across halves at **|cos| = 0.796 ± 0.103** (worst split 0.592) against a random-direction floor of **0.139** — a real direction, but a soft one. `c1` reaches 0.957, the three-dimensional subspace 0.852, and the structure behind **S reproduces at 0.997**. Changing the option-split seed moves c3's figure only 0.039. ⚠ Half-sample |cos| **understates** the stability of the released n=6,717 estimate. ⚠ **`#308`: and in a single person the two routes ADD.** Split at the medians, mean shame runs **0.383** (low on both) · **0.700** (unusual taste only) · **0.718** (transgressive spending only) · **1.077** (both) against an additive prediction of **1.034** — someone high on both carries **2.81×** the low/low group, which is exactly the sum. The continuous interaction is **−0.0150** against a permutation null of +0.0051 ± 0.0135, and the same test recovers a planted interaction of 0.15 as **+0.1502**. It survives controlling pick-count and splitting by sex (−0.024 … −0.008). **This is a null with power, not an absence of evidence.** ⚠ ** The interaction null carries its own resolution: **MDE = 0.038** against a meaningful interaction of ~0.05 (half a main effect). `#311`: and nothing buffers it.** Age, openness, a non-monogamous relationship style and a sexually liberated upbringing — a joint test over all eight (route × moderator) interactions gives an R² drop of **0.094pp** against a permutation null of **0.121 ± 0.061pp**, with **58% of permutations exceeding the observed value**. The same test detects a planted buffer of **10%** in three of the four variables (30% for openness), against **54.8%** for the single-coefficient test it replaces. **This is a null with power**: if any of the four took even a tenth off the shame coefficient, this design would have seen it. ⚠ **`#384`: and what shame changes is what a person has done, not what they believe about themselves.** Two items split this cleanly: whether they think they could stop being aroused by it (*Impossible* … *with little effort, yes*) and whether they have acted on everything that arouses them. Controlling S, c3⁻ and category count, shame predicts **−0.0833** (se 0.0123) on having acted — **−6.85 sd** past its own permutation threshold of 0.0238 — while on believing it could be stopped it predicts **+0.0135** against a threshold of 0.0248, **a null with power** (MDE 0.03 against a meaningful 0.05, and the rank-transformed version agrees in sign at +0.0144). **Two pre-registered readings both die**: shame as mobilised control (which predicts a positive belief coefficient) and shame as resignation (which predicts a negative one). ⚠ Direction is not identified — “shame suppressed the acting” and “having acted left less to be ashamed of” both fit this correlation. ⚠ **`#385`: one candidate explanation can be removed, though — it is not manufactured by when the interests arrived.** Splitting respondents into onset quartiles (median 12.5 / 14.7 / 16.7 / 19.9 years), the shame→acted coefficient is **−0.099 / −0.064 / −0.049 / −0.123**: all four negative and all four past a within-layer permutation threshold of 0.046, with a range of 0.074 that does **not** clear the 0.090 threshold from random equal-size strata. **The relation holds at every onset age and at the same size.** ⚠ This is partial orientation, **not an instrument**: onset age correlates with shame directly (−0.102), so the exclusion restriction fails and nothing here is causal — one rival explanation is gone, that is all. ⚠ Stratifying on a shame-correlated variable narrows within-layer shame variance, so only signs and threshold-crossings are compared, not magnitudes against the −0.083 pooled figure. ⚠ **`#386`: and the rival mechanism leaves its own trace — but the trace belongs to experience, not to shame.** If having acted is what reduces shame, the route runs through discovering the interest is still there, so people who have acted more should believe more strongly that it *cannot* be stopped. It checks out: `acted → belief` is **−0.0387** (se 0.0122, rank-transformed −0.0399), **−2.97 sd** past a threshold of 0.0246 built by permuting acting within the analysis mask. **And shame itself predicts that belief not at all** (+0.0135, a null with power, `#384`). ⚠ Strength is not comfortable: the design's MDE is 0.05, exactly the meaningful magnitude and above the observed 0.039, so roughly a quarter of replications would miss it — direction reportable, magnitude not. ⚠ Still not causal: acting and belief are measured at the same time, so this says the predicted trace is present, not which came first. ⚠ **`#387`: and the obvious next test is empty, by algebra rather than by power.** Controlling for acting inside the shame→belief regression moves the shame coefficient by exactly **−0.003221**, which is exactly **−a×b** from the two paths already reported — the omitted-variable identity of standardised OLS, agreeing to six decimals. It could not have come out otherwise, so it carries no information about the world. **The distinction matters for what to do next: a low-power test can be rescued with more data, an algebraically empty one never can.** The direction of this chain is not identifiable from these measurements, and no sample size changes that. ⚠ **`#388`: what does predict that belief is having the ordinary things work for you too.** Scanning 112 numeric person-level variables against the belief residual (after removing acting, S, c3⁻, category count and shame), the largest is **+0.0888** against a max-of-112-noise-columns threshold of 0.0419, and the top hits form one family — dirty talk, ordinary sex, oral sex — whose mean scores **+0.1159** against a single-variable null of 0.0219. **Overall rarity S is already controlled**, so this is residual to how unusual the person's tastes are: ⚠ **`#392` REVERSES THE DIRECTION OF THIS ENTIRE NOTE — and of `#390` and `#391` with it.** These five items are the complete `RATING_NEG_FIB` set (the survey has exactly five), and their values run only over {0, −1, −2, −3, −5, −8}, never positive, with **−8 the mode** and 0 the rarest response. Anchored against two count variables whose direction is fixed by construction, the five-item mean correlates **−0.4502** with `Totalsexacts` and **−0.1866** with total fetish categories — same sign, with the duplicated cunnilingus item pair correlating +0.697 as an internal-consistency positive control and noise-versus-anchor at −0.003. **So −8 is the erotic end: a higher score means the conventional works LESS well.** The correlation of +0.1255 therefore reads: **the less ordinary things do for someone, the more they believe they could stop.** ⚠ And the construct is not “conventional content” either — at −0.45 with the count of endorsed sex acts, the score is substantially general sexual responsiveness inverted. The original reading is kept below rather than deleted, because what makes this worth recording is that four rounds of gates passed over an unchecked sign: ~~it is not that conventional people feel changeable, it is that people for whom the conventional **also** works do.~~ ⚠ The family was defined after seeing the top hits; the pre-registered quantity is the maximum, which clears on its own, and the family score is a description of those hits rather than an independent test. ⚠ **`#389`: and that description does not survive — redefined by the questionnaire's own block structure it changes sign.** Using the two blocks `#327` identified as c3's ordinary end (`sex acts`, `sensations`) instead of five hand-picked items, the correlation is **−0.0259** against a single-variable threshold of 0.0253 — opposite in sign to +0.1159, with the positive reference firing and the negative control silent. **The family reading is withdrawn**; the pre-registered maximum (+0.0888 against 0.0419) never depended on that selection and stands. ⚠ The two definitions are not the same construct — hand-picked items rate *how erotic* conventional acts are, the block version counts *how many* options were ticked and correlates +0.34 with total picks — but that is a limitation of the replication, not a rescue of the claim. ⚠ **`#395`: and the same score also predicts shame — it is the common antecedent of two things that are unrelated to each other.** With the direction now fixed, the five-item score predicts shame at **+0.0827** raw and **+0.0657** controlling S, c3⁻ and category count (se 0.0126), **+5.10 sd** past a permutation threshold of 0.0247. So the less ordinary things do for someone, the **more** ashamed they are — while the same score independently predicts believing they could stop, and shame and that belief are themselves unrelated (`#384`). ⚠ MDE 0.05 exactly equals the meaningful magnitude here, so the strength is not comfortable. ⚠ **`#396`: and a whole class of follow-up tests is closed — by algebra.** Adding a control M and reading where X's coefficient moves reproduces **−a×b** exactly (predicted −0.007123, observed −0.007123, agreeing to six decimals) whenever `X→M` and `M→Y|X` have both already been estimated. **It carries zero new information; it is a test only when one of the two legs is unestimated.** This is the second such case on this page, and the reason it keeps getting proposed is that it *reads* like a mediation analysis — but mediation's information comes from its assumption of no unmeasured confounding, not from the arithmetic. Strip the assumption and only the identity is left. ⚠ **`#397`: and the five-item score turns out to be a fourth shame route, separate from c3⁻.** The two correlate only **+0.2395**, and entered together they both survive: the five-item score goes +0.0881 → **+0.0657** (threshold 0.0235) and c3⁻ +0.1093 → **+0.0932** (threshold 0.0264), losing 25% and 15% respectively. **Having unusual tastes and finding that ordinary things do little for you are two different things, and each carries shame on its own** — someone with entirely conventional tastes can still be ashamed because the conventional does not move them either. ⚠ c3⁻ is an eigenvector whose sign is arbitrary and was anchored against shame before use (+0.127). ⚠ Notably this route arrived through a different door: it was found while scanning the residual of the *changeability* item, not while looking for shame routes. ⚠ **`#398`–`#399`: scanning a third outcome — whether engaging with one's arousal feels therapeutic — finds two more, and they are not one construct.** That item is not inverted shame: with shame, S, c3⁻ and category count removed, the residual still has structure (largest \|r\| **0.1187** against a max-of-112-noise threshold of 0.0442, 10 of 21 candidates over). Two of the hits are new: *if my partner is aroused by something I can be too* (**+0.1038**) and *I can be aroused by a partner doing what I like even knowing they are not* (**+0.0661**), each surviving with the other absent from the model and barely moving when both are entered. **But they correlate only +0.031 with each other** — near orthogonal, consistent with their near-opposite content (absorbing a partner's arousal versus not needing it). **So these are two separate things that each predict the same outcome, not two indicators of one.** ⚠ Scans are discovery, not test; both were replicated only within this dataset. ⚠ **`#401`: and shame and healing are not two ends of one axis — 1,790 people report both.** Their linear correlation is **+0.0005** across all 15,501 respondents: not negative, zero. Split at both medians, the both-high cell holds **11.55%** of people (**1,790**) against an offset null of **10.43% ± 0.18%** built by keeping both marginal distributions and breaking only the pairing — **+6.13 sd above**, so these people are not merely possible but slightly **more** common than chance. ⚠ Nothing is controlled in this comparison, deliberately: the question is whether such people exist, and controlling would turn a group of people into a residual. ⚠ Same-source variance would inflate the correlation toward a single axis, so the near-zero reading is conservative. ⚠ **`#402`: and they are not a random slice of the ashamed — they have a shape.** Comparing both-high (n = 815 once the block-coverage rule for coordinates applies) against shame-only (n = 1,193) across eight person-level quantities, four clear a family-wise max-\|t\| threshold of 2.752 built from random equal-size regroupings: the five-item score at **d = −0.31** (\|t\| 6.80), position S at **+0.24** (5.27), mean onset at **−0.16** (3.45) and profile clarity at +0.13 (2.84). **So the both-high group has rarer tastes AND finds ordinary things more erotic, and their interests arrived earlier.** ⚠ Strikingly, c3⁻ — the largest shame route on this page — does **not** separate them (\|t\| 1.19) **— but read `#415` below before taking that as evidence about c3⁻: both cells here are high on shame, so c3⁻ has no room to vary, and on the other cut it reaches 5.15** — what distinguishes them along *this* cut is not the content of what they want but that they also have the ordinary half. ⚠ Both cells sit on the high-shame side, so nothing here measures differences in shame itself. ⚠ **`#415`: the other cut — both-high (815) against therapeutic-only (1,046) — isolates the shame dimension instead, and it picks out a nearly disjoint set of quantities.** There, the largest are **`EARLY` (d −0.291, \|t\| 6.24)** and **`c3⁻` (+0.241, 5.15)**, with `D` (+0.169, 3.62) also clearing a family-wise threshold of 2.681 — while the five-item score, which dominated the first cut at 6.80, falls to **1.54**. **So the content of what someone wants (c3⁻) marks the shame dimension, and "the ordinary works for me too" marks the therapeutic one; only S and EARLY mark both.** ⚠ This also qualifies the sentence above: c3⁻ failing to separate in the first cut is not evidence that it is irrelevant to these people — both cells there were high on shame, so c3⁻ had no room to vary. **A null from one cut is a null along that cut's direction only.** ⚠ **`#416`: and with all four cells used, the two dimensions turn out to be additive.** The third cut (shame-only versus therapeutic-only) carries no new information at all — it is exactly cut 2 minus cut 1 by construction, verified to **2.8e−17** — so a real interaction test needs the fourth cell, the 2,566 people low on both, which this project had never used. Doing it: the largest interaction `(both − shame-only) − (therapeutic-only − both-low)` is `EARLY` at **\|t\| 2.15** against a family-wise threshold of **2.73** from relabelling the four cells at random, with everything else below 1.16, and an MDE of 0.20 sd against a meaningful 0.20. **No quantity belongs specifically to the both-high cell.** So the shape those 1,790 people have is the **sum** of the two dimensions' shapes and nothing more — consistent with the earlier finding that "both halves" is not a construct. ⚠ **`#417`–`#418`: and the fourth cell — the 1,662 people low on both — is not simply "fainter everywhere".** It is strongly distinctive: position S differs by **d = −0.31 (\|t\| 10.17)**, with the five-item score +0.17, c3⁻ −0.17, mean onset +0.16 and profile clarity −0.09 also clearing a family-wise threshold of 2.73. **But orienting all eight quantities toward "more engaged" using an anchor that is neither shame nor a reading of mine** — the count of endorsed sex acts, whose direction is fixed by being a count and which plays no part in defining the cells — **only 6 of 8 point the faint way, against an offset null of 3.96 ± 1.46 whose 95th percentile is exactly 6.** With an MDE of 0.05 sd against a meaningful 0.20, that is a null with power. **So being low on both is a specific position, not a general dimming**: tastes markedly more ordinary and interests arriving later, but not less of everything. ⚠ **`#403`: and "both halves" behaves as one thing rather than two.** Combined as `z(S) − z(five-item)`, the score separates the two cells at **d = +0.399, \|t\| = 8.76**, and as `min(z(S), −z(five-item))` at **+0.388, 8.54** — **both above the best single quantity's 6.80**, which was the pre-registered bar precisely because a combination of two already-significant terms is otherwise guaranteed to clear an ordinary threshold. Two constructions, same verdict. ⚠ This is still the same dataset and the same pair of cells, so it is not a replication, and "stronger together" does not establish a latent variable. ⚠ **`#404`: and it does not become one on testing.** Putting the combination alongside its two components in a model of the *therapeutic* item — an outcome that played no part in constructing it — the combination falls from **+0.124** to **+0.018** against a permutation threshold of 0.046. ⚠ The additive version of the combination cannot even be tested: `z(S) − z(five-item)` is an exact linear function of its parts, so that design matrix is rank-deficient (3 of 4) and the coefficient is unidentifiable — a check worth three lines before any compute. ⚠ And the collapse itself is **not publishable as a null**: the design's MDE is 0.080 against a meaningful 0.05, so **the honest position is that there is no evidence for a latent variable and this design cannot rule one out either** — which is a different claim from having ruled it out. ⚠ One non-null aside: on the therapeutic outcome the two halves are **not** equal partners (rarity +0.047, ordinary-also-works +0.123), whereas they were equal in separating the two cells. ⚠ **`#405`: and across four outcomes the division of labour is not stable — this one is a null with power.** Fitting each of shame, changeability, therapeutic and having-acted on the same two halves with the same controls, the coefficient pairs point in directions spread over **195°** (−36°, −126°, +69°, +40°). Their circular dispersion is **0.662** against an offset null of **0.551 ± 0.216** — *above* the null mean, nowhere near its lower 5th percentile of 0.136 — with the ratio metric agreeing and an MDE of 0.10 against a meaningful 0.20. **So "both halves" is a good description of that group and not a construct**: it works where it was found and does not recur as one thing elsewhere. ⚠ The four outcomes intercorrelate, which deflates the dispersion and therefore biases toward *stability* — the instability survives a bias running the other way. ⚠ **`#406`: and position S itself does not point one way — it is reversed on 8 of 29 outcomes.** Every claim about S on this page comes from the shame outcome; fitted across the project's whole 29-outcome set with the same controls, S's coefficient is positive in **21** and negative in **8**, a sign consistency of **0.724** against an offset null of **0.579 ± 0.059** (upper 95th 0.690) built by permuting S while keeping the outcomes' intercorrelation. **That clears the bar by +2.47 sd — marginally — and the intercorrelation of the outcomes inflates consistency, so the bias runs toward the conclusion.** ⚠ The MDE sweep saturates at every level, because a sign-count statistic has almost no dynamic range: this design's MDE is **not measurable**, which is not the same as small. **So statements of the form "people high on S are …" have to carry "on the shame outcome"** — among the reversals is whether a person believes they could stop. ⚠ **`#409`: and splitting S in two does not repair it — a null with power.** Using the transgressive/ordinary block split that `#327` derived from loadings rather than from reading, the two halves' sign consistencies are **0.621** (transgressive) and **0.759** (ordinary) against the combined S's 0.724; the smaller of the two sits at **−0.87 sd** below an offset null of **0.651 ± 0.035** built by splitting the blocks at random into groups of the same sizes. **So S is not a mixture of two directions** — the transgressive half is if anything the more scattered one — and the outcome-specific qualifier above stands for a reason that has now been tested rather than assumed. ⚠ MDE 40% against a meaningful 50%, sweep unsaturated. ⚠ **`#411`: and shame barely suppresses action at all in people who have never had a partner.** Stratifying the shame→acted relation by partner count (0 versus ≥1), controlling age, category count and — in the narrow arm — S and c3⁻, the coefficient is **−0.015** among the never-partnered and **−0.077** among the rest, a difference of **−0.062** past a random-restratification threshold of 0.042 at the wide caliber (n = 12,516, guard-25 growth +89.4%). All three computable cells carry the pre-registered negative sign; only the widest clears. ⚠ **This establishes that the gap is not noise, not that shame needs an audience**: people who never had a partner have neither an audience nor an opportunity, and having-acted is depressed by both — no sample size separates them. ⚠ Wide and narrow calibers are different populations, and the wide arm drops S and c3⁻ because they require ≥8 blocks. ⚠ **`#390`: put in one model, intensity survives and breadth collapses — the withdrawal is reversed by a test registered in advance.** With both terms present (they correlate −0.37, so the collinearity world does not apply), the rating-based intensity score holds at **+0.1255** (se 0.0131, threshold 0.0253 from permuting both columns together) while the block-count breadth score falls from −0.0430 alone to **−0.0009**. So `#389`'s −0.026 was breadth speaking for intensity. **The three outcomes and their predictions were written before the run**, and the kill fired for the one that reinstates the reading — a withdrawal that stood for exactly one round and was undone by a pre-registered test, not by reinterpretation. ⚠ What this does **not** settle is the selection itself: the five items are still ones chosen by looking at the scan, so `#388`'s scope stands and the defensible pre-registered quantity remains the maximum, +0.0888. ⚠ **`#391`: within those five, though, it behaves like one family rather than one or two items wearing a name.** Entered singly, all five carry the same sign and all five clear their own single-variable thresholds (+0.090, +0.085, +0.069, +0.064, +0.063 against thresholds of 0.024–0.026), and dropping any one item leaves **86–104%** of the five-item score — the largest single contribution is 14%, and two of the items slightly dilute rather than carry it. This rules out the `#325` pattern where two indicators of one construct load it very unequally. ⚠ It does not touch the selection question: proving the five cohere is not proving the five were picked fairly. ⚠ **`#327`: and the block loadings reproduce that split on the other instrument.** Bootstrapped over people (300 draws, 23 of 32 intervals excluding zero), c3's shame-ward end is **urine** (+0.270), **nonconsent scenarios** (+0.263), **transformations** (+0.254) and **abnormal body states** (+0.244); its other end is **sex acts** (−0.287) and **sensations** (−0.268). Changing the option-split seed leaves the loading vector at r ≥ **+0.975**. ⚠ **A loading ranking is a description, not a name** — four naming attempts have already died here (`#201` `#202` `#303` `#324`). ⚠ **`#330`: and it is not rarity wearing a costume.** Strip the three content-free block properties — mean prevalence, option count, prevalence dispersion — from c3's loading vector entirely, and its shame correlation holds (**+0.1169**, bootstrap [+0.086, +0.130], overlapping the reference arm's [+0.111, +0.138]) while the split on the **disjoint onset instrument gets sharper**: transgressive half **+0.174 → +0.208**, ordinary half **−0.121 → −0.058**. Those three properties were adding noise to the cross-instrument separation, not carrying it. ⚠ Three properties are the three I thought of; this is a lower-bound argument, not a proof. ⚠ **`#332`: and a third correlate — how early it arrived.** Mean onset age correlates with shame at **−0.1024** raw and **−0.0810** after controlling both how many categories a person reports and their position score S (79% retained, 2×MDE = 0.068); **28 of 31 categories are negative** individually. Category split-half reliability is **0.924**, giving a disattenuated **−0.1065** with a bootstrap 95% interval of **[−0.134, −0.082]**. It survives dropping everyone with a censored ("26yo+") onset (**−0.0684**, 83% of the sample) and three different numeric codings of the age bins (range 0.004). ⚠ Onset age is retrospective self-report — association, not cause. ⚠ **`#380`: and this route is about twice as strong in respondents who are not biologically male.** At the wide caliber (n = 12,720), with shame **rank-transformed within each group** — which removes not only mean and variance differences in scale use but the response-shape confound that left `#296` at D5 — onset predicts shame at **−0.133** in group 0 versus **−0.068** in group 1 (difference +0.065, |t| = **3.69** against a random-equal-split family-wise threshold of 2.19). **All three onset summaries agree**: mean (|t| 3.69), median (3.51) and earliest (2.62) all carry the same sign and all clear their own thresholds. ⚠ They are not three independent tests — they intercorrelate +0.95/+0.65/+0.55 — but the least correlated of them, *earliest onset*, still clears. ⚠ Rank transformation discards magnitude, so only sign and threshold-crossing are compared, not effect sizes. ⚠ This is the reduced model (no S, no c3⁻): the wide caliber cannot carry c3⁻, whose profile requires ≥8 blocks, so “wide × full model” is impossible rather than undone. ⚠ At the narrow caliber with the full model the same difference is +0.053, |t| 2.14 against a threshold of 2.42 — same sign, below the bar. ⚠ **`#370`: and within a single person, the rare things arrive LATER — far more than the shared schedule explains.** For each respondent reporting ≥8 onset categories (n = 6,473), the rank correlation between their own onset ages and category rarity averages **+0.2219** (median +0.2532; **77.1%** of people positive) against an offset null of **+0.0884 ± 0.0034** — **+38.9 sd**. That null is not zero and could not be: at population level, rarer categories genuinely start later (corr of category median onset with rarity **+0.3406**), so the null is built by replacing every onset with a draw from that same category's own distribution, preserving the schedule and breaking only the individual's deviation. **Observed is 2.5× the null: almost everyone starts from the ordinary, and the ordering is personal, not merely demographic.** ⚠ **`#371`: and the few for whom it ran the other way carry more shame.** Regressing shame on that within-person ordering, controlling category count and S, gives **−0.0287** (95% CI [−0.047, −0.011], −3.00 sd from a person-shuffle null) at the wide caliber (n = 11,535) and **−0.0327** ([−0.057, −0.009]) at the narrow one (n = 6,473) — same sign, both past threshold. **Negative means the people whose rare interests arrived EARLY, before the ordinary ones, are the more ashamed.** The wide arm's MDE is 0.025 against an effect of 0.029: adequate, not comfortable. ⚠ **`#372`: and about half of that is simply that those people started earlier at everything.** Adding mean onset age to the same model drops the ordering coefficient from **−0.0287** to **−0.0148**, a **48.4%** drop (bootstrap 95% CI [+31.4%, +83.8%]) against an offset floor of −0.11% ± 5.01% built from a synthetic control with the same correlation to the ordering but none to shame — **+9.69 sd**. **And what remains does not survive on its own**: −0.0148 against a same-model permutation threshold of 0.0173 (−1.65 sd). **So the ordering is not yet an independent route** — half of it is overall earliness and the remainder is below this design's resolution. The description above still holds; the mechanism does not separate. ⚠ Wide and narrow calibers are **different populations** (`#346`), so this is a cross-population replication, not added power. ⚠ The first version of that wide arm **was not wide**: `S` is only defined at coverage ≥8, so requiring it re-imposed the narrow rule and the arm grew by 70 people instead of 5,000 — an inclusion-rule relaxation that does not move n never happened, and is a scope claim in the flattering direction. ⚠ **`#334`: three routes, and only one pair overlaps.** Onset-early carries an increment of **0.484pp** against S's **0.491pp** and c3⁻'s **0.949pp**. Over the three pre-declared pairwise interactions, permuted 1000 times with a family-wise max-|t| threshold of 2.465: `S × c3⁻` **p_fw = 0.769** and `c3⁻ × EARLY` **p_fw = 0.987** are additive, while **`S × EARLY` is sub-additive (|t| = 3.36, p_fw = 0.004)** and clears the threshold under all three numeric codings of the onset bins. Rare taste and early arrival overlap; the other two pairs simply add. ⚠ **`#337`: and the overlap survives at the person×category level** (147,387 cells, 13,517 people, permutation null over people): being **earlier than that category's own population median** (+0.082) and the **category's rarity** (+0.068) each carry shame, and their interaction is **−0.0094 (permutation p = 0.010)** — so the overlap is not the shared developmental schedule, which is centred out cell by cell. ⚠ **`#350`: it replicates in a larger, more heterogeneous sample.** At a looser inclusion rule (≥4 of the 32 blocks rather than ≥8, n = 6,717 → 8,196) the same family-wise test gives **|t| = 4.47, p_fw = 0.000** for `S × EARLY` (from 3.36 / 0.005), with the other two interactions still null (p_fw ≥ 0.658). ⚠ **The two calibers are different populations, not the same one with more data** — low-coverage respondents differ systematically (`#346`), so this is a replication across populations, not an increase in power. ⚠ **`#357`: two of those four moderators were deliberately coarsened before release.** The dataset's own column notes — a file this project had never read until now — record that *"How sexually liberated was your upbringing?"* was collapsed from **7 levels to 3**, and relationship style from **3 to 2**. Coarsening a moderator attenuates interaction detection, so the per-variable MDE of 10–30% is partly the release's doing, not the population's. The publisher also states the anonymization leaves correlations **~25% weaker than the original** — so every correlation on this page is a **lower bound**. ⚠ **`#373`: and that card is not describing this exact file.** Its header states 15,511 × 376; the CSV is **15,503 × 365**. Of the five columns whose level counts the card states outright, **two match and three do not**: `straightness` is documented at 5 levels and released at **2**; `childhood_adversity` and `TotalMentalIllness` are documented as None/Any and released with **only the positive label**. So the coarsening claims above are what the **card** says, not what this file **is** — and since the one checkable divergence went further than documented (5 → 2), the direction is right and the **magnitude is a lower bound**. ✅ One consequence runs the other way and improves an earlier caveat: because the card documents those two flags as None/Any and the row count rules out rows having been dropped, the file’s blanks **are** the None level, so reading them as zero is correct and `#366`’s warning about conflating “none” with “no answer” is withdrawn. ⚠ **`#374`: but the card is stale, not false — its deletions hold.** Its `DROPPED GT QUESTIONS` section names 13 identifiers as removed, and under a sound matcher (whole identifier-segment equality, positive-controlled at 8/8 on columns known to be present) **0 of the 13 survive in the file**. A first pass using bare substring matching claimed 2 survivors — `race` matching `Totalsexyrace`/`raceplay` and `smost` matching a question that merely *references* `bodypartsmost` — the classic unsound direction: absence of a substring implies absence of the column, presence does not imply presence. ⚠ The card uses the original survey's names, so a rename would also read as absent; this check can prove the list wrong, not prove it right. **Read the card as accurate about what was removed and out of date about how much was collapsed.**
> sexes** (`#285` `#286`). Rebuilt independently in each group (n = 3,232 / 3,485): the block-level
> loading subspace replicates at **0.7695** across the sex split against **0.8385 ± 0.0263** across a
> random split of the same sizes (**2.6×**), and — the sharper test — the *joint R² curve over all 29
> outcomes* correlates **+0.6667** across the sex split against **+0.8728 ± 0.0161** across a random one
> (**12.8×**). Both ends of the positive control behave (a sex-driven synthetic quantity gives −0.2553;
> `agreeableness`, which correlates −0.0749 with sex, gives −0.0016). The three outcomes that differ
> most: `I sometimes find peop…` **1.9% vs 5.0%**, `I find the thought of…` **3.2% vs 0.6%**, `animated`
> **6.4% vs 8.9%**. ⚠ This does **not** say any column is wrong — every one was controlled for sex. It
> says **the table is an average, and that average hides a resolvable disagreement.**

> ⚠ **`form_i` is the only standing dimension that does *not* reach shame** (`#278`). Everything else in
> this project ends up pointing at it — position +0.1185, breadth-type c3 +0.1286, the between-block
> contrast on the disjoint instrument +0.1384 — and this one does not: its correlation with shame is
> +0.0454, but its **increment over the six coordinates is +0.21 ± 0.13pp, indistinguishable from zero.**
> **When every quantity in a project points at the same outcome, the informative one is the quantity that
> doesn't.** ⚠ **`#316`: that is a statement about the *increment*, and the wider reading is wrong.** Un-orthogonalised, `form` correlates with shame at **+0.0797**, and its Spearman–Brown reliability is only **0.380** (`animated`↔`written` = **+0.2345** at n = 6,478; ⚠ **`#375`: this page previously printed +0.468 here, which is `2r`, the numerator of Spearman–Brown, mislabelled as `r` — SB(0.468) would be 0.638, not 0.380, and 2 × 0.2346 = 0.469. The reliability 0.380 was always right; the pair correlation was not.**), so the disattenuated value is **+0.1293** (bootstrap 95% interval **[+0.088, +0.167]**, `#320`) — the same size as position (+0.1185) and breadth-type c3 (+0.1286). It survives controlling response style (**+0.0791** disattenuated, 61% retained). What is absent is not the signal but its *unique* part: after removing both the six coordinates and response style, **+0.0168**, below this design's MDE of 0.0348. **`form` is not a shame-free dimension; it is a dimension this survey measures too coarsely.** ⚠ **`#419`: the direction of these two items has now been checked, and so — for the first time — has the direction of the shame item itself.** Both `animated`/`written` and the shame question are `LIKERT_PM3` (symmetric, −2…+2 and −3…+3), structurally unlike the all-negative `RATING_NEG_FIB` scale whose direction `#392` had to reverse. Calibrating the whole family with an item whose direction is forced by its wording — *I have acted upon or experimented with all the things that arouse me*, which must rise with the count of sex acts a person endorses if higher means more agreement — gives **+0.118** and **+0.056** against the two count anchors. **So higher does mean more agreement, and every shame result on this page rests on an assumption that had never been verified until now.** ⚠ Separately, the two count anchors **disagree** about these items — arousal by drawn and written material goes with **fewer** endorsed sex acts (−0.076, −0.062) and **more** fetish categories (+0.095, +0.035) — which is a substantive split, not an instrument failure. ⚠ **`#420`: and it survives a proper test.** With `S`, c3⁻, shame and age controlled, the form score predicts **−0.076** on the count of endorsed sex acts (threshold 0.019) and **+0.049** on the count of fetish categories (threshold 0.022), both past offset nulls built by permuting the form score itself — **opposite signs, both surviving**, against a backdrop where the two counts correlate +0.373 with each other, so collinearity residue was not a straw man. **People who respond to drawn and written material want more kinds of things and have done fewer of them — and this is not overall rarity of taste, which is controlled.** ⚠ Both counts are self-reported and standardised, so only signs and threshold-crossings are compared; the split is a description, not a causal claim. ⚠ **`#421`: and "breadth" on this page is not one quantity.** Enumerating the five breadth measures actually used — onset-category count, fetish-category count, endorsed-sex-act count, block coverage and total picks — only **6 of their 10 pairwise correlations reach 0.6**. Onset-category count, block coverage and total picks intercorrelate at **+0.83 to +0.86** and behave as one thing (how much of the survey a person engaged with); fetish-category count sits at its edge (+0.62 to +0.74); and **the endorsed-sex-act count is the outlier, correlating only +0.26 to +0.51 with the rest** — its weakest link being with fetish-category count at **+0.2615**. **So "breadth" is at least two things — how many kinds of thing interest you, and how much you have actually done — and the form score above pushes those two in opposite directions.** ⚠ **`#422`: and the ambiguity turns out to be bounded, so one definition fixes it rather than ninety annotations.** Auditing both pages, about 90 uses of breadth-like wording do not name a specific measure within their immediate context — but **every one of the 15 places where the odd-one-out measure appears (7 here, 8 in the Chinese page) names it explicitly**, as `Totalsexacts` or "endorsed sex acts". **So a bare "breadth" on this page never means the sex-act count.**

⚠ **`#423`: and enumerating the question types turned up a loaded trap that has not yet fired, plus one fact about people.** The survey has two Fibonacci-coded rating families whose value sets are exact mirrors — `{0,−1,−2,−3,−5,−8}` and `{0,1,2,3,5,8}`. `#392` established that **−8 is the most erotic end** of the first. Anchoring the second against the same two counts whose direction is fixed by being counts, it comes out **positive on both** (+0.166 and +0.114, against a permute-within-mask null at 7.4 and 4.9 sd) while the first reproduces its **−0.450 / −0.186** in the same script. **So magnitude is arousal in both families and the sign is decorative — using them by raw numeric value would give opposite signs for the same thing.** The second family is used nowhere in this project (0 of its 7 items), which is exactly why nobody caught it: **an unused variable's direction is not a safe assumption, it is an unchecked one.** ⚠ And now that the direction is anchored, those seven items say something: they ask about finding **your own body** erotic, and the people who do endorse **more** sex acts and **more** fetish categories — **an erotic relation to one's own body goes with a wider sexual life, not a narrower one.** ⚠ Scope: only **2,061** people answered that block (13% of the sample, and the item wording selects on body type), so this is a statement about them, not about everyone here.

⚠ **`#424`: and there is no specialist taste in this survey.** The largest never-anchored question family — 67 arousal ratings, everything from anal to spanking to mind-break — was anchored against the same two counts. **122 of 134 tests clear a family-wise threshold, every one of them positive, and not one of the 134 is negative at all**, from +0.028 at the weakest to +0.372 at the strongest. ⚠ The obvious objection is that these 67 items intercorrelate, so a single shared factor landing positively by chance would turn them all positive at once. **That objection is real and it was measured**: under a null that moves whole person-rows together — preserving every column-to-column correlation exactly (0.0757 against 0.0757) while destroying the relation to the counts — the number of negatives ranges from **34 to 104 with a median of 67**, a spread about twice what independent signs would give, **and zero negatives never occurred in 200 draws.** **So the reading survives its own strongest objection: no taste here, at any intensity, goes with a narrower sexual life. More intense is never more specialised.** ⚠ "Wider" is defined by two count measures, this is cross-sectional, and the weakest item is close enough to zero that "never negative" is the claim — not "everything goes together".

⚠ **`#429`: and one signed claim on this page rests on a direction that cannot be checked here at all.** Enumerating the last unexamined question family turned up eight ordered variables this project uses whose numeric direction was never established — the five Big Five traits, powerlessness, porn habit and a bondage average. That matters because three passages above read them **as words**: "less extraverted, more neurotic, less agency". **Extraversion is now established**: it correlates **+0.146** with the banded partner count, and the literature's sign for that pair is positive, so higher means more extraverted — **and that holds under four different codings of the band variable** (ordinal ranks, band midpoints, log midpoints, and a binary none-versus-any that discards the bands entirely): **+0.115 to +0.146, same sign every time.** **The other four Big Five traits inherit it at one remove** — all five share an identical n, range and step (15,503 · −6 to +6 · 1), so they are one scoring pipeline, but that is an inference about the pipeline, **not a measurement of each trait**. **Powerlessness gets neither**: its range is −9 to +9, so it is not that pipeline, and this release contains no direction-fixed quantity to anchor it against — the one candidate, a mental-illness total, turns out to be a string with the single value "Any". **So "less agency" is UNVERIFIED here: the magnitude stands, the word does not.** ⚠ **`#461` retried it under the corrected rule and the verdict holds, now with a number.** The rule that produced the original verdict was later found broken (`#442`), so the retry used every quantity on this page whose direction is fixed by construction — endorsed sex acts, fetish categories, and the banded partner count. Three variables whose direction **is** established reach **0.196**, **0.247** and **0.146** against that set; **powerlessness reaches 0.074 at its best**. **So it is not that the test was too strict — it is that nothing available fixes which end of this variable is which.** ⚠ And even a strong anchor would only settle direction if a prior fixed the expected sign of the relation, which for "powerlessness against how many sex acts someone endorses" there is not; that was written down before the numbers were read. ⚠ A literature prior is a weaker anchor than the wording-locked item used for the shame scale, and it is labelled as one. ⚠ Claims stated as \|r\| — such as "all Big Five \|r\| ≤ 0.056" — are sign-immune and are untouched by any of this.

⚠ **`#433`: and once its direction was settled, the porn-use variable turned out to add exactly one thing.** Against the four outcomes this page already carries — shame, "could I stop", therapeutic, how much acted on — with **rarity of taste, how well ordinary content works, breadth type, category count and age all controlled**, more porn use still predicts **more shame** (**+0.061**, **5.19 sd** against a family-wise threshold of 2.44) and predicts **none of the other three** (1.48, 1.39, 1.02). Those three are **nulls with power**: the same design detects an effect of **0.029**, less than half the shame coefficient. **So what porn use contributes, over and above how unusual or how wide someone's tastes are, is shame — and only shame. Not a sense that it is out of control, not a sense that it heals, not more of it acted on.** ⚠ n = **6,715**, cross-sectional, and the raw associations are small throughout (0.030 to 0.077) — this is a statement about which one survives controls, not about a large effect.

⚠ **`#434`: and shame turns out to be the brake, not the carrier — which is the opposite of what the previous paragraph led me to write.** Having found that porn use predicts shame and nothing else, I wrote that the chain must run use → shame → behaviour. Tested, it runs both ways at once. Porn use reaches "how much I have acted on it" by **two opposing routes**: directly, more use goes with **more** acted (**+0.0171**); through shame, more use goes with more shame and more shame with **less** acted (**+0.0611 × −0.0797 = −0.0049**, past its permutation threshold of 0.0018, bootstrap **[−0.0067, −0.0032]** excluding zero). The net (+0.0122) is smaller than the direct because the two oppose each other. ⚠ **`#435` retracts the share I first reported here.** I wrote that the indirect route cancels **39.8%** of the direct one; bootstrapping that ratio gives **[−3.6, +2.2]**, because its denominator — the total effect, +0.0122 — sits almost on zero, and a ratio with a near-zero denominator is not an estimate. **The indirect effect itself stands** (it clears its own permutation threshold and its own interval excludes zero); **only the fraction does not.**

⚠ **`#435`: and the brake is not a flat tax — it discriminates, and one quantity is braked in the opposite direction.** Running the same three-path split for five quantities at once, with the algebraic identity holding to 2e-16 in every case: **rarity of taste loses 8.1%** of its route to behaviour through shame **[−12.4%, −4.8%]**, while **"ordinary content also works" gains 7.2%** **[+4.2%, +11.9%]** — two intervals that do not overlap. ⚠ The **signs** here are algebra, not evidence: the shame→behaviour segment is shared, so anything that raises shame must lose and anything that lowers it must gain; the panel deliberately includes a quantity with a negative path to shame so that this is visible rather than hidden. **What is not algebra is the size** — and the sizes differ by more than their own resampling spread. ⚠ Two of the five (porn use, category count) have totals too near zero for their share to be estimable at all, and they are reported as such rather than dropped.

⚠ **`#436`: and the one coordinate that points away from doing turns out to point at something else instead.** `c3⁻` — the breadth-type coordinate — is the only one here whose path to "how much I have acted on it" is negative. Split three ways with and without shame in the model: **with shame controlled, what survives is not doing less — it is feeling less able to change what arouses you** (**−0.0347**, z **−2.95** against a family-wise **2.37**, essentially unmoved from −0.0342 without shame). The path to acting less **does not separate from shame at this resolution**: it falls from z −2.82 to **−2.25**, just under the threshold, with the coefficient itself dropping only a fifth. **That is a threshold crossing, not a collapse.** ⚠ **`#437` resolves it by asking whether the threshold itself is the limit.** At ten times the permutations the threshold settles at **2.384** with its own bootstrap interval **[2.323, 2.434]** and a drift of **1.7%** between 2,000 and 4,000 draws, while the observed **2.276** sits below that whole interval. **So the answer is not that acting-less is absent — it is that this design cannot separate it from shame**, and that is now a measured statement rather than an open one. ⚠ It also means the family-wise construction used throughout this page is stable at this scale, which was the outcome that would have forced every number built on it to be restated.

⚠ **`#439`: and feeling unable to change what arouses you has two roads, not one.** This page already says that what shapes "I can't stop" is experience rather than emotion — how much someone has acted on it predicts the belief (**−0.039**) while shame does not. Breadth type predicts it too (**−0.035**), and at almost the same size, so the obvious reading is that it works through experience. It does not. Decomposed with acting as the mediator, the route through experience carries **−2.4%** of the total **[−6.4%, −0.5%]** — and the minus sign is the point: it runs the **opposite** way. Breadth type goes with having done **less** (−0.027), and doing less goes with feeling **more** able to change, so that route should be pulling the other way. It is, and it cancels almost none of the direct path (**−0.036**, larger than the total). **So people high on breadth type feel they cannot change this even though the one thing about them that ought to make it feel changeable — that they have acted on it less — is also true of them.** ⚠ The pre-registered kill here was mis-worded: it asked whether the indirect effect differs from zero, which it does, rather than whether it carries the total, which it does not; both readings are kept in the record rather than the first being quietly replaced. ⚠ n = 6,715, cross-sectional, shame in the model throughout; the family threshold moved 3.3% between 400 and 4,000 permutations.

⚠ **`#440`: and it is not what you like — it is how your liking is distributed.** The direct path from breadth type to "I cannot change this" was the largest unexplained thing on this page, so it was taken apart at the level of the object: the coordinate was rebuilt 32 times, once with each block of the survey removed, sign-aligned to a common reference, and its coefficient recomputed each time. **No block carries it.** The median block moves the coefficient by **0.25** of its own bootstrap spread and the largest by **1.86** — inside the **2.24** that the maximum of 32 reaches by resampling alone. Dropping the top one through six blocks by loading leaves the coefficient between **−0.038 and −0.045**, never weaker than the **−0.036** it starts at. **So the feeling that this cannot be changed does not come from any particular content. It is carried by the shape of a person's pattern across the whole survey, not by what is in it.** ⚠ The pre-registered kill here would have published the opposite: it compared a maximum over 32 blocks against a one-standard-deviation bar, which a maximum clears almost surely, and it named block 11 as the answer. The corrected bar is the null distribution of the maximum. Both are on the record. ⚠ The drop-k curve is an approximation rather than a true leave-k-out refit, and is reported as corroboration, not as the test.

⚠ **`#441` narrows this: the shape is not the breadth-type contrast, and that contrast is the weakest of the three.** `c3⁻` is the third eigenvector of the block-by-block residual correlation; the obvious next question was what the first two are and whether they predict the belief as well. **All three do.** Against a bar built from the largest of the three under permutation (**0.028**), the first component reaches **0.052**, the second **0.053**, and the breadth-type third only **0.036**. **So feeling unable to change what arouses you goes with the leading residual structure being strong in general, not with one particular contrast** — and the sentence above is right that it is not about content, but wrong to leave the impression that breadth type is the carrier. ⚠ **`#442` corrects this**: the first component was called unanchorable because its correlation with **one** direction-fixed count is **+0.028** — but against the **other** count, fetish categories, it reaches **−0.131**. The one-anchor rule was the error, and its own positive control says so: on this mask a variable whose direction is already established, porn use, reaches only **+0.099** against the first count and **+0.139** against the second. **A rule that declares a known-direction variable unanchorable is measuring the rule, not the variable.** With the corrected rule — clearing the bar on either count — all three components are anchored, and the first one reads: **more fetish categories, less sense that this can be changed.** ⚠ The second correlates **−0.38** with the count of endorsed sex acts, so a large part of what it is may be overall involvement rather than a distinct contrast; that is a caveat, not a result. ⚠ Each component was anchored separately — an eigenvector's sign is arbitrary, and this project has published a backwards reading off one four times.

⚠ **`#442`: and it is not involvement wearing a different name — controlling involvement makes it stronger.** The obvious deflation of the paragraph above is that the second component correlates **−0.38** with the count of endorsed sex acts, so "the residual structure predicts feeling stuck" might just be "being more involved predicts it". Adding that count to the controls and refitting, with both arms reported: the three coefficients go from **0.052 / 0.053 / 0.035** to **0.052 / 0.062 / 0.047** — none collapses and two **grow**, the breadth-type one by **31%**. **So involvement was masking the relationship rather than producing it, and what the leading residual structure tracks is something involvement does not contain.** ⚠ I predicted the second component would collapse. ⚠ **`#443` gives it a first shape: there are two ways of being more involved, and they land on different people-facts.** The second and third components are both anchored by the same count — endorsed sex acts — but with opposite signs, so the structure cuts involvement in half. Turned to face the same way and run against the four outcomes, each half clears the shared bar (**0.034**) in **exactly one cell, and they are different cells**. The first half reaches **feeling less able to change what arouses you** (**−0.054**) with essentially nothing on shame (+0.010). The second half reaches **less shame** (**−0.088**, the largest coefficient in the table) with the agency cell pointing the **other way** (+0.034 — a whisker under the bar, so recorded as unresolved rather than as absence). The two profiles correlate **−0.21**, bootstrap **[−0.54, +0.16]**, which rules out their being the same thing read twice. **So being more involved is not one condition: one form of it costs you the sense that this is changeable and no shame, the other costs shame and, if anything, returns some of that sense.** ⚠ Four points make a profile, so the interval is the result and the point estimate is not. ⚠ Both components' signs are stated in their anchored frame, never in the one the eigensolver happened to return.

⚠ **`#444`: the half that costs you the sense of changeability is loaded on the bodily-fluid questions — and the test that first said so could not have failed.** Asking what the two halves are made of, the obvious comparison is the correlation between their two loading vectors, which came out **−0.01**. That number is worthless: they are eigenvectors of the same symmetric matrix, so they are orthogonal by construction and the comparison could only ever have returned zero. Replaced with a test that can fail — how much their top-loading block sets overlap, against what two random orthogonal vectors would give — the answer is **inside the null at every cut** (overlaps of 1, 1, 2, 4, 4 for the top 4 to 12), so **"they are made of different blocks" is unresolved, not established.** What does survive is narrower and about content: taking the six blocks whose own wording names a bodily fluid, their loading on the first half averages **+0.254** against **−0.092** for the other twenty-six — a gap of **+0.346** at **4.3 sd** of a block-label permutation — while on the second half the same six sit **inside** the null (−1.7 sd). ⚠ This is a description, not a name: an ordering of loadings has twice been turned into a construct on this page and killed both times. ⚠ The six-block set was noticed in this data before it was formalised. **`#445` puts that to the test and it holds**: re-estimating the loadings on twenty independent random halves of the respondents and evaluating the gap on each half's own permutation null, it clears in **20 of 20**, every gap positive, median **+0.313** against a half-sample minimum detectable effect of **0.158** — so the effect is **2.2×** what a half can resolve, and a failure to replicate would have been visible. ⚠ This is split-half replication, not held-out data: the same respondents were in the sample where the pattern was first seen, so the concern is reduced rather than removed. ⚠ **`#446` measures what that twenty was actually resting on, and it is the content rather than the coordinate.** The twenty half-sample loading vectors agree with each other at a median of **+0.86**, but their spread runs from **−0.18** to **+0.97** — the coordinate is reproducible in most halves and collapses in a few. The fluid gap tracks that (**+0.58**), yet in the five halves where the coordinate is worst recovered it is **still positive in all five**, median **+0.19** — above the half-sample detectable effect of **0.158**, though only just. **So the content pattern is the sturdier of the two: the coordinate is a carrier that sometimes fails to re-form, and the fluid blocks stay together anyway.** ⚠ At the edge of resolution, so this ranks the two rather than separating them. ⚠ **`#447` says which part of the coordinate is the fragile one, and it is not the part the obvious explanation predicted.** The natural account of a coordinate failing to re-form is that the second and third eigenvalues sit close and the two components trade places — a relabelling, not a change of structure. That is refuted by its own sign: the eigenvalue gap and the per-vector agreement correlate **−0.37**, so the halves that recover the vector worst are the ones with the *larger* gap, which is the opposite of what a swap predicts. Compared as two-dimensional subspaces instead — using principal angles, which no relabelling can affect — **the smaller of the two angles never exceeds 22° in any of the twenty halves** (median 12°), while the larger one stays under 45° in **18 of 20** and reaches the random-subspace level (**82°**, against a null whose 5th percentile is **77°**) in one. **So one direction of the pair re-forms every single time and the second one fails outright in about one half in twenty** — the pair is not equally solid in both of its directions, and claims resting on it inherit that asymmetry. ⚠ **`#448`: and the fluid pattern sits on the *less* preserved of the two, which makes it more independent of the coordinate rather than less.** Pairing each half's plane against the full-sample plane and reading off which direction each half preserves best, the fluid signature on the better-preserved direction reaches a median of **1.8** standard deviations of its block-label null, while on the worse-preserved one it reaches **4.3** **[2.5, 4.8]**. **So the content that replicated in all twenty halves is carried by the part of the coordinate that reconstructs least reliably** — the two are not the same fact, and the content does not depend on the coordinate being well conditioned. ⚠ I predicted the opposite. ⚠ One limit worth stating: all twenty recovered directions lie inside the same two-plane by construction, so their agreement with one another is not evidence of independent recovery and is not used as any. ⚠ **`#449`: and asking the content directly — no eigenvectors at all — costs most of the sample and does not settle it.** A quantity built purely from wording, how much a person picks in the six fluid blocks relative to the other twenty-six, correlates **+0.48** with the coordinate, so it is the same construct family. But requiring those blocks to have been answered drops n from **6,715 to 2,490**, and at that size the design loses its specificity: a **sham** six-block split, chosen at random, clears the same bar in **7 of 20 tries**. The two outcomes that come closest (shame **+0.048**, the changeability item **−0.050**) sit exactly on a threshold that arbitrary splits clear a third of the time, so **this is recorded as unresolved, not as a negative result.** ⚠ The useful part is the sample arithmetic: the coordinate is not an ornament over a simpler question — it is what lets the analysis keep the two-thirds of respondents who never answered those blocks. ⚠ **`#450` repairs that arm's bar and the answer flips depending on which null is asked — which is the point.** The same observation, **0.0497**, clears the bar built by shuffling people (**0.0468**) and does **not** clear the bar built by drawing random six-block splits (**0.0725**). **The two nulls exclude different worlds:** shuffling people asks whether this quantity relates to the outcome at all; redrawing the split asks whether *these six* blocks are special rather than any six. For a claim about particular content, only the second question is the one being made — and under it, at n = 2,490, **any six blocks would do.** ⚠ This does not touch the loading result above, which was tested against a block-label null from the start and reached **4.3 sd** with **20 of 20** replications at full sample. ⚠ **`#451` joins the two strongest arcs on this page for the first time: what arrived early feels more fixed.** One arc says things that arrived earlier go with more shame; the other, built over the last several rounds, says a person can feel unable to change what arouses them for reasons that are not shame, not experience, not involvement, and not any single kind of content. Putting mean onset age against the four outcomes with current age held — the two correlate **+0.32**, so that control is not optional — **later onset goes with feeling more able to change** (**+0.050** against a family-wise **0.031**, and a detectable effect of **0.015**), while the shame cell reproduces what the page already knew (**−0.060**: later onset, less shame) and serves as the positive control that the instrument is measuring the right thing. **So the sense that this cannot be changed is partly a fact about when it arrived** — the earlier something showed up, the less a person feels able to move it. ⚠ Onset ages are recalled, not recorded. **`#452` checks whether that can produce the result, and it cannot — by the construction of the project's own recall-bias measurement.** The bias this dataset actually measures (`#114`: people report their more highly rated categories as beginning earlier) is defined *after* subtracting each person's overall precocity — and that precocity correlates **+0.993** with the mean onset age used here. **So the measured bias lives in a layer that is almost exactly what this focal has already had removed from it.** As a further check, adding overall rating intensity as a stand-in covariate — labelled a stand-in, not the real control — leaves the coefficient at **+0.048** from **+0.050**, retaining **95%**. ⚠ The separation is not total: the residual within-person tilt still correlates **+0.27** with mean onset. ⚠ Cross-sectional. ⚠ **`#454`: and it is a gradient, not a cliff.** Cut into the four onset strata this page already uses — median onset **12.5 / 14.7 / 16.7 / 19.9** years, reproduced exactly and in equal quarters — the adjusted sense of being able to change what arouses you runs **−0.060 → −0.016 → +0.007 → +0.068**, strictly increasing, with the first and last intervals not overlapping. Rank agreement with the ordering is **+1.00** against a label-shuffle null whose 95th percentile is **+0.80**. The threshold alternative does not fit: the earliest stratum sits **−0.080** from the rest, but the other three spread **0.084** among themselves, so nothing singles out the early end. **So this is not "before some age it is fixed" — every step later in onset comes with a little more sense that it could move.** ⚠ With four strata, perfect ordering is the strongest result available and its own floor is p ≈ 0.042, so the ordering test is at its resolution limit; the gradient itself is the substance. ⚠ The cuts are the page's own, not chosen here. ⚠ **`#455`: and shame runs the same gradient in mirror image** — **`#456` withdraws the reading that this means one antecedent.** Across the identical four strata, with the identical controls, adjusted shame runs **+0.073 → +0.049 → −0.047 → −0.074** — perfectly ordered, opposite in sign, exactly as the page's older result implies. Both directions were fixed by prior findings before this round, so each is tested one-sided: **p = 0.040** for changeability and **0.042** for shame. Their steepnesses are **+0.128** and **−0.147** on the same scale and the difference, **0.019**, cannot be resolved — **but the interval on that difference, [−0.109, +0.073], is wider than either steepness itself, so this is a resolution limit and not a finding of sameness.** **What can be said: one thing about when a person's tastes arrived moves both how ashamed they are and how movable it feels, in opposite directions and at sizes this design cannot tell apart.** ⚠ A two-sided ordering test cannot fire at four strata at all — a perfect order arises in 2 of 24 random arrangements, so the 95th percentile of \|rho\| is 1. That is why the direction had to be pre-specified, and it qualifies the paragraph above as well.

⚠ **`#456`: the two consequences have nothing to do with each other, and saying "one antecedent" was reading more into that than it holds.** Holding shame while re-running the changeability gradient moves it from **+0.128** to **+0.128** — retention **1.007** — and holding changeability while re-running shame moves it from **−0.147** to **−0.147**, retention **1.005**. Both stay perfectly ordered. **But that near-zero mediation is close to forced**: the two outcomes correlate **−0.002** with each other, so controlling either was never going to remove anything from the other, and the test is much weaker than it looks. **What the arithmetic does support is narrower and worth having: when a person's tastes arrived is tied to two things — how ashamed they are, and how movable it feels — and those two things are unrelated to one another.** That matches what this page already found from the other direction: shame changes what people do, not what they believe they could stop. ⚠ So the timing is one fact with two unrelated consequences, not one mechanism with two faces, and this round cannot tell which of those the world is. ⚠ **`#457` turns that caveat into a number that can be computed before the test is run.** How far controlling one outcome can possibly move the other's coefficient is fixed by three correlations, so it is knowable in advance: for these two arms the bound is **0.74%** and **0.36%** of the total effect, and what the full fits actually moved was **0.7%** and **0.5%**. The same calculation says the page's porn-use mediation **does** have room, so this is not a rule that refuses everything. **A mediation test whose bound is a fraction of a percent cannot fail, and that is now checked before the round rather than noticed after it.**

⚠ **`#458`: and the two paths do come apart — three of six page quantities touch exactly one of them.** Running all six person-level quantities this page already carries against both outcomes at once, with a family-wise bar of **0.034**: rarity of taste (**+0.079 / −0.086**), "ordinary content also works" (**−0.066 / −0.127**) and onset age (**−0.058 / +0.050**) move **both**; **breadth type** (**+0.082 / −0.030**) and **porn use** (**+0.059 / −0.016**) move **only shame**; and **how many categories a person answered** (**+0.010 / −0.035**) moves **only the sense of changeability**. **So being ashamed of what arouses you and feeling unable to move it are not two readings of one state: what you are drawn to and how much you use pornography reach the shame and stop there.** ⚠ The category count looked like the weakest link — a measure of how much of the survey someone walked through. **`#459` tests that and the volume reading does not hold.** Of the five engagement measures this page enumerates, only two move the changeability outcome: the onset-category count (**−0.035**) and **block coverage** (**−0.042**), both counts of **how many distinct areas a person entered**. The three that count *within* areas — total picks (**−0.023**), fetish categories (**−0.014**) and endorsed sex acts (**−0.031**) — clear nothing, against a shared bar of **0.034**. **So it is not that people who answered more feel more able to change: sheer number of things ticked says nothing. What tracks it is the number of separate domains a person has been into at all.** ⚠ **`#460` then splits answering behaviour against itself and the artifact reading does not survive.** Two quantities are built from the same two numbers: **width**, how many separate domains a person entered, and **depth**, how many options they ticked per domain entered. Both are produced by answering, so a general tendency to answer a lot would show in both. Run together — they correlate **+0.41**, so neither is eating the other — **width predicts the changeability outcome (−0.036) and depth predicts nothing at all** (**+0.004** and **−0.023** against a bar of **0.030**). **So what tracks feeling able to change is not how much someone answered but how many separate territories they have been into; how thoroughly they filled in each one says nothing.**

⚠ **`#463`: and the two leading components of the residual structure fall on the two different paths — the first time both sides of the fork can be named.** Adding the first component, oriented so that higher means **more fetish categories** (the frame the paragraph above uses), to the same seven-quantity model: it reaches the changeability outcome (**−0.047**) and not shame (**+0.017**), while **breadth type still reaches shame (+0.078) and not changeability**, against a shared bar of **0.034**. **So where a person spends their openness goes with how ashamed they are; how many separate fetish categories they hold goes with how movable it feels. Two different questions about the same tastes, landing in different places.** ⚠ In this model the category count falls just under the bar (**−0.031**) where in the model without the first component it cleared — the two models are not comparable, and the earlier claim is scoped to its own rather than withdrawn. ⚠ **`#464` corrects the name attached to the first component, though not its direction.** Checking every countable quantity on this page against all three components: the first component's four anchors all point the same way, so its orientation stands — but its strongest are **total picks (0.227)**, **block coverage (0.213)** and **onset categories (0.183)**, with fetish categories the **weakest of the four at 0.131**. **So it is not "fetish breadth" — it is the same overall-engagement-breadth direction that block coverage and the category count mark, and it lands on the changeability path exactly where they do.** The sentence above should be read as engagement breadth, not fetish content. ⚠ The same check kills a guess of mine in the other direction: the breadth-type component was supposed to be the one with no countable correspondent, and it has the **strongest** of the three (**0.460** with the endorsed-sex-act count). **So the five failed attempts to name it did not fail for want of an external correlate**, and whatever the reason is, it is not that. ⚠ **`#465` narrows what a name for it would have to survive.** Taking that strongest correlate — the endorsed-sex-act count — out of the breadth-type coordinate altogether: it accounts for **21%** of the coordinate's variance, and the residual still predicts shame at **+0.069** where the original was **+0.078**, keeping **87%**, and still predicts the changeability outcome not at all. **So what a person's openness is spent on is related to how much they have actually done, but is not that: strip out how many sex acts they endorse and seven-eighths of the link to shame is still there.** ⚠ Which means the unnamed thing is not that count in disguise, and any name offered for it has to survive the same subtraction. ⚠ **`#466` removes every countable quantity at once, and two things come out.** The six counts on this page **jointly** account for only **31%** of the breadth-type coordinate — the strongest alone was 21% — so **69% of it is untouched by any of them**, and after removing all six the link to shame keeps **74%** of its size (**+0.059** from **+0.079**). **So there is a substantial piece of "where a person spends their openness" that no count in this survey reaches, and three-quarters of the shame relation lives there.** ⚠ The second thing qualifies the paragraph above rather than the first: with the counts stripped out, the residual **also** clears on the changeability outcome (**−0.031**), where the coordinate as built did not. So the clean one-path-each separation is a property of the coordinate **as constructed**, not of its count-free core — the counts were carrying the changeability side. ⚠ **`#469`: and the rarity–shame link this page rests on turns out to be twice as strong in domains fewer people enter.** A separate line of work on this repository, run against 186 societies rather than people, argues that condemnation tracks rarity only where norms can actually suppress a behaviour — and that sexual interest is not such a domain. That is marked there as a reading generated by the data, not an established one; this is the person-level corollary tested here. Splitting the survey's blocks by how often their options are picked at all, and computing rarity **within** each block so the split is not the thing being measured: the slope from rarity to shame is **+0.042** in the sixteen most-entered domains and **+0.096** in the sixteen least-entered, a difference of **+0.053** with a bootstrap interval of **[+0.019, +0.090]**. **So being drawn to rare things costs more shame when few people go there at all — the coupling is not uniform across the map.** ⚠ The difference grows monotonically with where the split is placed (**−0.001** to **+0.074** across five cut points, four of them positive), so the size is cut-dependent and the whole curve is reported rather than one cell. ⚠ Rarer blocks give each person fewer picks to average over, which makes their rarity score noisier and would **attenuate** their slope — so the direction found here is the conservative one. ⚠ This is person-level evidence about this survey's population; it can agree or disagree with a society-level claim but cannot stand in for one. ⚠ **`#470` pins down what is robust here and what is not.** The obvious objection to the paragraph above is that as the split moved, the uncommon group also grew from eleven blocks to twenty-one, so the trend could be group size rather than commonness. Rebuilt with the two groups always holding the **same number of blocks** — the k most-entered against the k least-entered, middle discarded — and swept over k = 6, 8, 10, 12, 14: **the difference is positive at every k**, but it is **not monotone** and it ranges from **+0.020 to +0.077**, with three of the five intervals excluding zero. **So the direction survives every specification tried — nine of ten across both sweeps — while the size is not estimable to better than about a factor of four.** The dip at k = 12 (**+0.020**) is noise, and **`#471` establishes that rather than leaving it open**: removing each of the twenty-four blocks in turn at k = 12 moves the difference by at most **0.031**, against a **0.074** bar built from the largest such move under resampling — no single block explains the dip, and the neighbouring k = 11 and k = 13 both sit back up at **+0.044** and **+0.054**. ⚠ The attenuation argument is now measured rather than asserted: at equal block counts the uncommon group still gives each person **5.7–6.5** picks per block against **6.8–8.0** for the common one, so its score is the noisier of the two and the noise runs against the finding. ⚠ **`#472`: and this happens to shame and to nothing else.** Running the identical design — same blocks, same split, same equal-count sweep, same people — against the other outcome instead: the difference between common and uncommon domains is **+0.060** (median across k) for shame, **three of four intervals excluding zero**, and **+0.005** for the sense of being able to change, **none of four**. That is a null with power: at k = 14 the changeability arm resolves to **±0.034**, comfortably below the **+0.061** shame shows at the same k. **So the fact that rare tastes cost more shame in sparsely-entered corners of the map is about shame specifically — the same corners do nothing to whether a person feels the thing could move.** ⚠ Both arms' coefficients are otherwise as the rest of this page has them; only the between-domain difference dissociates. ⚠ **`#473`: and the same question cannot be put to the other path, which is a limit of the design rather than a result.** Asked of the engagement-breadth coordinate instead — build it from the sparsely-entered blocks and from the densely-entered ones and compare — the difference comes out at **−0.059** and clears at every k. **But the reference arm shows the identical difference on shame (−0.068), within 0.006–0.015 of it at every k**, so what is being detected is not a path being moderated but the two coordinates being different quantities: built from half the blocks each, they correlate only **+0.69** and **+0.81** with the full-sample score. **Splitting the blocks leaves rarity meaning what it meant — rarity is defined inside a block — and does not leave a coordinate meaning what it meant.** The result above stands; the analogous question about the other path is not askable this way, and no answer to it is claimed here. ⚠ **`#474`: and inside the handful of domains almost everyone enters, the relation this page is built on is gone.** Estimating the rarity-to-shame slope in the common group alone, against a null where zero is the right expectation: it runs **−0.055 · −0.039 · +0.040 · +0.076** as "common" is widened from the 6 to the 12 most-entered blocks, **crossing zero between the eighth and the tenth**. The uncommon group over the same sweep is **+0.046 · +0.057 · +0.094 · +0.101**, positive and clearing at every one. **So the link between being drawn to rarer things and feeling ashamed of it is not a property of the map as a whole: it is absent in the few domains nearly everyone visits, and it appears as soon as the set is widened.** ⚠ The negative end clears its null at only one of the four widths (n = 1,435 there), so a reversal is **not** claimed — what is established is that the positive relation is absent at that end and present everywhere else. ⚠ The control behaves: the same instrument on the same people finds the known positive effect in the uncommon group at all four widths. ⚠ This model holds all six quantities at once, which is not the model earlier paragraphs used; coefficients are not comparable across them. ⚠ A pre-registered arithmetic claim of mine failed its own control here — the products of the two correlation sets sum to **−0.025** where the identity I wrote implied nearer zero, because that identity needs the predictors to be orthogonal and these are not. The result above does not rest on it. ⚠ Cross-sectional. What it settles is narrower and worth stating plainly: **whether a number is significant here depends on which null it is asked against, and the right null is fixed by the world you mean to exclude, not by which one is stricter.** ⚠ Supporting this, the one block in the set that names fluids only in general rather than a specific one is also the one block that does not fit (**−0.047**) — what a wording-driven rule looks like, and not what a rule drawn around high loadings looks like. ⚠ The direction of the "could I change it" item is locked by its own answer wording ("with little effort, yes" is its top code), so this is not resting on an unchecked sign. ⚠ And I predicted the opposite cell would be the one to survive. **So shame does not carry porn use through to behaviour — it subtracts from it.** ⚠ The decomposition is an OLS identity (c = c′ + ab held to 2e-16) and the data are cross-sectional, so these are two routes through a correlation structure, not two causal mechanisms; the same numbers are equally consistent with shame driving use. ⚠ The sentence this replaces was written one round earlier, by me, and was **pre-registered as the outcome I expected not to find** — I predicted no mediation at all. Reading rule, established rather than assumed: **unqualified "breadth" refers to the engagement family — onset-category count, block coverage, total picks and fetish-category count, which intercorrelate +0.62 to +0.86 — and the count of endorsed sex acts is always named where it is meant.** ⚠ The 90 is an upper bound: judging "named" by whether a specific measure appears in a ±120-character window is a proxy, and it over-counts. ⚠ **`#375`: and there is no third indicator to fix that with.** Of 93 numeric person-level candidates, the largest correlate of the form residual is **`biomale` (|r| = 0.210** against a max-of-93-noise-columns floor of 0.040 ± 0.006), and **none of the top 25 passes a same-construct test** — balanced positive loading on both indicators plus α({a,w,X}) > α({a,w}) — a test that fires on a synthetic parallel third item (α₃ 0.704) and stays silent on noise (0.179). **So "measured too coarsely" is the final reading, not an open task**, and the residual is substantially sex-linked, consistent with the split above. ⚠ **`#325`: and the two indicators do not carry it equally** — `animated` ↔ shame **+0.0985**, `written` ↔ shame **+0.0515**, a difference sitting exactly on this design's resolution (2×MDE = 0.047). The direction is shared; the share is not. On sex the two diverge far more (**−0.061** vs **−0.267**), so a composite correlation can be almost entirely one indicator's. ⚠ **`#346`: the +0.1185 is caliber-sensitive.** It is computed on respondents covering ≥8 of the 32 blocks (n = 6,717). Relaxing that inclusion rule to ≥6 and ≥4 (n = 12,720) gives **+0.1477** and **+0.1715** — a **45% swing**, larger than any analysis knob in [`CALIBER.md`](CALIBER.md). ⚠ The direction is backwards from measurement error (fewer blocks means a noisier S, which should attenuate), so low-coverage respondents are not merely noisier — they differ systematically, and that is an open question this project has not addressed. `c3⁻ ↔ shame` is unaffected (**+0.1278 → +0.1290**).

> ⚠ **These six columns are not six independent measurements** (`#270`). They are all built from the
> *same* option split, so they share measurement error. Two independent splits let that error be
> measured directly (same-split cross-name minus cross-split cross-name): **5 of 30 off-diagonal cells
> exceed twice the cross-split spread**, and they cluster — **c1↔c2 |0.416|**, c3↔c2 |0.255|, D↔c2
> |0.191| — while position S and profile clarity stay clean (max 0.064). **c2 is the hub.** This follows
> from their construction: c1/c2/c3 are *adjacent eigenvectors of the same matrix*, and adjacent
> eigenvectors' estimation errors rotate into each other. **Two adjacent columns agreeing may only mean
> they wobbled together.** ⚠ This does not overturn any column's own result (each was controlled and
> scoped separately); it overturns reading the six as six independent coordinates.
> ⚠ **Caliber of the 1.4%** (`#264`, `#271`): old S + least squares; the alternative caliber (S varying
> with block subset) gives **1.21%**. Both sit in 1.2–1.4%. The knobs that move this number are listed
> and measured in [`CALIBER.md`](CALIBER.md) — **which states that it is incomplete.**

---

# What this says about people

Each row is one sentence about people, at the strength it was actually measured. The ledger entry is
the receipt.
> ⚠ **Every "N of M" count on this page is an interval, not an exact number** (`#233`). The family-wise threshold is estimated by resampling the permutation null, so the count moves with it. Measured over 25 independent resamplings: the position score reads **19–21** of 31, the between-block contrast **6–12**, breadth component 1 **10–12**, component 3 **18–22**. Width is driven by **how many outcomes sit near the threshold**, not by how strong the score is (`corr(borderline count, sd(N)) = +0.9078`; a pure-noise score and a deliberately overpowering one are both pinned).


| | | receipt |
|---|---|---|
| **The "what" of a sexual preference is personal. The "when" is not.** | Same people, same split procedure, same number of units *k*: **"how uncommon the things you like are" split-half reliability 0.57–0.67**, **"how much earlier than the shared schedule your uncommon interests arrive" only 0.055–0.108** — a **5.8–10.4× gap** (mean difference +0.5321, **26.6×**, sign consistent across four values of *k*). **And this is not an instrument limit**: plant a person-specific signal into the "when" and the same instrument recovers it at **0.67**. **A second, independent way of asking "when" gives the same small number** (`#161`: "how well the shared schedule fits you" = 0.163, indistinguishable from "how far you deviate" = 0.114 at 1.3× spread; both **12.2×** below the "what") | `#159` `#160` `#161` |
| **Sexual interests arrive on a shared developmental schedule — concrete first, relational 2–3 years later** | appearance 14.0 · body parts 14.4 · clothing 14.7 → power dynamics 16.8 · bondage 16.9 · mental alteration 17.0. Using it to guess which of any two interests came first reaches **66.852 ± 0.191**, against a theoretical ceiling of 66.5% for "everyone shares one ordering" — the schedule has nearly exhausted what a shared ordering can provide | `#75` 8 seeds · 35,438 pairs · 6,230 people; all-pairs bound **[60.5%, 66.5%]** `#63` |
| **Content and individualised valuation move endorsement probability by the same amount** | option base rate **±21.5 pp** (ledger records ±22.6; **a 5% drift that is not explained**, `#157e`) · person overall rate **±16.7 pp** · person × option interaction **±23.7 pp** (`#157` moved two corrections that had lived only in ledger prose into the code; the interaction reproduces exactly). Content dominates every *predictability* measure (3.5–5.6×) **purely because it is estimated 179× better** (3,228 observations vs 18) | `#88` `#90` |
| **"Is this sexual content?" cannot be asked of this survey** | In a binary endorsement matrix an option's **base rate** and its **content** are the same number. The item main effect has a closed form: measured R² = **0.994**, slope 0.988, and completely insensitive to the *shape* of the base-rate distribution in 23 of 23 blocks | `#69` |
| **Taste for uncommon things is a continuum everyone is on, not a special minority** | The distribution widens symmetrically: the upper tail picks rarer options, the lower tail picks commoner ones, and the **median does not move**, each side 11–13× its own bootstrap sd | `#99` |
| **It is a reliable personality dimension, and it is not "how many boxes you ticked"** | Split-half across disjoint block sets **+0.432 ± 0.016** (person bootstrap, n=7,316, 400 draws — an error bar it went eight rounds without, `#167`; the −0.022 it used to carry is a curveball *null floor*, which answers a different question) against a planted ceiling of +0.832. **And the name itself is now tested** (`#163`): split the blocks into their two most content-dissimilar halves and **85% of the reliability survives**, while a pure *position* plant survives at 100% and a pure *content* plant collapses to −2% — **"prefers uncommon things" is a transferable position, not "likes one family that happens to be uncommon"**. **And it is not a shadow of sex** (`#164`): regressing sex out of *every* block-level score leaves the tendency at **102%** (4.9× its own spread, and the direction is *up*), while a purely sex-driven position plant collapses to **−0%** and a purely personal one survives at **+0.986** under the same treatment; **67%** survives removing the pick count, and its correlation with pick count (+0.608) is **lower** than the null's (+0.719). **And it is not a projection of a bigger "content" dimension** (`#165`): scoring people on the *option matrix* itself — which specific things they ticked — gives a split-half of only **+0.26**, against position's **+0.60** (3.1× its own spread, and the pre-registered 0.60 threshold sits outside the noise band at 3.1×). **Which content you prefer is largely shared; where you sit on the uncommon–common axis is yours** | `#100` `#104` `#165` |
| **And preferring the uncommon is not affectively neutral — it comes with shame** | On two independent Likert items with **zero item overlap** with the blocks (n=15,503), the position score predicts **shame at +0.1185**, and it survives every control: pick count (+0.1155), sex (+0.1154), and the **content** score itself (**+0.1132, 9.0× its own bootstrap sd**) — content mediates only **4.5%**, so shame does *not* track *which* families you like. Permuting shame across people gives **+0.0008 ± 0.0118** (1% of the effect). The internal discriminant: on the same pipeline the *therapeutic* item starts at +0.0937 and **collapses to +0.0075 once pick count is controlled** — so this is not a generic artifact. ⚠ At the strictest coverage threshold (≥16 blocks, n=950) shame attenuates to +0.0156; 6 of 7 thresholds share the sign. **Cross-sectional self-report — "uncommon → shame" and "shame → only dare to want the uncommon" are not separable here.** ⚠ **Scope added by `#184`**: run against all 20 Likert outcomes with a max-statistic family-wise threshold (\|r\| = 0.0393), shame is **rank 1 of 20** and clears it by 6.3× — but **10 of 20 items clear it**, so shame is the *largest* correlate of the position score, not its only one  ⚠ **`#230` — shame has a second route, and it is slightly the stronger one**: the third **breadth-type** component (`#229`, person-score split-half **+0.5631**) predicts **the same shame item at +0.1286**, against the position score's +0.1185, while correlating only **+0.2036** with the position score. Entered together (standardised betas, n=6,717) **both survive: breadth-type-3 +0.1090, position +0.0963**. This does **not** retract the row above — the position effect stands under control — but it permanently changes its standing: from *the* correlate of shame to *one of two*, and not the larger. ⚠ The component's **score** is well defined; its **name** is not (`D5`) — eigenvector rotation is unidentified, so "broad about transgressive categories rather than ordinary acts" is a reading of its block loadings, not a tested claim  ⚠ **`#231` — and rarity turns out to work at two levels, with opposite signs at the second**: split the 32 blocks by their own mean pick rate (a quantity external to the score's construction) and build a residual breadth score from each half. The **ordinary** half predicts shame at **−0.0618 ± 0.0128** and the **transgressive** half at **+0.1007 ± 0.0119** — a gap of **+0.1625, 9.3× the pooled spread**, and **+0.1958 after disattenuation** (the reliability confound is real, 0.646 → 0.716, but the disattenuated gap is *larger*, so it cannot explain the result). Permuted shame gives +0.0023 ± 0.0123; both ends of the positive control fire (+0.5680 when the synthetic outcome loads only on the transgressive half, −0.0041 when it loads equally). **Being broad about ordinary things goes with *less* shame; the same breadth, moved to where few people go, becomes shame.** ⚠ "Transgressive" is operationalised as *low block pick rate* — a coarse proxy that does not separate social taboo from mere rarity ⚠ **`#232` — the two routes to shame meet nowhere else**: run the within-block position score `S` (reliability +0.7282) and the between-block contrast `D` (transgressive-half minus ordinary-half breadth, +0.6785; `corr(S,D) = +0.2029`) against all 31 outcomes. Their outcome profiles correlate **+0.1512** against an offset of **0.9824 ± 0.0049** for two noisy readings of one quantity — **170× below it**, and level with two admittedly different personality variables (age vs openness = 0.1332). Their hit sets intersect in **two** outcomes: **shame (S +0.1207, D +0.0862 — the only same-signed one)** and `biomale` (+0.1162 vs −0.0893, opposite). **Two almost disjoint predictors of a life, meeting only at shame.** ⚠ `D6`: the registered kill also required a surviving residual effect after a common factor, and that test is **degenerate with two indicators** — the two residuals came out as ±the same number (+0.0333 / −0.0333) because one principal component of a 2-D space leaves a single axis. It must be redone with ≥3 indicators. ⚠ And the "N of 31" counts throughout this page are **±2 intervals, not exact** (`#232d`) — the family-wise threshold is resampled, and the same score gave 10 and 8 in one run ⚠ **`#235` — the claim crosses instruments; the score does not**: this release carries a second, deliberately **disjoint** instrument (31 AGE_ONSET categories, item overlap with the blocks = 0). The same contrast built there predicts **shame at +0.1384 ± 0.0098** (n=9,944) against the block instrument's +0.0830 ± 0.0142 (n=4,718) — **+0.2335 vs +0.0902 disattenuated**, and the standing pick-count confound is near-absent there (`corr = +0.0428`; controlled beta **+0.1344 ± 0.0099**). **But the two scores correlate only +0.1336, which is 24.5% of their attenuation ceiling of 0.5452** — the registered 50% was not met, while the same arithmetic gives 95.4% for two disjoint halves *within* the block instrument. **So "opening in rarer domains ↔ shame" is about people; the profile's dimensionality, the fluid family and the components' identity (`#229`–`#232`) remain properties measured *through the block instrument* and have not been shown to survive a change of instrument** ⚠ **`#236` relaxes this by one step**: rebuilt on the disjoint onset instrument (people split, category×category residual subspace), the shape is **still multi-dimensional** — k=2 replicates at **0.9379 ± 0.0478** against a permutation null of 0.4587 ± 0.0143 (**33.5× the null's own spread**), k=3 at 0.9529 (**102×**). **So "breadth has shape, at least two dimensions" is cross-instrument; only the specific families and component identities remain block-bound.** ⚠ And the null taught something: at **k=1 the null is already 0.9063** — category prevalence is a property of the questions, independent of people, and it replicates across person halves perfectly. **The first dimension is the instrument's; the person's structure starts at k=2**, which is exactly where the null falls away ⚠ **`#237` — do not read the two replication numbers side by side**: a subtractive positive control (replace a random λ of people with permuted noise, both endpoints asserted) shows the two statistics have very different dynamic ranges. The block-instrument profile replication falls **almost exactly in proportion to (1−λ)** (1.00 / 0.77 / 0.57 / 0.32 / 0.14 / 0.01), while the onset-instrument k=2 subspace **barely moves until three quarters of the people are noise** (1.00 / 1.02 / 0.94 / 0.88 / 0.37 / 0.00). λ₅₀ ≈ **0.57** vs **0.83**. **So 0.9379 is not "twice as strong" as 0.4290 — it is a quantity that asks how many directions exist in a population, not who a given person is, and the two are not numerically comparable** ⚠ **`#238` — and it is the *earliest* wants that carry it**: on the disjoint onset instrument, split each person's categories at **their own** median onset and take the mean rarity of each half. Entered together (n=4,574) the **early** half predicts shame at **+0.1307 ± 0.0137** and the late half at **+0.0788 ± 0.0157** — late minus early is **−0.0519, 2.5× its own spread**, and the early half stays ahead **after disattenuation** (+0.2536 vs +0.1716), so the reliability gap does not explain it. **Shame tracks how uncommon the things you wanted earliest were, not where you later went.** ⚠ `D6`, and one rival is explicitly alive: `#114` measured that people **remember their favourite interests as earlier** (−0.2000 yr per rating sd), so the early half may simply be enriched for the most-loved interests. **This is a fact about the self-reported timeline, not about developmental order** ⚠ **`#239` downgrades the early-vs-late asymmetry to `UNVERIFIED`, and the reason is worth more than the result**: residualising each onset on **the person's own rating of that category** (the `#114` pathway is real here — mean `corr(onset, rating) = −0.1175`, negative in 23 of 24 matched categories; both ends of the positive control fire) leaves only **2.8%** of the asymmetry — *but that comparison silently changed the sample from 2,740 to 6,301 people*, because residualisation moves the median split and therefore the ≥4-per-half filter. **On the same people the asymmetry retains 70.2%** (−0.0690 → −0.0484) yet is **no longer resolvable** (0.85× its own spread). So the recall-bias rival is **neither eliminated nor confirmed** — the resolution is not there. ⚠ **What does survive is the main effect**: after residualisation both halves still predict shame, **+0.0985 ± 0.0124 (early) and +0.0966 ± 0.0127 (late)**, n=6,301. **How uncommon what you open to is tracks shame regardless of when you acquired it** ⚠ **`#241` re-checked this on the intersection sample**: the two halves are scored on different people (5,322 and 6,113, because each requires ≥4 blocks of coverage), so the gap was re-run on the **4,718 people who have both** — ordinary **−0.0589**, transgressive **+0.1012**, gap **+0.1601** against 2× spread 0.0394, **98.5% of the original retained**. The conclusion is unchanged; this line is the scope. ⚠ **`#290`: this correlation is the same in both sexes** — +0.1297 ± 0.0188 and +0.1081 ± 0.0186, a difference of −0.0216 against an offset of 0 ± 0.0273 from random splits of the same sizes (0.8×, not resolvable), with a working positive control (an outcome driven by a `sex × S` **interaction** moves it by +0.3300). So although the six coordinates as a whole are not invariant across sex (`#286`) and **position S is the source** (`#289`), **shame is not where that non-invariance lives.** | `#179` `#184` `#186` `#192` `#201` `#229` `#230` `#231` `#232` `#235` `#236` `#237` `#238` `#239` `#241` `#290` | ⚠ **`#360`: that +0.1185 is one point on a monotone curve.** Across coverage thresholds ≥4/6/8/10/12/16 (n = 12,720 → 950) it runs **+0.172 · +0.148 · +0.119 · +0.084 · +0.071 · −0.002**, monotonically, with non-overlapping endpoint intervals. **A synthetic relation of fixed true strength declines by 0.074 over the same thresholds**, so roughly 43% of the observed 0.174 drop is selection and range restriction and roughly 0.10 is not; controlling pick count flattens the curve to 0.123. **The relation is strongest where S is worst measured and absent where it is best measured** — the opposite of measurement-error attenuation, and this project cannot separate sample from estimate quality here. ⚠ **`#363`: and the decline is specific to shame.** Running the same six-threshold curve for all 29 outcomes, the median \|endpoint difference\| is **+0.0015** against a pure-selection baseline of **+0.0435 ± 0.0193** (a synthetic relation of fixed true strength), with only **2 of 29** outcomes above that baseline — while shame's **+0.1692** ranks **1st** and sits **+6.5 sd** above it. So no general inclusion artifact explains it. ⚠ This also corrects the split above: real outcomes decline **less** than the synthetic baseline, so "43% is selection" was generous and the unexplained share is larger.
| **Two orthogonal person-level dimensions, each pointing at something different** | Residualise the *content* score (which specific options you tick) on the *position* score (how uncommon they are) and run both against all 20 Likert outcomes with max-statistic family-wise thresholds. **Position clears on 9 of 20, led by shame (+0.1155)**; **the content residual clears on 6 of 20, led by `animated` (−0.1063, 8.7×) and `written` (−0.0630)**, plus `biomale` (+0.0707) and two gender-identity items — **and it does not predict shame at all** (+0.0029, 0.2×). So *how uncommon your taste is* tracks **shame**, while *which specific things you like* tracks `animated`/`written` and **gender identity**. ⚠ **`#201` withdraws the "medium (drawn vs written)" reading**: `corr(animated, written) = +0.2507` (29.4×) — strongly *positive*, against a calibration pair that reads −0.2673 — so they are not two ends of one axis, and the content dimension predicts what they **share**, which this project has not yet named or tested. ⚠ **This split is partly produced by controlling pick count**: evaluated *marginally*, the position score predicts `animated` at **+0.1193 (10.0×)**, statistically indistinguishable from its shame correlation **+0.1185** — the clean division of labour appears only once pick count is held fixed (`#192`). ⚠ **The orthogonality is constructed, and the raw dimensions are not orthogonal at all**: `corr(S, C) = +0.4323` at the person level, so `corr(S, C_resid) = 0` is a definition, not a finding (`#129` `#189d`) — these are *one dimension and what is left of another after subtracting it*, not two independent things, and the content score is a cross-person principal component, so purely idiosyncratic combinations are invisible to it  ⚠ **`#227` — 这个「内容」维度的正名是 breadth（你对多少东西说「是」），不是内容口味**：the block-level principal components were summed **without aligning their signs** — `np.linalg.eigh` returns an arbitrary sign, and 17 of the 32 blocks pointed one way while 15 pointed the other, so every content person-score behind `#188`/`#189` is a partially cancelling ±breadth mixture. Aligned by **leave-one-out sign consensus** (15 of 32 blocks flip), the dimension correlates **+0.8320 with pick count**, and **+0.5772 even after the position score is removed**. The three named directions all survive **with the same sign** — `animated` **−0.0522** (was −0.1063), `written` **−0.0494** (was −0.0630), `biomale` **+0.1255** (was +0.0707) — but the magnitudes move by up to 2× **in both directions**, so the numbers in this row are instrument-dependent, not properties of the object. Aligning is a repair, not a distortion: it reaches **26/31** outcomes against the unaligned score's 23/31 (17/31 vs 15/31 after removing position). ⚠ **The +0.5772 is `UNVERIFIED`**: the alignment procedure's positive control passed its first clause (a planted-then-sign-scrambled signal is recovered at **0.9083** aligned vs **0.3423** unaligned) but **failed its second** — I had pre-registered "unaligned must stay below 0.30" and **chose that floor instead of measuring it**; with 16 blocks per half a random-sign sum keeps ~1/√16 of a strong common signal. **The raw breadth reading does not depend on any of this**: it is +0.8320 aligned and **+0.5154 unaligned**, both above the registered 0.5  ⚠ **`#228` — and that breadth is not a single response style**: split the options *inside* each block in half — two independent measurements of the same person×block cell, so every block-level property cancels — and a person's **residual breadth profile across blocks replicates at +0.4290 ± 0.0141** (77,872 cells) against a corrected permutation null of **+0.0043 ± 0.0012**, with a planted-strength sweep monotone throughout. People are broad about *some kinds of things and not others*, and that shape is reliable. ⚠ Reported at `D6`, not as settled: the positive control's registered clause failed (0.4789 against a required 0.4893) and was not moved — **its required increment was priced against zero when the baseline is +0.429, because the phenomenon is already there**. Scope is narrow: this refutes «breadth is *only* a global response style», **not** «the global level contains no response style» | `#188` `#189` `#192` `#201` `#227` `#228` |
| **A person's own uncommon interests sit ahead of their own common ones** | After removing the population-shared schedule and the person's overall precocity, the within-person «onset residual × category rarity» = **−0.0328** against a within-item cross-person permutation null of +0.0018, **8.8×**. **A discrete version (earliest bin vs the person's own repertoire) reproduces it independently: +0.0767 with the schedule removed, 14.5×**. `#114`'s recall bias contributes only **4%**, and with the opposite sign. ⚠ **`#211` adds the boundary this claim never had**: rerun through the same pipeline with onsets capped at 14 / 16 / 18 — where a planted effect still fires at every cap — Δ is **+0.0055 (0.8×)**, **+0.0012 (0.3×)** and **−0.0033 (0.8×)**, i.e. **indistinguishable from zero at every cap**, while the uncapped arm gives −0.0328 (8.9×). Range restriction is ruled out (within-person rarity sd *rises*, 0.473 → 0.533). **So Δ is carried by later onsets and is absent among early ones** — and `#213` shows there is **no threshold at 14**: sweeping the cut from 10 to 20 gives a monotone curve (−0.056 → −0.070 → −0.081 → −0.303) whose largest second difference does not clear its own spread, so *the later the stretch, the stronger*, not *only after 14*. (The sweep's resolution is the 2-year onset bins, i.e. ~5 distinct cuts, and the right end rests on n=1,944.) ⚠ **`#218` bounds the right end**: the c=18 point's own null depends heavily on the permutation scheme — within-item −0.120, within-person +0.053, **curveball fixed-margin −0.198** — so its net value is an **interval [−0.356, −0.105]**, not a number, and under the fixed-margin null (the scheme this project uses most) the negative control **fails**: margins alone reproduce 65% of it. **`#212` separates the two readings and it is the window, not the artifact**: splitting each person's *own* timeline at age 14 — which needs no cross-person comparison and so is immune to censoring — the post-14 half gives **Δ = −0.0704 (13.6×)**, *twice the full-sample estimate*, while the pre-14 half gives **+0.0012 (0.2×)**; both halves have non-zero reliability (0.098 / 0.228) and a plant fires in the post-14 half at g=1. **The full-sample −0.0328 was diluted by the pre-14 stretch where the effect does not exist.** ⚠ The post-14 half is itself age-selected (contributor median age 26.5 vs 22.5) and its permutation null is 30% of the effect ⚠ **`#243` — and it is not a population effect at all**: split people by whether they reported **≥8 onsets by age 14**, and match the rest on total category count (both medians 14). The early-accumulating group gives **Δ = −0.1407 ± 0.0062** (n=2,806); the count-matched non-early group gives **+0.0268 ± 0.0060** (n=2,629) — **opposite signs, 19.5× the pooled spread**. A random same-size subset lands on the full-sample value (−0.0298), a 15-year threshold reproduces −0.1407, and the permutation null is 1% of the effect. **Range restriction cannot explain it**: the early group has *lower* within-person onset spread (2.278 vs 3.022) yet the stronger correlation, and restriction only attenuates. **The full-sample −0.0328 is what is left after two groups with opposite signs cancel.** ⚠ "Early" here means *reported* ≥8 onsets by 14, which carries both really-early and remembered-early (`#114`) ⚠ **`#245` replaces the two groups with one continuous line**: index each person by `A8`, the age of **their 8th onset**, and Δ is **monotone** in it — **−0.1407 ± 0.0062** at A8≈13.5, −0.0828, **+0.0051 ± 0.0074 at A8≈17.5**, +0.0945, **+0.1259 ± 0.0189** at A8≈28. Range **+0.2665, 13× the pooled spread**; both ends of the positive control fire; the permutation null's range is −0.0019 ± 0.0190. Entering both, the early-group indicator falls from −0.1892 to **−0.0400 ± 0.0134** while A8 keeps **+0.2213 ± 0.0139** — **79% of the dichotomy is just position on this line**. **There are not two kinds of people; there is one line, and it crosses zero at about 17½.** ⚠ The residual 3.0× on the indicator is not nothing — going continuous assumes linearity, and that residue is where the assumption fails ⚠ **`#247` — the population number is the average height of a landscape**: index each person by their **accumulation rate** (interests per year between their first and last onset) and Δ runs **monotonically from +0.0882 ± 0.0110 to −0.3607 ± 0.0078** across eight strata, every one of them n ≥ 1,129, crossing zero at about **1.4 new interests per year**. The span, **0.4489, is 13× the full-sample −0.0328**. It is a surface rather than two lines — `rate` −0.2995 ± 0.0130 and `A_close` +0.2439 ± 0.0099 with a real but small interaction (−0.0571, 19% of the main effect); both ends of the positive control are exact. **A quantity whose range is 13× its own mean carries almost no information as a population average.** ⚠ Uncontrolled edge: `rate`'s denominator is a *recalled* age span, so people who compress their memories get a large rate | `#128` `#130` `#211` `#212` `#213` `#218` `#243` `#245` `#247` |
| **The map's strongest psychologically-anchored dividing line is not "early vs late" — it is "objects vs scenarios"** | A 6 coherent splits × 11 non-sexual variables grid, with a max-statistic null giving a family-wise threshold of 0.0560. **PC4 lights up 6 variables, the strongest cell at 9.0×**: one side is clothing · genderplay · secretions · roles · dirtiness · transformation · toys, the other is age · horror · nonconsent · creatures · incest · gentleness · brutality · pregnancy · mythical. **Five anchors survive within-sex** (openness +0.083 · extroversion +0.088 · neuroticism −0.064 · powerlessness −0.059, both sexes agreeing in direction). ⚠ The naming is my reading of the loadings (D5); the scale is small (0.033–0.088). ⚠ **The model-level direction is withdrawn**: a naming-independent structural test (`#139`) shows PC4's two sides are **no more non-isomorphic than any other split** (0.8×), so this line is compatible with both model A and model B | `#138` `#139` |
| **Sex anchors "the sexual" itself, not one family within it** | early family +0.0706 vs late family +0.0816, difference **0.7×**. Openness (+0.0593, 4.1×) and powerlessness (−0.0441, 2.8×) really do anchor only the late family — **but that difference is not specific to these two families**: five other equally coherent splits of the same matrix give 0.0702–0.2106, so **the early/late line is the weakest of the six**. The two families differ in organisation and in arrival, but not in **external causes** | `#137` |
| **The relational family arrives together yet contains several independent dimensions** | The early/late line has the **largest structural asymmetry of six coherent splits** (5.73, 3.0×): the late family has **2.42 more effective dimensions**, **0.35 lower** split-half reliability, **0.14 lower** single-factor share. **It is one package in time and not one thing in preference structure** — the concrete family is the reverse: scattered in arrival, more like a single thing (effective dimensionality 7.64 vs 5.30 at matched item count). **And what it contains is the three axes `A02` already named** (who submits · who is seen · who receives; held-out correlations 0.81/0.81/0.91 against a random-loading floor of 0.349) — the relational family is the **intersection** of three near-independent axes | `#139` `#140` |
| **What arrives early is scattered; what arrives late is a set** | Three blocks against a within-person permutation null: early×early **−0.01272 (27.9×)** · **late×late +0.02103 (23.1×)** · late×early −0.00484 (15.2×). What arrives late is the relational family (power dynamics · bondage · sadomasochism · mental alteration · sensory, 26–31% after age 19); what arrives early is the concrete family (appearance 8.6% · clothing 10.9% · body parts 11.1%). At the item level: the late family's mutual connectivity is **+0.0674**, the early family's −0.0075. **And "a set" is literal**: the relational family's acquisition ages are **9.4% tighter** than an equally-sized random draw (17.7×, after removing the population schedule), while the concrete family is **9.0% more dispersed** (12.1×). **Development is not one kind of thing arriving in order; it is two kinds of thing, differently organised, arriving differently** — a dimension `#75`'s schedule does not have. ⚠ Connectivity has **no knee** (1.6×), so "closes at 17" holds only for the category count | `#135` `#136` |
| **For a third of people the map closed at 17; for the rest it opens outward** | In the 29–32 band, **33.3% report no new interest at all after 17**. Among those with later entries, the later-acquired categories are **less connected** to their earlier ones, retaining 89% after stripping rarity at the pair level, with `#114` opposite-signed, and **all five age bands agreeing in sign (5.3–10.9×) with no age gradient** — so it is not narrative reconstruction. **Expansion, not deepening** — model C cannot be localised to "reweighting what is already there". ⚠ **Direction only, not magnitude**: cuts from 13.5 to 17.5 give −0.0165 → −0.0069 (a factor of 2.4), and a **within-person median split is unresolvable (1.0×) — the phenomenon is tied to absolute age, not to "later in your own sequence"** | `#133` `#134` |
| **The sexual map is largely fixed by the end of adolescence** | Among 29–32 year olds, **68.4% of their own reported interests were acquired before 17**, and the latest one averages **22.6 years** (a within-person measurement, no cross-sectional assumption). Cross-sectionally the category count grows only from 12.4 to 12.9 over fifteen years (+4.5%; cohort-confounded, weaker). **This puts a time boundary on model C: if recursive reshaping exists, its window is mostly inside adolescence** | `#132` |
| **At the population level the map really does start from what everyone has** | The batch of interests a person reports earliest sits at the **33rd percentile** of their own repertoire by rarity (49.4×). **In units of each person's own permutation spread this is z = −0.5515**, and **every one of five breadth strata is resolvably negative (17.5–27.3×) — the effect is universal, independent of how many things a person likes** (`#147`). ⚠ `#146`'s "the broader your taste the stronger it is" is **withdrawn**: the raw Δ's 27-fold variation is the arithmetic bound `(k−m)/k`, and after normalisation the residual gradient is *smaller* than what a deliberately breadth-independent planted effect produces. The content line does not explain it either (PC4 only 0.2×), nor does rarity dispersion (+0.191). **The mechanism is located** (`#148` `#152`): **it is the shared developmental schedule projected onto each person** — **five different definitions of the schedule all explain most of it, sign consistent (61%–104%)**. ⚠ **The magnitude is specification-dependent**: `#148`'s "80%, exceeding a random baseline by 44 points at 20.0×" is **withdrawn** — that baseline was **one** random draw (20 draws span −61%..+118%) and the denominator was wrong; the correct reading is **61 ± 44 points = 1.4×, unresolvable**. The remaining **9.4×** was taken to be "personal" — and `#159` shows **it is almost not about people at all**: split a person's interests in half and their "earliness tendency" has a split-half reliability of only **0.105**; split the **people** in half and **the category-loading profile reproduces at 0.96 (0.81 after removing rarity)**. **Certain sexual interests simply arrive earlier or later than the shared schedule predicts, and it is the same profile for entirely different groups of people.** `#149` says **that part is real but not a trait**: two independent readings agree on the same set of interests (−0.5741, **91.6×**) but only **−0.0910** on the person's **other** half (6.3× smaller). **What uncommon things you like is a stable property (`#100`: +0.4611, 23.1×); when you got them is not.** ⚠ **Mechanism UNVERIFIED**: at the item level Spearman(rarity, **median** onset) = +0.437, but the median is a **worse** schedule (held-out pairwise order 63.30% vs the mean's 66.70%), and the mean ordering is unrelated to rarity (+0.011). The left-tail explanation is dead (−0.091). **Effect stable, mechanism open** | `#130` `#131` |
| **The rarer a person's eventual taste, the larger that head start** | −0.0459; **after 1:1 caliper matching on the number of categories answered, −0.0417 (3.1×, 91% retained)**, three matching specifications agreeing at 85–100%, permutation null 17%, planted positive control +0.5878. `#114`'s recall bias contributes 39%. **It reproduces on a discrete statistic: matched +0.0532, 5.1×**. **And `#150`'s pre-registered point prediction landed**: measured instead as "how rare is the single earliest one" gives **+0.0370** (predicted +0.0404, sign reversed by definition, discriminant 0.2×) — **the statement does not depend on which statistic I use**. ⚠ But CONSISTENT, not PROVEN: the equivalence bound can only exclude a discrepancy larger than 86% of the effect, and `#151` shows **this release has no instrument that can weld it shut** (forced choice is cleaner per item but has only 10 blocks; reliability 0.252 vs 0.461, and the net result is a loss of resolution) | `#128` `#129` `#130` `#150` `#151` |
| **Its only external anchor that holds is sex** | +0.093 (disattenuated +0.141). All Big Five \|r\| ≤ 0.056, **openness only +0.023** — it is not general novelty seeking. **The null's strength is now quantified** (`#154`): a single curveball null can by itself produce personality correlations as high as **0.054**, while `R15` averaged three draws, leaving a residual of ≈0.004 — a 23× margin. **This claim is safe by design, not by luck** | `#101` `#102` `#154` |
| **People remember their most-loved sexual interests as arriving about nine months earlier** | −0.2000 years per rating sd (**19.8× SE**); a within-category shuffle null is **0–11%** of the effect (`#153`: the original round drew it **once**, and that draw was the smallest of 20; the null's realisation sd is 5.5% of the effect); the planted ladder is monotone. **This is the first recall bias directly measured in this dataset, and the entire maturation schedule rests on top of it**. ⚠ **It is a population regularity, not a personal one** (`#162`): the per-person slope's split-half reliability is **at or below its own null's realisation noise** (0.054 vs a null sd of 0.058), while a planted person-specific bias is recovered at **0.41–0.55** — so this is a real absence, not a power failure. **"Some people are more prone than others to remembering their favourites as early" does not hold.** | `#114` `#162` |
| **And that memory distortion deepens with time, nearly doubling over fifteen years** | the 15-year-old band −0.0505 → the 30-year-old band −0.0917, an age trend of **3.5×**; the residual artefact is **opposite-signed**, so correcting only amplifies. **It is not generated at the moment of answering the survey** | `#119` |
| **Among interests of the same kind, which one came first predicts your whole remaining preference profile** | **+0.0159, 6.1× spread**, after subtracting each of the two ratings, their difference, and the person's mean rating. **The pairwise design is structurally immune to response level** — its sibling claim ("the earlier it arrives the more central it is") died on exactly that confound | `#107` `#110` `#116` `#117` |
| **And the remaining preferences are pulled toward whichever came first** | displacement **+0.0339 — 3.1× its own spread**, with the null at **6% of the effect**, positive in 46 of 68 pairs, with a generative positive control firing monotonically. ⚠ **Doubling the sample did not strengthen it** (3.093 → 3.048), and the source line claiming "strengthening succeeded" is withdrawn; the ratio's first decimal is itself inside the bootstrap noise (20 seeds: 3.023–3.163) | `#118` `#142`, labelled DESCRIPTION: direction does not discriminate causality |
| ~~**"Top/bottom" is one word over three near-unrelated things**~~ **downgraded to: the three role axes are strongly correlated but not the same thing** | **`Entry 24` withdrew "three near-independent axes" and "effective dimensionality 2.95 of 3" long ago.** With a validated reliability ladder (positive control: SUBSTANCE against itself r_true = **+1.018**; sham +0.023), **POWER–SUBSTANCE r_true = +0.605**, i.e. **37% shared variance, not 5%**. The pre-registered kill lands in the middle band (0.45 < 0.605 < 0.70) → **UNVERIFIED**: neither one construct nor confirmed distinct. ⚠ **`#141` is withdrawn in full** — it re-priced the claim by re-running `A02/R034` without reading `Entry 24`, put the withdrawn "2.95/3" back on this line, and recommended quoting the **observed** correlation (0.112), which is precisely the attenuated one | `Entry 24` · `#143` |
| **"Extreme" is not one axis; it is two mutually exclusive directions** | abjection and filth ↔ blood, burning, weapons — people who like one end systematically dislike the other | A02 |
| **82.7% say "porn gave me this interest", and that sentence leaves no trace in either their preference structure or their timeline** | No timing signature (a 1-year shift is excluded; the concentration discriminator is null at **8.8× the required power**) · no structural signature (misfit < 0.1 sd, extremity difference < 10%) · it **tracks how much the person ticked overall** (rho **+0.2922** uncontrolled, **+0.2523** with all response-style indicators; the "85% of it is response style" step is **UNVERIFIED** — every item is erotic content with no reverse-keying, so general agreeableness and general erotic endorsement are not separable in this release). ⚠ But the step "85% of that is response style" was downgraded to **UNVERIFIED** by `#26` — every item is erotic content with no reverse-keying, so general agreeableness and general erotic endorsement are not separable in this release | A06 · `#26` |

---

# What was retracted (kept here because I once reported it)

| What I said | Now | Why |
|---|---|---|
| "Sexual is a value, not a category" (the epoch title) | **False, and the question was two questions** | Line 1 of the loader deleted the rival; and within a block base rate ≡ content, so that contrast can never adjudicate `#67` `#69` `#70` |
| "Domain-general beats domain-specific, 186/276" | **Inverted: 0 general / 201 specific** | The 186 was a −0.135 estimator handicap I had given the specific side `#82` |
| "The three components are the same size (in predictability), 1.05×" | **Withdrawn** | Subtracting a null does not fix an estimator; it charges the estimator's failure to the estimand `#86` `#87` `#88` |
| "A minority concentrates on rare options" | **Wording withdrawn, existence retained** | It is a symmetric widening, not a subgroup `#99` |
| "The earlier something enters the map, the more central it is" | **Withdrawn, 10% remains** | That was not time, it was "people who rate everything highly" `#115` |
| "The trait tracks earlier acquisition age" | **Withdrawn** | −0.030 against a null of −0.028; the null is 91% of the effect `#102` |
| "The √n accumulation across blocks reflects latent structure" | **Inverted** | At fixed budget, more blocks is worse; it is ordinary √N `#64` |

---

# What this data cannot do (each measured, not guessed)

| Cannot | The measured bound |
|---|---|
| See structure carried by ≤30% of people (variance-explained methods) | 5% carriers at **±50 pp** leave skill on the floor; ≈30% is required `#91` |
| Distinguish "a small group unusually intense about a few things" from "one heavy-tailed continuum" | Margin-preserving concentrated/diffuse plants all sit inside seed jitter at 3000 swaps per block; **the way out is an external anchor, not a stronger plant** `#122` |
| Distinguish memory becoming blurry from a story becoming settled | Real curvature is **0.3× its own spread** — it never cleared resolvability at all, so this is a **capability boundary, not a null**; pushing it to 2× needs about **48× the sample** `#120` |
| Measure the interaction magnitude directly, without a model | **Algebraically impossible** — the residual's first moment is margin-determined and identically zero under a margin-preserving null `#105` |
| Count dimensions | An estimator with an honest floor cannot count; the one that can count has a floor that is an artefact `#89` |
| Recover presentation-order primacy | The release exports multiselect answers **alphabetically** (119 pairs, agreement 1.0000); display order is destroyed `#69` |
| Separate breadth from acquiescence | Needs reverse-keyed or forced-choice items; this release has neither |
| Causal direction | Cross-sectional, and a stripped release |
| **Say what makes some interests carry more shame than their rarity predicts** | Across the 31 onset categories, rarity explains **58%** of how ashamed a category's holders are (`r` = +0.758). The remaining **42% is real** — residual sd **0.145** against a per-category sampling floor of **0.029** (**4.9×**), and a person-shuffle null puts the residual at **0.031 ± 0.006**. But four existing quantities (the category's mean onset age, its onset spread, its holders' mean position score, its holders' mean breadth-type score) give **R² = 0.024** against a permutation null of **0.132 ± 0.083** — **less than chance**, while a planted single-driver residual is recovered at **R² = 0.80**. **We can see that something is there and we cannot measure it.** Descriptively (a description, not a name — five naming attempts have died here): more shame than rarity predicts for `humiliation`, `gender`, `bestiality`, `incest`; less for `vore`, `brutal`, `creepy/creature` (`#338` `#339`). **And the thing is stable**: split the respondents in half twenty times and the two halves' 31-dimensional residual vectors correlate **+0.905 ± 0.042** (Spearman–Brown **0.95**), level with a synthetic category-level true signal (**+0.942**) and far above a person-level noise control (**−0.159 ± 0.229**). ⚠ **Stable is reliability, not validity** — a reproducible category-level quantity can still be a shadow of something else (`#343`). |

**And a transferable methodological result — but a much smaller one than I first published.** Block-count matching takes pornhabit's congruence deficit from 0.2285 → 0.0871 → 0.0439 **by direct measurement on that one split**, and **reverses its ordering against sex**; that correction stands and never depended on a correlation — **though it is a single-split direct measurement whose own precision has never been estimated (`#169`, scope stated rather than promised)**. What does **not** stand is the law I built on top of it: ~~`corr(deficit, coverage gap) = +0.815` over nine splits~~ was **killed by `#20`** — median r over K ∈ {3,5,8,12} is **+0.127**, the worst leave-one-out is **−0.294** (removing pornhabit, at 6 of 8 specifications), and the permutation null at this n is enormous: **|r| p95 = 0.820 at n=6 splits**, so +0.815 never cleared its own null. Sex and modality both carry large deficits at near-zero coverage gaps. **The unit was never people** — all 15,503 respondents appear in every split, but the estimand is a correlation over *splits*, and there are six. ⚠ And "nobody has pointed this hazard out" is **false, killed by `Entry 15`** — the explorer's own `analysis/swarm/14-missingness.md`, **five months earlier**, states that group comparisons on gated columns compare different subpopulations. **The hazard is theirs; one correction on one split is mine; the law was nobody's.** (`#11` `#15` `#20`)

---
## What each big round established (R = one iteration, one belief update, one commit)

**[R01 · the object](A01_object_and_structure)** — The item-level data is not the 68 category
ratings; it is 101 multiselect columns exploding to **1,332 options over 15,468 people**. Entry
to every block is gated on a parent rating: **P(enter | parent > 0) = 0.99**. This is
undocumented and it constrains every design downstream — naive cross-block holdout is
conditioned on liking the parent category. Person-profile split-half reliability **0.727**;
item base-rate reliability **0.999**. The noise injection did not destroy individual signal.

**[R02 · is there a shared grammar](A02_is_there_a_shared_grammar)** — Yes, and the first two
instruments could not see it. PC1-vs-PC1 gave 0.064 (retraction #2); held-out CCA gave 0.272 vs a
0.055 floor; leave-one-block-out gave **32/32 blocks positive**, median gain +0.0340 against a −0.0029 floor. **[BOUNDED — RETRACTIONS #25]** with all 32 coverage indicators in the baseline the gain is +0.0170 and 31/32 blocks stay positive; coverage is partly a mediator, so the honest statement is a bound of **[+0.017, +0.037]**. Factors learned from 31 domains predict a domain they never saw.

**[R03 · naming the coordinates](A03_naming_the_coordinates)** — Four coordinates survive a
**block** split-half (not a person split-half — the question is whether a coordinate is
recoverable from either half of the *domains*). Naming failed twice before succeeding. The
surviving three: *light restraint/toys ↔ insertive extremity and confinement* · *abjection and
filth ↔ blood, burning, weapons* (two **opposed** extremities, not one intensity axis) ·
*receiving a substance ↔ giving it*. None is the folk axis. **"Top/bottom" is one word over
three near-independent coordinates**: who submits, who is seen, who receives — disattenuated
**[DOWNGRADED — RETRACTIONS #24, re-affirmed #143]** "three near-independent axes / effective dimensionality 2.95 of 3" was withdrawn long ago. With a validated reliability ladder (positive control: SUBSTANCE against itself r_true = **+1.018**; sham +0.023), **POWER–SUBSTANCE r_true = +0.605** — they share **37% of variance, not 5%**. The pre-registered kill lands in the middle band (0.45 < 0.605 < 0.70): **UNVERIFIED**, neither one construct nor confirmed distinct. ⚠ **`#321`: and that is now a resolved intermediate, not a failure to measure** — the bootstrap 95% interval is **[+0.561, +0.659]**, entirely inside the band, so the two share **31–43% of variance**; the positive control (SUBSTANCE against disjoint halves of itself) recovers **[+0.943, +1.126]**. **RETRACTIONS #141 is withdrawn in full** — it re-priced the claim by re-running A02/R034 without reading the ledger entry that had already superseded it.

**[R04 · acquisition and time](A04_acquisition_and_time)** — Interests arrive on a
population-shared schedule (content-like early: appearance 14.0, body parts 14.4, clothing 14.7;
relational late: power dynamics 16.8, bondage 16.9, mental alteration 17.0). **[SURVIVES FRAMING SWAP — RETRACTIONS #50]** Within-person "acquired together" tracks "liked together" at RSA **+0.599**, and unlike the cross-domain grammar (#49) it carries predictable variance: person-level onset→preference R² = +0.0136 against a −0.0043 null, 31% of the same-domain ceiling after stripping intensity leakage ⚠ **`#358`: the schedule now has its own evidence, and a ceiling.** The 31 category means are resolved — span **3.37 y** against a mean bootstrap interval of **0.34 y**, with only **87 of 465 pairs overlapping (19%)** — and the ordering is **not** a rarity artifact (`corr(mean onset, category rarity)` = **+0.064**, stable at +0.03…+0.09 across three numeric codings of the age bins). Its order reproduces at Spearman **ρ = 0.805** between the sexes and **0.87–0.93** across current-age bands, against a random-split ceiling of **0.966** and a within-person label-shuffle floor of **−0.169**. **Shared, but not identical: the male–female 0.805 sits well below the 0.966 a random split gives.**
(−0.126 — **[VERIFICATION — see RETRACTIONS #16]**, published as `01-age-onset` Finding 1) and recall anchoring, and after near-synonymy is excluded (0.594 for
280 pairs sharing no content word vs 0.646 for 64 that do). **[CONFIRMED — RETRACTIONS #22]** onset carries structure preference does not: observed top residual eigenvalue 0.959 against a purpose-built rival world at 0.441 ± 0.040 (95% upper 0.532), positive control fires at injection 0.3 and stays silent at 0.0. The attached '80% of the sd' is descriptive, not tested — interests a person does *not* like together were acquired at the
same time. But the organising variable is arrival time, not coordinate membership (retraction #9).

**[R05 · group differences and the instrument](A05_group_differences_and_the_instrument)** —
**One split's group comparison on this release was partly measuring survey coverage**: block-count matching took pornhabit from 0.2285 → 0.0871 → 0.0439 and **reversed its ordering against sex** — measured directly on that split, and it stands. **[KILLED — RETRACTIONS #20]** the law I built on it — ~~corr(congruence deficit, coverage gap) = +0.815 across nine splits~~ — does not: median r over K ∈ {3,5,8,12} = +0.127, worst leave-one-out = −0.294 on removing pornhabit, and the permutation null has **|r| p95 = 0.820 at n=6 splits**, which +0.815 never cleared. The estimand was a correlation over *splits*, not over the 15,503 people who appear in every one of them.
**[CORRECTED — RETRACTIONS #15]** the sentence that used to follow — "not documented anywhere I have
read about this dataset" — **is false**: the explorer's own `analysis/swarm/14-missingness.md`, five
months earlier, states that group comparisons on gated columns compare different subpopulations.
**The hazard is theirs; one correction on one split is mine; the law was nobody's (`#20`).** Drawn-vs-live-action consumers — whose content contains no real bodies — differ by **0.0204 ± 0.0265**, bounded below sex-sized (0.093). **[UNVERIFIED — RETRACTIONS #34]** the written-vs-visual deficit is unresolvable at 5 seeds (0 of 9 corpus cuts have effect > 2× seed spread, while sex is resolvable in 9/9); ~44 seeds per cell would be needed. That is a real, bounded constraint on A.

**[R06 · induction](A06_induction)** — the retrospective induction claim, its two absent signatures and the breadth it actually tracks are stated once in the standing-claims table above; this section adds only what the table does not carry. **[CORRECTED — RETRACTIONS #26, caught by #144]** an earlier version of this page reported "rho +0.2515, 85% surviving response-style control" — that pair **appears nowhere in the ledger** and came from a pre-A09 draft of this page. The "85% is response style" step is **UNVERIFIED**: every item is erotic content with no reverse-keying, so general agreeableness and general erotic endorsement are not separable in this release. **Design consequence**: retrospective self-report of induction is unusable as an outcome measure — phase 3 must be prospective.

**[R07 · breadth](A07_breadth)** — Quantity without shape. A person's set is only **0.88%** more
concentrated in coordinate space than a size-matched base-rate set — real (t=−15.5) and
negligible. Sets are **24.2%** of the way from chance to perfect nesting. Breadth is moderately one trait (Spearman–Brown 0.557). **[UNVERIFIED — RETRACTIONS #26]** the '9–13% response style' figure comes from an index that is orthogonal to the POWER axis (+0.024) but correlates +0.385 with breadth itself; response style and erotic endorsement cannot be separated without balanced-keyed items, which this release lacks.

**[E01·R05 · is breadth the object](E01_sexual_as_a_value_not_a_category/A05_is_breadth_a_nuisance_or_the_object)** — Nearly domain-encapsulated. All 15 non-sexual
variables jointly: **R² = 0.012**. **[PARTLY RETRACTED — see RETRACTIONS #17]** That held only with survey progression controlled and a *gated* outcome. On the ungated `totalfetishcategory` with acquiescence controlled, childhood adversity → breadth is **r = +0.059, effect/floor 7.5** — real and small, matching the published d=0.151. Adult sexual assault, corporal punishment and upbringing remain near zero.

**[R09 · consumption](A09_consumption)** — Consumption touches both terms: it correlates with θ
(rho 0.17) *and* independently with the coordinates (**0.0439** after matching coverage, breadth
and sex — 3.6× the neuroticism reference). Direction is unavailable; this is exactly what a
prospective design would resolve.

**[R10 · additivity](A10_additivity)** — Ivan's model B requires it. The feature crosses every
substance boundary it meets (+0.24 to +0.58) **except source gender, where it is +0.017**.
Additivity is basis-dependent: in the folk basis (self/other) it fails; in a basis crossing role
with source gender it may hold. Small n on the decisive cells (3–4 pairs) — **PLAUSIBLE, not
CONFIRMED**.

---

## How the shared signal accumulates

**[RETRACTIONS #60]** `increment = 0.00723 × √(source domains)` — CV **6.4%** across n=1…31, against 11.2%
for log and 51.9% for linear (`#60`), with a permuted-label null of exactly 0.0000 at every n. **[MECHANISM REVERSED — #61, #64]** blocks are *not* interchangeable (subset variance 58× seed variance), and at
**fixed total respondent-rows more blocks is worse**: n=8 gives 0.0042, n=16 gives 0.0028, n=31 gives 0.0025.
The accumulation tracks **total sample**, not block count — the √ shape is ordinary √N. Block boundaries are a
tax on the estimate, not a source of it. Price list for new collection:
0.06 needs 69 domains, 0.08 needs 122, 0.10 needs 191 — extrapolated beyond the measured range.

## Within-block vs cross-domain

**[RETRACTIONS #59]** Two largely independent contributions, 87% additive, both against two exactly-null
controls: **cross-block factors +0.0409** (fitted on 31 *other* blocks) and **within-block structure
+0.0290**, combined **+0.0606** (`#59`). The aggregate cross-domain signal is the *larger* — pairwise block→block
is ~0 (#49), and it accumulates across blocks.

## The item margin

**[RETRACTIONS #57]** Fifty-seven rounds measured structure over PEOPLE. The ITEM side, measured once:
item-neighbour structure recovers **+0.0206** held-out R² over a marginals base, against the person
factors' **+0.0289** and a random-neighbour control's **+0.0006** — **71% of the person margin**, on
578,989 held-out cells. **[#58]** Fitted jointly, the two are **67% the same structure**: combined increment +0.0359 against the person margin's +0.0293, so the item side's unique contribution is +0.0066 — ratio 1.65, **below resolution**.

## Tools: six edges

| tool | what it checks |
|---|---|
| `tools/readme_ledger_audit.py` | README → ledger (has a number here been withdrawn by a later entry; is any number uncited) |
| `tools/round_status.py` | round → ledger (current status of the claims this round supports; has a sibling round superseded it) |
| `tools/guard_lint.py` | round → guards (were the applicable guards called; is a verdict threshold a hard-coded literal) |
| `tools/repro_audit.py` | ledger → round (does the ledger's number still come out; **is a value the ledger condemned still being printed**) |
| `guard_lint.error_bar_scan()` | artifact → precision (**does the real arm have any source of jitter at all**, `#168`) |
| `lib/gates.py` | 10 guards + the `Gate` comparison rules every round attacks itself with |

Each tool carries its own `P6` proxy ledger: **readable in the hit direction only; the output is a
must-read list, not a verdict.**

## Governance

| file | what it holds |
|---|---|
| [`RETRACTIONS.md`](RETRACTIONS.md) | **167 entries.** Every claim killed, scoped or corrected, with what killed it. Twelve are a later round of mine destroying an earlier round of mine |
| [`FROZEN.md`](FROZEN.md) | Lines where further computation cannot identify what it is measuring, each with its unfreeze condition |
| [`PREREGISTRATION.md`](PREREGISTRATION.md) | What the next rounds will test, with thresholds fixed in advance. Rounds r01–r52 were **not** preregistered and are labelled exploratory throughout |
| [`ADVERSARY_FORECAST.md`](ADVERSARY_FORECAST.md) | What I expect an outside challenger to overturn, written before one arrives |
| [`STANDARD_AUDIT.md`](STANDARD_AUDIT.md) | These 52 rounds scored against the campaign standard, including everything they fail |

## Layout

**E · A · R** — an *epoch* closes when the ontology shifts (the object turned out to be a different
object), an *arc* closes when a decision becomes safe, a *round* closes on one belief update.
**One epoch, 25 arcs, 213 rounds.** `R` is numbered consecutively across the whole project, not
restarted per arc. Every count is discovered, never chosen; see `~/.claude/CLAUDE.md` §P16.

Each round is a directory with `run.py`, `README.md` and `results/`.
`lib/rounds.py` maps a round name to its path, because several rounds reuse an earlier round's
loaders and that dependency is made explicit rather than hidden. Environment is a self-contained
`.venv` (system python 3.14 has no `ensurepip`; pip was bootstrapped).

## Scope, stated once

Cross-sectional · one instrument · one population (18–32, US/Canada/Europe) · aggressively
anonymised, with correlations attenuated roughly 25% by design · every measure self-report.
No causal claim in this repository is identified, and none is made.
