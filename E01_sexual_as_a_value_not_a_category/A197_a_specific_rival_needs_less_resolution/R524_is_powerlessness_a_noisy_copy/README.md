# R524 · 问一个更便宜的问题,顺手撞上一个从没试过的锚

⚠ **FRONTIER**。`#479` 的 NEXT:用 `#479c`(**对手越具体,需要的分辨率越低**)去碰
页面上悬着的 `powerlessness`。

## ① 先建参照尺度(否则「低」没有意义)
同族内最大 |r| = **0.373**(性行为计数 × 恋物类别数);
而**同一构念的两次测量**通常远在 **0.5** 以上。

## ② 对手:它是不是那三个方向已定的量的**噪声版本**?
| vs | r |
|---|---:|
| pornhabit | +0.024 |
| bondageaverage | +0.042 |
| **extroversion** | **−0.174** |

**最强 0.174 « 0.5** -> **对手被排除:它不是已知量的影子。**

## ★★ ③ 而最强的那一个,方向**已经确立**
`extroversion` 的方向由 `#429c` 定下 -> **它本身就是一个合格的锚**,
而 **−0.174 越过了那些计数没能越过的同一道杠**(计数最强只有 0.074)。
**正对照**(`pornhabit` 0.196 · `bondageaverage` 0.247)全部过杠。

对着「能动感是外向性的一个侧面」这个标准立场读:
**这个变量的高端 = 更没有能动感** —— 页面此前那句措辞终究是对的。

## ⚠⚠ ④ 两条实质性的告诫,已随结论一起上页面
1. `extroversion` 自己的方向**也**压在文献先验上 -> **这是第二级先验叠在第一级上**;
2. **这一对的符号先验是在看到相关之后才写下的** ——
   **而这个顺序正是本项目在别处当作警告的那个顺序**。
⇒ 所以页面把它报成**可读**,不是**已预注册**。

## 一句关于人的话
**「更没有能动感」这句话现在可以说了 —— 而它靠的是两级文献先验,不是一次测量。**

## IMPOSSIBLE
① 两级先验;② 符号先验后置;③ 只试了三个「方向已定」的量作锚。
