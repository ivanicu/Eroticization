# R480 · 扫描看不见这个性质,因为页面写的是比值,分母的区间从来不在句子里

⚠ **CLOSURE**。`#435` 的 NEXT:页面上还有别的「分子稳、分母近零」的比值吗?
**这是第六次扫描类轮次,前科五次**,所以边界先写、原文打印**并持久化**。

## ① 三道筛子,而第三道**没有判别力**
| 判据 | 剩下 |
|---|---:|
| 页面上的百分比/倍数 token(两页) | **360** |
| 上下文提到**固定分母**(样本量/轮次/列数/覆盖率)-> 排除 | 35 |
| 上下文提到**被估计的效应** | 60 |
| 其中**自己带区间的**(已经做过这件事) | 129 处 |
| **候选(提到效应 · 无固定分母 · 不带区间)** | **34** |

## ★★ ② 而那 34 条读下来**全是好的** -> 判 UNVERIFIED,不判无罪
`59.6% of its own R²` 分母是 **1.671pp** 的 R²(离零很远)·
`92%` 分母 **40.4%** · `54.8%`/`30%` 是**检出率**(分母是模拟次数)·
`45% swing` 两端 **+0.1477 / +0.1715** 都远离零 · `102%` 自带「4.9× 自身展布」。

**⇒ 仪器看不见这个性质,因为页面陈述的是**比值**,而分母的区间**从来不在句子里**。**
按 P6:**UNVERIFIED,不是无罪** —— 我没有证明页面上没有别的,我证明的是**这种扫描查不出来**。

## ③ 于是修法不是审计,是接口(`#383a`)
`lib/bounded.share(num, den_boot)` —— **让分母的自助区间成为计算占比的前提**:
- 分母区间**含零** -> 返回 `ok=False`,**且 `value` 是 `None`** ——
  **调用方拿不到那个数,而不是拿到之后被提醒不要用**;
- 自检 **3/3**(近零分母被拒 · 远离零通过 · 拒绝时不返回数值)。

**复演 `#435c`**:`pornhabit` 占比 -> **拒绝**(区间 [−0.011, +0.034] 含零);
`S` 占比 -> **放行,−0.0812**,与已发布的 **−8.1%** 吻合。
**这个接口会在 `#435c` 被发布之前就拦住它。**

## IMPOSSIBLE
① 判别式是代理,三次收窄之后**仍然**是代理 -> 结论是「查不出来」,不是「没有」;
② 手判 34 条是我一个人判的;③ 接口只管**今后**写的轮次,不追溯页面上已有的数。
