# R474 · 那个静默的失败模式在这张表里占 0.5%,而唯一一次误用是我自己一小时前造的

⚠ **CLOSURE**。`#429` 的 NEXT:`to_numeric(errors='coerce')` 还在哪里静默地留下有偏子样本?

## ① 边界先写:**部分**解析才是危险的
| 类别 | 列数 | 为什么 |
|---|---:|---|
| 完全解析(掉幅 0) | **152** | 什么也没发生 |
| 完全不解析(存活 0) | **211** | ⚠ **响亮** —— 下游全 NaN,会自己叫。`TotalMentalIllness` 正是这类,所以它被抓到了 |
| **部分解析(掉>10% 且存活>0)** | **2** | **静默的有偏子样本** |

覆盖率 **365/365**(100%)。**⇒ 这个失败模式在本表的占有率是 0.5%。**

## ② 用过的只有 1 个,而且是已知的那个
`sexcount`(15,263 -> 4,524,**掉 70.4%**)—— **正对照通过**(已知的那个必须被找到,它被找到了)。

## ★ ③ 而项目早先的轮次是**对的**
`R410`/`R454`/`R455`/`R456`/`R457` 的每一处数值化都走**显式分档映射** `SXMAP`,不是 `to_numeric`。
**⇒ 唯一那次误用是我自己在 `R473` 造的,不是继承来的。**

## ④ 副产物:项目里有**两套** `sexcount` 映射 -> 于是做了规格曲线
| 映射 | r | \|z\| |
|---|---:|---:|
| 序数 0..4(`R473` 用的) | **+0.1458** | 18.3 |
| 档中点(`R410` 用的) | **+0.1235** | 16.2 |
| 对数中点 | **+0.1463** | 18.3 |
| **二值「零 vs 有」**(丢掉全部分档信息) | **+0.1154** | 14.0 |

**四种全部同号 -> `#429c` 的方向结论不依赖映射的选法。** 已写上页面。

## IMPOSSIBLE
① 「用过」按列名点名(`#423b` 同一个坑);② 掉幅阈 10% 是我选的,但 2 个在场的都远超它(70%);
③ 本轮不检查**映射本身**对不对,只检查方向结论是否依赖它。
