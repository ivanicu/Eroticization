# R506 · 把判据的正对照变成**必填参数**,然后拿它重跑历史

⚠ **CLOSURE**(除非有历史判定改变;没有)。`#461` 的 NEXT。

## ① 工具:`lib.bounded.anchor_rule`
`known_direction` 是**必需参数** -> **先在正对照上跑**;
**有任何一个不过杠 -> 抛 `PositiveControlFailed`,不返回任何候选的判定**
(与 `share()` / `mediation_headroom()` 同一形状:**拿不到那个数**)。
自检 **3/3**(正对照失败必抛错 · 通过则放行 · 抛错时不产出判定)。

**对照 2**:把它指向**原来那套单锚集** -> **确实抛错** -> **执行不是空的。**

## ② 重跑历史三次(各用**自己的**掩码,不混)
| 案例 | 变量 | 最强 \|r\| | 判定 | 台账 |
|---|---|---:|---|---|
| `#428c` 全样本 | `powerlessness` | 0.074 | 否 | 否 ✓ |
| `#428c` 全样本 | `extroversion` | **0.146** | 是 | 是 ✓ |
| `#428c` 全样本 | `pornhabit` | **0.196** | 是 | 是 ✓ |
| `#428c` 全样本 | `bondageaverage` | **0.247** | 是 | 是 ✓ |
| `#442b` 块掩码 | `c1` / `c2` / `c3` | 0.131 / 0.376 / 0.460 | 是 | 是 ✓ |

块掩码上三个正对照:**0.139 / 0.190 / 0.173** —— 全过。
**⇒ 与台账不一致的判定 = 0 -> HISTORY_STANDS。**

## ⚠⚠ ③ 而第一版跑出「4 处不一致」,错的是**我**
我把另外四个 Big Five 在 `LEDGER` 里标成「锚得住」——**凭记忆敲的**。
页面原文写的是:它们「**隔一层继承**……是**关于流水线的推断,不是对每个特质的测量**」。
**它们从未被主张锚得住。**

**⇒ 检查的**期望值**必须从记录读,和它的**方法**必须从记录读,是同一条规矩。**
**而这是第一次,凭记忆敲的那一半是「期望」而不是「方法」。**

## 一句关于人的话
没有,这是仪器轮。**产出:一个会拒绝在没有正对照时给出判定的判据,以及一次干净的历史重跑。**

## IMPOSSIBLE
① 只有三个方向已固定的锚;② 只重跑了三次历史,不是全部;③ 阈 0.10 仍是选的,
**但三个正对照都远高于它、`powerlessness` 远低于它,结论不依赖这个值**。
