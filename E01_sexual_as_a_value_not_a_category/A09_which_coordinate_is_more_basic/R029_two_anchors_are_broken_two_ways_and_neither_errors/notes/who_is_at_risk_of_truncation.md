# R476 · 一条覆盖四分之三轮次的教训,本来就不可能靠记住修好

⚠ **CLOSURE**。`#431` 的 NEXT:把 `#398d` 从「靠记住」搬进「靠接口」。

## ① 先证明措辞是代理
458 轮里**只有 51 轮(11%)**真的从 `results/` 读回数据 ——
**而其余 407 轮也没有违反 `#398d`**,它们在内存里算完就下结论,**根本没经过终端**。
**⇒ 「从 `results/` 读数」是目的的代理,和 `#431c` 完全同形。**
**真正的失败是:让一段**可能被截断**的终端输出成为证据。**

## ★★ ② 而截断只在输出长时发生 —— 于是问题变成可测的
| | |
|---|---|
| 全部轮次 | **459**(覆盖率 100%) |
| **输出行数不可预先知道** | **347 = 76%** |
| **本会话 10 轮里** | **8 / 10** |

**判决 = STRUCTURAL。** 一条覆盖 76% 轮次的教训,**不是纪律问题,是接口问题**(`#383a`)。

## ③ 修法:`lib/bounded.show()`
把一张表送上终端的**唯一**入口:最多打印 n 行 · **同一次调用把整张表写进产物** ·
**最后一行永远说明「完整的在哪里」**,所以屏幕上的东西**自称是节选** · 被截掉的行数是**打印出来的**。
自检 **4/4**(截断被报出 · 全显时不谎称截断 · 产物真写了 · 行数与源一致)。
**本轮自己用它输出** —— 打印 12 行,并说出其余 335 行在哪。
**一个被当作修法提出、却不被提出它的那一轮使用的工具,是建议,不是修法。**

## ⚠ ④ 它管不到的那一半,写下来而不假装解决
我在会话里**临时跑的 shell**不经过 `run.py`,而本会话两次违反 `#398d` **正是那样发生的**。
**接口管住了轮次的输出,管不住我自己的手。** 这一半仍在「不可机械检查」类里。

## IMPOSSIBLE
① 「不可预先知道」是**静态代理** —— 遍历一张 3 行表的循环实际上是安全的
   -> **76% 是风险的上界**,不是风险率;
② 但结论(「不是罕见」)不依赖这个数的精确值;③ 只查 `run.py`,不查临时 shell。
