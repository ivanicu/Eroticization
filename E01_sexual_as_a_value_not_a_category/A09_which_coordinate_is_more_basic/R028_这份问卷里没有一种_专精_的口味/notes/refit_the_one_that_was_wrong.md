# R471 · 我在写下那条一般化的**同一轮**里,就把它的符号搞反了

⚠ **FRONTIER**。`#426` 的 NEXT:并排跑 `R449` 唯一在范围内的零。

## ① 预测,写在跑之前(`#426` 的 NEXT 里已注册)
> 破坏结局间相关会让零**更宽**,于是低离散度看起来更显著,**偏向通过**。

## ② 并排(差别只有零这一项)
| 零 | 均值 | **sd** | 5 分位(下侧) | 判定 |
|---|---:|---:|---:|---|
| 旧 · 逐结局各自打乱 | 0.55105 | **0.21578** | **0.13566** | 落在零里 |
| 新 · 整行一起搬(正确) | 0.56495 | **0.21635** | **0.18353** | 落在零里 |

观测圆离散度 **0.66153**,远在两个阈之上 -> **`R449` 的结论一个字不动。**

## ★★ ③ 而预测**错了,而且是符号错**
- 零的**宽度**几乎没动:**0.21578 -> 0.21635,×1.00**(我预测「更宽」)。
- 下侧 5 分位从 **0.136** 移到 **0.184** -> **旧零比新零更难通过**,
  即旧零是**偏向失败**的,不是偏向通过。

**⇒ `#425d` 那条一般化只在它被测出来的那个统计量上成立。**
`R468`/`R469` 是**符号计数**:逐列打乱把零从二项的 ≈55–79 撑到 **34–104**。
这里是**圆离散度**:同样的破坏,把零的宽度改变了 **0.3%**。
**同一个机制,两个统计量,一个被撑宽一倍,一个纹丝不动,而且尾巴还朝反方向。**

**⇒ 零的偏差方向与大小是**统计量**的属性,不是**零**的属性 ——**
**必须逐个统计量测,不能从机制推。我从机制推了,一轮之内就把符号推反了。**

## ⚠ 三值
**KILL 记 UNVERIFIED**:我写的 KILL 是「`R449` 在正确的零下仍然稳定」,
但 `R449` 从来没主张过稳定 —— 它的结论就是**不稳定**(一个有功率的零,MDE 0.10 ≤ 有意义 0.20)。
**测的是我的前提,不是它的结论。** 不折进 OVERTURNED。
**并排比较本身记 CONFIRMED**(两个对照都过:`row_perm` 保住结局相关 `max|Δcorr| < 1e-9`;
观测量不随零改变)。

## IMPOSSIBLE
① 400 次零 -> 5 分位的分辨率有限;② 只在这一个统计量上测了,**不外推**(这正是本轮的教训);
③ `exec` 拼接会重绑变量名 -> 本轮 gate 改名 `GATE`(`g` 被 `R449` 占用)。
