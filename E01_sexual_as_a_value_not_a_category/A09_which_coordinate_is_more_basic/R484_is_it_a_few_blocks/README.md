# R484 · 不是「你喜欢什么」,是「你的喜欢怎么分布」

⚠ **FRONTIER**。`#439` 的 NEXT:`c3⁻` 通往「改不了」的直接系数(**−0.0355**)
是页面上最大的一块没有解释的东西 —— 它从哪来?

## ① 机器是拼来的,不是重写的(P4)
`R371` 已有 `c3_drop(drop, ref)`(留一块重建 + **符号对齐**,`#368a` 那个坑本项目栽过四次);
`R372` 已确立块载荷与符号约定(正 = 更羞耻端)。
⚠ **命名冲突**(`#427e`):`R371` 把 `A`/`B` 绑成块矩阵,`R449` 把 `A` 绑成 `z(S)` ->
**先把 32 个留一块向量全部算完存好,再 exec `R449`。**
**对照**:两条管线的全量 `c3` 相关 = **+1.0000** -> 它们描述的是同一个坐标。

## ⚠⚠ ② 预注册的 KILL 会发布相反的结论
写的是「某一块的移除使系数移动 > 1 个自助 sd」。
**但那是 32 块里的最大值** —— 一个 32 取最大几乎必然越过 1 sd。
**正确的杠是最大值自己的零分布**:自助人层,每次算完 32 个位移取最大。
**两个判决都留在记录里**(`#439d` 同一条)。

| | |
|---|---:|
| 全量系数 | **−0.03554** |
| 自助 sd | 0.01044 |
| 32 块位移的**中位** | **0.25 sd** |
| 第二大 | 0.80 sd |
| **观测最大**(第 11 块) | **1.86 sd** |
| **多重性零(最大值分布)95 分位** | **2.24 sd** |
| 预注册(错的)门 | 通过 -> **CONTENT_SPECIFIC** |
| **改正后的门** | **未越阈 -> COORDINATE_LEVEL** |

## ③ 旁证:按载荷抽掉前 k 块
k=1..6,系数 **−0.0377 ~ −0.0449**,**从来没有比起点 −0.0355 更弱**。
⚠ 这条曲线是**近似**(取那些留一块拟合的均值),不是真正的留 k 块重拟合 ->
**作为旁证报出,不作为检验本身。**

## 一句关于人的话
**「这件事我改不了」的感觉不来自任何特定内容。**
**承载它的是一个人在整份问卷上的图样的**形状**,不是图样里装的东西。**

## IMPOSSIBLE
① 横断面;② 32 块,n=6,715;③ 抽 k 块的曲线是近似;
④ 「没有任何一块携带它」= **在这个设计的分辨率下**,不是「一定不存在」。
