# R470 · 边界先写,于是 24 变成 6,6 变成 1

⚠ **CLOSURE**。`#425` 的 NEXT:哪些零「逐列打乱 + 跨多列汇总」?

## ① 边界写在扫描之前(已有四次过度指控:`#382c` `#394c` `#407a` `#422b`)
**在范围内**:① 零**逐列**打乱,**且** ② 它喂的统计量**跨 ≥2 列汇总**。
**不在范围内**:零喂的是**单列**统计量 —— `perm_in` 在那里是**正确**的,没有列间相关可破坏。

## ② 三道筛子,每道都收窄
| 判据 | 剩下 | 性质 |
|---|---:|---|
| 含逐列打乱的文件 | **222** | — |
| 同文件出现跨列汇总的词 | **24** | ⚠ **共现,是代理不是判定** |
| `perm_in` 在**遍历多列的循环体内** | **6** | 结构 |
| **手读后真的在范围内** | **1** | 逐题读原文 |

## ★ ③ 那五个「假阳」都是同一种,而且它们是**对的**
`R465` · `R446` · `R450` · `R442` · `R467` 打乱的都是**单一向量**,对着**固定**的多列 ——
**那些列的相关反而被完整保住**。`R450` 的代码注释自己就写着:
「打乱 `S`,保住结局之间的相关」。
**⇒ 「逐列打乱」这个描述抓错了对象:要紧的不是打乱了几列,是**被打乱的那一侧有没有携带要保住的相关**。**

唯一在范围内的是 **`R449`**:四个结局**各有各的 seed**(`8600+s_*7+k`)。

## ⚠ 负对照失败,而失败的是对照本身
我写的负对照是「`R469` 应被识别为已修」,但 `R469` 只用 `row_perm`,
**根本没进候选集** -> 对照的前提就是错的。按 P6 记 **UNVERIFIED,不是通过**。

## IMPOSSIBLE
① 第二道筛子是**共现代理**,只用来收窄,不用来判定;
② 手读是我读的 -> **单人判断,没有独立编码者**;③ 覆盖率:222/222 含逐列打乱的文件全扫。
