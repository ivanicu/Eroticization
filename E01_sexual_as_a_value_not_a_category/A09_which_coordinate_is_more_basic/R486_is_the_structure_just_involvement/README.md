# R486 · 卷入度在**遮蔽**这个关系,不是在制造它 —— 而我的锚定判据判错了自己的正对照

⚠ **FRONTIER**。`#441` 的 NEXT:`c2` 与性行为计数相关 −0.38,
**那「残差结构预测改不了」会不会只是「卷入得多」换了个说法?**
**预注册预测:B 部分成立(`c3` 活,`c2` 塌)。**

## ① 主结果:两臂并报,而三个都没塌
| 成分 | 不控计数 | **控计数** | 保留比 | 越阈(阈 0.0273) |
|---|---:|---:|---:|---|
| `c1` | 0.0520 | **0.0524** | ×1.007 | 是 |
| `c2` | 0.0534 | **0.0620** | **×1.162** | 是 |
| `c3` | 0.0355 | **0.0465** | **×1.312** | 是 |

**判决 = BEYOND_INVOLVEMENT。** ⚠ **我预测 `c2` 会塌 —— 本会话第四次预测错。**
**⇒ 卷入度是在遮蔽这个关系,不是在制造它。**
**对照**:不控计数那一臂精确复现 `#441`(`c3` = 0.03546 vs 0.03554)-> 变的只有控制集。

## ★★ ② 而一个对照失败,它比主结果更值钱
**`CONTROL2` 失败**:方向**已确立**的 `pornhabit` 在这个块掩码上对性行为计数只有 **+0.0993**,
**卡在我自己设的 0.10 杠下**(`#428c` 的 +0.181 是在全样本 15,503 上的)。

**⇒ 我在 `#441` 用的「单锚 ≥ 0.10」判据,会把一个方向已知的变量判成锚不住。**
**一条会判错自己正对照的判据,量的是判据,不是变量。**

改成「**两个锚任一过杠**」后:
| | vs 性行为计数 | vs 恋物类别数 |
|---|---:|---:|
| **正对照** `pornhabit` | +0.0993 | **+0.1394** |
| **正对照** `bondageaverage` | **+0.1900** | +0.1331 |
| `c1` | +0.0277 | **−0.1307** |
| `c2` | **−0.3764** | +0.0095 |
| `c3` | **+0.4600** | −0.0374 |

**三个成分全部锚得住,而且各自只被两个锚中的一个锚住。**
**`c1` 的读法**(`#441` 说它「符号不可用」,已在页面更正):
**恋物类别越多,越不觉得这件事能改。**

## 一句关于人的话
**「觉得改不了」不是「卷入得多」的另一种说法 —— 把卷入度拿掉,它反而更清楚。**

## IMPOSSIBLE
① 加控制会同时吸走真信号 -> **两臂都报了**,结果是**比较**不是后一个数;
② 这个块掩码 n=6,712,锚的功率本就比全样本低一半 -> 杠必须按**正对照**定,不能凭空定;
③ 「卷入度里没有的东西是什么」—— **本轮没有回答,页面上也没有一句话给它命名**。
