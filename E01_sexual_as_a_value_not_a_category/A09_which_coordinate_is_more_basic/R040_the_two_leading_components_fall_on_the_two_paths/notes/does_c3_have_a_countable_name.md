# R508 · 我 NEXT 里的前提是反的,而查它顺手改了页面上一个名字

⚠ **CLOSURE**(世界 B 没触发)。`#463` 的 NEXT:`c3⁻` 有没有可数的外部对应物?
⚠ **而 NEXT 的前提本身可疑,所以先查它** —— `#442b` 自己的表里 `c3` 与性行为计数是 **+0.46**。

## ① 三个成分 × 六个可数锚(判据用 `#462a` 的**正对照必填**工具)
| 成分 | 最强 \|r\| | 最强的锚 |
|---|---:|---|
| `c1` | **0.2272** | 总勾选项数 |
| `c2` | **0.3764** | 性行为计数 |
| **`c3`** | **0.4600** | **性行为计数** |

正对照(块掩码)全部过杠。**对照**:`c3` 与性行为计数复现 `#442b` 的 +0.4600。

## ⚠⚠ ② 前提是反的 —— 本会话第四次
我以为 `c3⁻` 只有结构级的名字、`c1⁺` 有内容级的;
**实测 `c3` 的可数对应物是三个里最强的,比 `c1` 强一倍。**
**⇒ 给 `c3⁻` 命名的五次失败,不是因为缺一个外部相关物。**

## ★★ ③ 而同一张表改了页面上一个名字
`c1` 的四个锚**全部同号**(恋物 −0.131 · 块覆盖 −0.213 · **总勾选 −0.227** · 起始类别 −0.183)
-> **定向不变**;
**但恋物类别是四个里最弱的** -> **它不是「恋物广度」,它就是「整体卷入广度」那条方向** ——
与 `COVB`/`ncat` 标记的是同一条,而它落在「能不能改」上的位置**与它们一致**。
**页面上 `#463` 那句话已改为按卷入广度读。**

## 一句关于人的话
**那条通向「觉得能改」的路,标记它的不是「喜欢多少种恋物」,是「整体上摊开了多宽」。**

## IMPOSSIBLE
① 只有六个可数锚;② 块掩码;③ **本轮没有回答**「为什么 `c3⁻` 命名会失败」,只排除了一个原因。
