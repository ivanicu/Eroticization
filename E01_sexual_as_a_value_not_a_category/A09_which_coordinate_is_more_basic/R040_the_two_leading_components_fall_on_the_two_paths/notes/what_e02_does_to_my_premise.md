# R512 · 另一个会话在同一个目录里开了一个纪元,而它落在我这条线的前提上

⚠ **REFLEX**(信念更新 + 一次地址修复;本轮**不跑新数据**)。

## ① 先修地址(§P16:R/A/条目号是**地址**)
`RETRACTIONS.md` 里 **`#466` 出现两次**,`R510` 也是 —— 两个会话同时在这个工作目录里跑。
**改我这一份**(对方是 Ω 级且是 HEAD):`#466`→**`#467`**,`R510`→**`R511`**(`git mv`,不是重建)。
**⇒ 全局连续编号在单会话下是一条规矩,在多会话下是一个**需要仲裁的资源**,而项目里没有仲裁。**

## ★★ ② 而它落在我这条线的**前提**上
`E02·A189·R510` 的起因是 **Ivan 今天给的反例**(原话在它的 `run.py` 里):
「黑金属和吃屎都小众,一个觉得帅一个觉得羞耻」·「习近平一个人当皇帝很小众,为什么不羞耻」。
它在 **186 个社会**上分别取「怎么看待」与「有多常见」两列,得到:
**婚前性 ρ=+0.78(n=97,越族零)· 强奸 −0.43(n=17)· 同性 −0.36(n=34,两者都在零内)。**

它自己把 +0.78 **降级为 UNVERIFIED-for-mechanism**(态度与频率是**同一批编码者从同一批民族志**编的),
并把「压得住/压不住」这个读法标为 **D4,由数据生成、未建立**。

## ③ 我这条线因此改变的,和没有改变的
**没有改变**:`#465`/`#467` 的数一个字不动 —— 它们是**人层**的,`E02` 是**社会层**的。
**改变的是它们能意味着什么**:我这几轮一直在缩「`c3⁻` 的那 69% 是什么」的靶子,
而其中一个一直没被点名的候选是**「社会压制了多少」**。
**若 `E02` 的 D4 读法成立,那个候选在性兴趣这个领域上是被排除的** ——
因为那正是规范**压不住**的那一类。
⚠ **但它是 D4,不是结论** -> 我记下的是**一个具名的对手**,不是一次前提替换。

## ④ 我不写它的页面
`E02` 的页面表述归那个会话。**跨会话越界去写对方的结论,是把两条独立的线合并成一条**,
而 `#458b` 那条教训(两个会话= 两条独立证据)在这里正好反过来适用。

## 一句关于人的话
**我这四百多轮把「稀有 → 羞耻」当机制,而另一个层次上的证据说:**
**那种耦合只出现在规范**压得住**的行为上,而性兴趣不是那一类。**
**⚠ 这句话目前是 D4。它现在有名字,还没有证据。**
