# R466 · 一个 90 处的问题,被一次可判定的检查变成了一条定义句

⚠ **CLOSURE**(§0.2)。`#421` 的 NEXT:先找出来,修补留到下一轮。
**⇒ 而「找出来」的结果改变了「该修什么」,所以修补在本轮完成 —— 理由写在下面。**

## ① 扫描:未点名的处数,三个窗宽一致
| 窗宽 | 泛称处数 | **未点名** |
|---:|---:|---:|
| ±80 | | **112** |
| ±120 | | **108** |
| ±200 | | **105** |

**三个窗宽给出同一个结论** -> 不是窗宽在说话(`#377a` 的教训)。
⚠ **覆盖率与结论一起报**(`#376b`):正则找不到的不是「没问题」,是「没看」。

## ⚠ 而 108 是一个**明显虚高**的上界 —— 第四次过度指控
打印原文后能直接看到两个过度匹配源(`#374b` 的规矩正是为此):
**`广度型`**(那是 `c3` 这个**坐标**的名字,不是计数)· **已在原地限定的「答题类别数」**。
按 `#383a`:**不再加强正则**,只移除一个**从来就不在范围内**的词 -> 剩 **46 + 44 = 90**。
**仍然是上界。**

## ★★ ② 于是把问题换成一个**可判定**的
**页面上有没有任何一处实际指「性行为计数」却没点名?**
| 页 | 提到「性行为计数」的处数 | 其中**已点名** |
|---|---:|---:|
| `README.md` | **7** | **7** |
| `README_zh.md` | **8** | **8** |

**全部 15 处都写作 `Totalsexacts` / 「认可的性行为计数」/ "endorsed sex acts"。**
**⇒ 一句裸的「广度」在这一页上**从不**指那个落单的量。**

## ⇒ 修补是**一条定义句**,不是 90 处标注
已写上两页:**未加限定的「广度」= 卷入家族**(起始类别数 · 块覆盖数 · 总勾选项数 · 恋物类别数,
彼此 +0.62 ~ +0.86)· **而性行为计数在被指涉时总是被点名**。
> **`#421` 的 NEXT 说「修补留到下一轮,因为找出来与改对是两件事」。**
> **那句话是对的,而它正是让本轮改对的原因**:第一步找出的不是一张 90 项的待办,
> 是**「逐处标注」这条路不成立**的证据 —— 于是第二件事变成了一句话,而它可以在同一轮里做完。
> **⇒ 分成两步的价值不在于「拖一轮」,在于第一步可以**改变**第二步是什么。**

## 对照
`lib/breadth_audit.controls()` **4/4** ✅(两个正对照 · 两个负对照)·
**正对照**:`#421` 刚写上页面的那两段必须被判为 named -> **通过** ·
三个窗宽结论一致 ✅。

## IMPOSSIBLE
① 「点名」是**代理**:窗口内出现具体量名 **⇏** 那一处说的就是它 ——
**未点名清单是上界,已点名计数是下界**;
② 窗宽是我选的(已扫三个);③ 本轮不判断哪一种广度「更对」。

## 一句关于人的话
没有。**产出是:一个 90 处的问题,被一次可判定的检查变成了一条定义句。**
