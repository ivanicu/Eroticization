# R475 · 我按教训的**措辞**写了检查,而不是按它的**目的**

⚠ **CLOSURE**。`#430` 的 NEXT:本会话自己新写的 9 个轮次,有没有触犯已记教训?
覆盖率 **9/9**。

## ① 正对照写在跑之前
**我已知本会话违反 `#398d` 两次**(读了两次被截断的回滚屏)。
**⇒ 一个返回「零违反」的扫描是错的,不是干净的。**

## ② 结果:教训分成两类,而复发的全在第二类
| 教训 | 可机械检查 | 机械查出的违反 |
|---|---|---:|
| `#385c` 含 NaN 的数组不得整体打乱 | 是 | 0 |
| `#392e` 新变量进主量前打印值集 | 是 | 0 |
| `#379c` 门必须标 branch | 是 | 0 |
| `#374b` 过度指控要打印原文 | 是 | **4** -> ⚠ 见 ③ |
| **`#398d` 从 `results/` 读数** | **否** | — |
| **`#383a` 改接口而不是记更牢** | **否** | — |

**4 / 6 可机械检查 · 2 / 6 不可。**
**⇒ 而我本会话真正违反的那一条(`#398d`),恰好在不可检查的那一类里 ——**
**所以机械检查一次都没看见它。分类本身就是预测:要记住的,就是会复发的。**

## ★★ ③ 而那 4 个是**第五次过度指控**,原因很具体
四轮**全部把原文持久化到了 `results/*.csv`**
(`value_sets` · `percolumn_null_sites` · `other_columns` · `coercion`)。
**`#374b` 的实质满足了,走的是产物而不是 stdout。**

**⇒ 我按教训的**措辞**(「打印」)写了检查,而不是按它的**目的**(「可复查」)。**
而这两者在这里**恰好相反**:stdout 会被截断(那正是 `#398d`),
**所以「持久化」严格优于「打印」,而我的检查惩罚了更好的那条路。**

⚠ **真正剩下的一条**:`R470` 对六个调用点的逐字手读发生在**一次性 shell 命令里**,
不在 `run.py`,也不在 `results/` -> **那次检查无法从产物复现**。这是一个**新的、真的**缺陷,
由一个瞄准别处的检查顺带打出来。

## IMPOSSIBLE
① 只查本会话 9 轮,不重审历史;② 六条教训是我挑的,不是全部;
③ 「不可机械检查」是**就我当前想得到的检查而言**,不是证明不可能。
