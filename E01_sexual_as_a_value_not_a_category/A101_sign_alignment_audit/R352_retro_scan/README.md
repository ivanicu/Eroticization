# R352 · 回溯扫描:特征向量符号 —— 没有新的受害者,而修复是被包含的

`#306b` 是本项目**第三次**撞上特征向量符号,前两次靠人眼。现在有了 guard 20,回头扫一遍。

⚠ **这是 Closure,不是 Frontier。**

## ⚠ 先过 L3 的先例闸口 —— 而它当场救了这一轮
`R272_align_the_block_signs_and_see_who_it_hurts` **已经做过这件事**:
`#226b` 诊断 `R210:73` 把 32 块的 `Z@pc` 直接相加(17 正 15 负),`R272` 用**留一共识法**修好、
翻了 15/32 个块,并在同一轮里定价(`animated` −0.1030 → −0.0522,`biomale` +0.0870 → +0.1255)。

> **我差一步就把一个三百轮前的已知 bug 当成新发现写出来。**
> 拦住它的是**先读轮次的名字**,而不是任何一个守卫。

## 所以还活着的问题不是「有没有 bug」,是「修复传下去了吗」
| | |
|---|---:|
| 全仓 `run.py` | **342** |
| 用了特征分解 | **73** |
| 跨版本/劈分/种子比较过 | 26 |
| 其中检出对齐符号 | **3**(12%) |
| 把每块 PC 分累加的轮次 | **17** |
| 其中未检出对齐 | **10** |
| **`R272` 之后仍未对齐的** | **0** |

**十个未对齐的轮次全部早于 `R272`,而 `R272` 之后一个也没有。修复是被包含的。**

## 宽判据标出的 5 轮,逐个读过
| 轮次 | 判定 |
|---|---|
| `R271` | 一块一次 `eigh`,分半信度取了 `abs()` —— **符号已被有意处理** |
| `R274` | 两次 `eigh`,比的是**前 k 维子空间**(投影),符号不变 |
| `R282` | 两次 `eigh`,比的是 ‖P1·V2‖_F —— **投影范数,符号不变** |
| `R303` | **只有一次** `eigh`;结论是两个相关的**对比**,全局翻号会同时翻两个 |
| `R323` | **只有一次** `eigh`;同上,单一全局符号约定 |

**一个受影响的也没有。**

## 这一轮真正产出的东西
不是发现,是**从一次性修复变成常驻检查**:
`R272` 是手工的、一次的;**guard 20 是自动的、往后每一轮都开火的。**

## 一句关于人的话
没有 —— **这一轮只说明:一个被修好过的错误,值得再问一次「修好之后呢」。**
