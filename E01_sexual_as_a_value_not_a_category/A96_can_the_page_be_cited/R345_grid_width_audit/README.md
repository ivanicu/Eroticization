# R345 · 公开页面上的每条带数断言,背后是几个格

`#299e` 写下的流程规则是「上公开页面必须带上它活过的那张网格」。**本轮扫了一遍,
而正对照当场把那条规则否掉了。**

⚠ **这是 Closure,不是 Frontier** —— 它不分开任何世界,它保护的是「这一页上的话能不能被引用」。

## 正对照 FAIL,而 FAIL 是对的
我注册的是「`#298` 必须被标为无网格」。**仪器说它有网格 —— 仪器是对的。**
`#298` 有一张**三格、符号一致**的网格(参照群体的三种切法:+0.0545 · +0.0438 · +0.0233)。

> **它仍然在下一轮被推翻,因为推翻它的旋钮(选项子集)当时不在网格里。**
> **所以 `#299e` 的补救是不够的:网格只能覆盖我已经知道的旋钮。**
> 一张扫过自己旋钮清单的网格,**是自审穿了一件表格的衣服** ——
> 而真正抓到 `#298a` 的不是扫描,是有人去**发明**了一个新旋钮。

**改写后的流程**:一个结论上公开页面前,要花一轮去**试着发明一个能弄坏它的旋钮**,
而不是去扫已经在 `CALIBER.md` 里的那些。**扫描是 Closure,发明旋钮才是那个对抗者。**

## 计数(按**块**归属引用,不按行 —— 按行会把 12% 读成 46%)
| | |
|---|---:|
| 带数的行 | **100** |
| 能检出网格证据 | **75**(75%) |
| **有引用但检不出网格** | **13**(13%) |
| 完全没有账本引用 | **12**(12%) |

**13 条宽度 1 的行集中在三块**:
- **L16–24 头段** —— 1.4% / 0.14% / 7.8% / 49% / 0.715%,只引 `#264`。
  ⚠ 而 `CALIBER.md` **就是**这个数的网格 —— 页面没指过去。
- **L88–90 `form_i`** —— 增量 +0.21 ± 0.13pp,只引 `#278`。
- **L97–104 共享测量误差** —— 引 `#264`/`#270`/`#271`,检不出网格标记。

## 仪器的两个对照
- **负对照**:剔掉网格关键词后仍检出 199/283(表结构还在);**再剔掉表格后 0/283** ——
  它检的是关键词与表结构,不是文本长度。
- **P5★**:检出 75 条有网格、标出 13 条宽度 1 —— 仪器两个方向都返回过非零。

⚠ **安全侧(P6)**:检出网格 = **UNVERIFIED,待读**;检不出 = **确定要标注**。
它挑得出「明显没有网格」的,挑不出「网格做得不好」的 —— `#298` 就是后者。

## 一句关于人的话
没有 —— **但这一页现在知道自己有 13 句话是站在一个格上的。**
