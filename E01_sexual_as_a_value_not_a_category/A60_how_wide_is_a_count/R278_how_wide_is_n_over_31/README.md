# R278 · 「N/31 越阈结局数」到底有多宽,以及宽度由什么决定

**类型:CLOSURE**(如实标注 —— 它不分开任何世界,它保护的是一个横跨多轮的**写法**)。
`#232d`:同一个分数、同一次运行里,越阈个数两次算出 **10 和 8**。

## 25 次独立重抽阈值

| 分数 | N 均值 | sd | 实测跨度 | 阈值 | 阈值 sd | 边界结局数 |
|---|---:|---:|---:|---:|---:|---:|
| 位置分 S | 19.84 | 0.46 | 19–21 | 0.0377 | 0.0025 | 2 |
| **跨块对比 D** | 9.16 | **1.57** | **6–11** | 0.0457 | 0.0046 | **5** |
| 成分 1 | 11.16 | 0.37 | 11–12 | 0.0380 | 0.0026 | 3 |
| 成分 3 | 19.76 | 1.07 | 19–23 | 0.0378 | 0.0030 | 5 |
| 纯噪声 | 0.00 | 0.00 | 0–0 | 0.0374 | 0.0026 | 0 |
| 极强(12 个结局之和) | 23.08 | 0.56 | 22–24 | 0.0373 | 0.0033 | 2 |

**★ `corr(边界结局数, sd(N)) = +0.9078`,而两个极端都窄(噪声 0.00 · 极强 0.56)。**

> **一个计数的宽度不来自分数有多强,来自有多少个结局正好坐在阈值上。**

所以「N/31」不是一个数:`#232` 写的 `~8–10` 方向对但偏窄,实测是 **9 ± 1.6 → 6–12**;
`#230a` 的成分 3「21」落在 18–22 内,但中心值是 **~20**。

## ⚠ P7:新守卫在第一次使用时就被自己的正对照打出一个假阳
第 11 个守卫 `count_needs_interval` 上线后第一次跑,**FAIL 了纯噪声那一行** ——
它的计数钉死在 0/31,25 次重抽都给同一个数,**展布真的是 0**。
守卫把「展布为 0」一律读成「你没重抽」。

> **「没重抽」与「重抽了而它不动」是两件完全不同的事:前者未知,后者是**已测得的窄**。
> 把两者折成同一个 FAIL,就是 P6 的三值塌成两值 —— 一个已知的窄区间被当成了未知。**

已修:`spread == 0` 且 `n_resamples >= 10` 且来源合法 -> `PASS`,并标注「钉死」。
修后重打,全门通过。

## 一句关于人的话(这一轮没有关于人的话,如实说)
这是一轮 Closure。它没有告诉我关于人的任何新东西 ——
它只是让此前二十几轮里所有「31 个里有 19 个」这种话,
**从一个听起来很硬的数,变回一个有宽度的量**。
