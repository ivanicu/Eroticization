# R421 · 「孤立」占比随窗宽翻转 —— 而窗宽不变的那个量,回答了问题

⚠ **CLOSURE**(§0.2)。产出是**一个关闭的决定**,不是一个发现。

`#376b`:核对器覆盖率 3.1%。两个世界:**A 可核而未核**(扩正则)· **B 印的时候就没带约束**(改写作规则)。

## 规格曲线(三个窗宽全部报,不报单格)
| 窗宽 | 已核 | 可核而未核 | 孤立 | 孤立占非原始量 |
|---:|---:|---:|---:|---:|
| ±100 | 10 | 110 | 141 | **54.0%** |
| ±200 | 10 | 166 | 85 | **32.6%** |
| ±400 | 10 | 219 | 32 | **12.3%** |

**⚠ 「孤立」占比从 54% 翻到 12% —— 它是窗宽的函数,不是页面的性质。**
注册的 kill(>50%)只在 ±100 会开火。**所以世界 B 报不了,而世界 A 也不能靠这一列断言。**

## ★ 但有一个量在三个窗宽下都不动
**已核 10 : 可核而未核 110–219** —— 相差**一个数量级**,方向在三个窗宽下一致。
**⇒ 核对器的 3.1% 覆盖率,受限于**我的正则**,不是受限于**页面的写法**。**

## ⇒ 关闭的决定
**扩正则是高杠杆的一步;写作规则暂时不改。**
(`#375c` 的推论「凡印导出量,就把输入量一起印」仍然成立,只是**本轮没有证据说它是当前瓶颈**。)

## ⚠ 负对照失败了一次,而它是对的 —— 本窗口第三次子串假阳性
我的合成负对照句是「一句话里只有一个**孤零零**的数字 **0.1234** 而已」,
被判成「可核而未核」—— 因为 `MARK` 里有一个**裸的 `零`**,而「孤**零**零」含它。
**继 `#374b` 的 `race`(命中 `Totalsexyrace`)与 `smost`(命中 `bodypartsmost`)之后,第三次。**
> **修的是标记,不是测试。** 把测试改到能过,是把仪器的病写进结论。

## 对照
正对照:`#376` 抽到的 5 个数,**0 个**落进「孤立」· 负对照(修好后):合成句落进「孤立」· 规格曲线三个窗宽全跑。

## ⚠ 上界措辞(IMPOSSIBLE ②,跑前写下)
「窗内有 `±`」不等于那个 `±` 是**它的** -> **「可核而未核」是上界,「孤立」是下界。**

## 一句关于人的话
没有。**产出是一个关闭的决定:瓶颈在我的正则,不在页面的写法。**
