# R429 · 12 岁到的还是 20 岁到的,「越羞耻越少去碰」都一样成立

`#384b`:`羞耻 → ACTED` = −0.0833,−6.85 sd。**但方向不可识别。**
这份数据里有一个**时间上在前**的东西:起始年龄 —— **一个人 9 岁时的起始,不可能由他今天的实践量造成。**

## ⚠⚠ 这不是工具变量,而且我不会把它写成工具变量
`EARLY` 与羞耻**直接相关**(`#332`:−0.102),**排除性限制不成立**。
本轮做的是**部分定向**:那条关系在 `EARLY` 的分层内部是否仍成立、大小是否相近。
**成立且相近 -> 它不是「起始时间造出来的」。这仍然是关联,只是少了一个候选解释。**

## 四层(n≈1,680 / 层)
| 层 | 起始中位 | `羞耻 → ACTED` | se | |
|---|---:|---:|---:|:--|
| 1 | **12.5 岁** | **−0.0988** | 0.0243 | 越阈 |
| 2 | 14.7 岁 | **−0.0639** | 0.0246 | 越阈 |
| 3 | 16.7 岁 | **−0.0489** | 0.0245 | 越阈 |
| 4 | **19.9 岁** | **−0.1225** | 0.0245 | 越阈 |

层内零(**层内**打乱 `ACTED`)95 分位 = **0.0459** -> **4/4 越阈,全部同号。**
四层全距 **0.0736** vs **随机等大小分层**的零 0.0515 ± 0.0221(95 分位 **0.0902**)
-> **未越阈:四层读作同一个数。**

## ⚠ 第一版四个门里三个 FAIL,而它们是**同一个**原因
我写 `ACTED[rg.permutation(NN)]` —— **打乱了整个数组(含 NaN)**,掩码内出现 NaN,
阈变成 **NaN**,所有比较恒为 False,MDE 扫描于是 **0/30**。
> **一个 NaN 阈会让每一个门都「失败」,看起来像四个问题,其实是一个。**
修法:`perm_finite` 模式(**只在有限项内部打乱,保住缺失格局**),且**层内的零必须在层内打乱**。

## 对照
guard 22 四个不同的 x · **guard 26 用 MDE 扫描**(最小层 80% 检出 **0.08 ≤ 争议 0.0833**,`#384d` 的规矩)·
负对照(层内打乱)0.0216 ≤ 0.0459 · offset 零 ② 非退化。

## IMPOSSIBLE
① `EARLY` 与羞耻相关 -> 分层顺带压窄了层内羞耻的方差 -> **只比符号与越阈,不比与 −0.0833 的大小**;
② 四层各 ~1,680 人,功率低(已先算 MDE);
③ **「不是起始时间造出来的」≠「是羞耻造成的」** —— 只少了一个候选解释。

## 一句关于人的话
**无论他的性兴趣是 12 岁到的还是 20 岁到的,「越羞耻、越少去真的碰」这件事都一样成立,大小也一样。**
