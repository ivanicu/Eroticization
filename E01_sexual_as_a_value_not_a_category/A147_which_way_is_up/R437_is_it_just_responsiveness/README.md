# R437 · 不是「整体不容易被唤起」的影子 —— 控制掉它,系数几乎不动

`#392d` 的担心:五题与 `Totalsexacts` 相关 −0.40,**它可能只是「这个人整体上有多容易被唤起」取反**。

## ⚠ 先执行 `#392e` 的新规矩(三行,跑在建模之前)
`Totalsexacts`:取值集合(计数)· 众数 · **与方向已知的锚 `totalfetishcategory` 的相关 -> 同向,方向确认**。
**这一步就是上一轮缺的那一步。**

## 结果
| | |
|---|---|
| corr(五题, `Totalsexacts`) | **−0.4015** |
| 控制前 五题 | **+0.1259** (se 0.0126) |
| **控制后 五题** | **+0.1197** (se 0.0132) |
| **掉幅** | **+4.93%** |
| offset 地板(与五题相关度相同、但与 `BELIEF` 无关的合成控制) | −0.28% ± 4.13% · 95 分位 **+6.28%** |
| 控制后是否仍越阈 | **+0.1197 vs 0.0259 -> 是** |

**⇒ 世界 A:`Totalsexacts` 吃掉的部分连地板都没越过(+4.93% vs +6.28%)。**
**那 +0.126 不是整体唤起广度的影子。**
(同一模型里 `Totalsexacts` 自己只有 −0.0246,se 0.0161 —— 它自己就不预测这个信念。)

## ⚠ 而我又犯了 `#385c` 那个错
`NUL` 用了 `BELIEF[permutation(NN)]` —— **打乱整个含 NaN 的数组**,阈变成 **NaN**,
于是负对照与另判**同时**「失败」,而那是**一个**原因。
**`#385c` 就是为这个写的教训,我在写下它之后又犯了一次。**
修法:项目已有的 `perm_finite` 模式 —— **只在掩码内打乱**。

## 对照
guard 26(MDE 扫描 0.05 ≤ 争议 0.126)· 负对照(掩码内打乱 `BELIEF`)+0.0070 ≤ 0.0259 · 地板非退化。

## IMPOSSIBLE
① `Totalsexacts` 是**计数**,五题是**评分** -> 排除的是**一个特定操作化**,不是「唤起度」这个构念的全部;
② 两者相关 −0.40 -> 控制后系数天然变不稳,所以**掉幅对着地板读,不对着 0 读**。

## 一句关于人的话
**觉得「我能停下来」的,是那些**连普通的东西也不太打动他**的人 ——**
**而这不是因为他整体上认可的性行为更少(那一条已经控掉了,几乎没动)。**
