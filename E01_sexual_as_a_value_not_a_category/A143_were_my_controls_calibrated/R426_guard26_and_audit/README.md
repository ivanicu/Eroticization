# R426 · guard 26,和一次覆盖率 1.9% 的自我审计:4/4 的正对照都种在争议幅度之上

`#381c`:合成正对照种在 0.25,争议幅度 0.043 —— **5.9 倍**,轻松通过,**并且会让我报出一个零**。
`#379c` 刚证明**账本里的教训不等于手里的动作** -> **把它变成门。**

## ① guard 26 · `positive_control_at_the_contested_magnitude`
**它抓的不是「正对照失败」,而是「正对照**通过了**,但它种的幅度**大于**争议量的幅度」** ——
一个**看起来已验证、实际是沉默**的仪器。**checks fail toward PASS,而这是它穿着对照的外衣。**
**9 个攻击向量**:种在之下过 · 恰等于 · 种在之上过(必须 FAIL)· 没过 · 争议为 0 · NaN · None · 负号 · R425 实况。

## ② 回溯审计(**FRONTIER** —— 答案我事前不知道)
| 轮次 | 种植 | 争议 | 比值 |
|---|---:|---:|---:|
| `R425_S_by_sex` | 0.25 | 0.0334 | **7.5×** |
| `R415_wide_caliber_replication` | 0.12 | 0.0307 | **3.9×** |
| `R413_interaction_not_quintiles` | 0.15 | 0.05 | **3.0×** |
| `R414_within_person_order` | 0.12 | 0.05 | **2.4×** |

**4/4 都在争议幅度之上,中位 3.45×。**
⚠ **覆盖率 = 4/212 = 1.9%** —— **抽不到的 208 轮不是「没问题」,是「没看」。**
⚠ 而这 4 个全是 `R413`–`R425`,**是一个「我恰好把种植幅度写成字面量」的近期地层,不是一个样本。**

## ⚠ 第一版造出了一个 355× 的假指控
我用**观测到的效应**当「争议幅度」。**但一个零的争议幅度是**有意义的效应量**,不是那个≈0 的观测值。**
`R413` 是一个零:观测 −0.00042,有意义 0.05 -> 第一版算出 **355×**。
**修法:轮次若声明了 `meaningful=`,用它。** 修后 R413 是 3.0×。
> **一个审计工具的假阳性,和它审计的那类错,是同一个方向的错。**

## ③ ⇒ 逐例看缺口有没有被别的门补上(**不笼统指控**)
- **`R413`**(零):正对照 3.0× 偏大,**但它有 MDE 0.030 < 有意义 0.05** -> **MDE 做了正对照没做的事。零仍成立。**
- **`R414`/`R415`**(**非零**结果):正结果**不依赖**对照的灵敏度 -> 缺口影响小。
- **`R425`**(零):**真实数据上的参照当场拦住了它** -> 那一轮的 UNVERIFIED 是对的。

**⇒ 过度种植是系统性的(4/4),但在 4 例中的 3 例,另一个门补上了缺口。**
**风险是真的,而它已被别的机制部分吸收 —— 这是一句比「我的对照全坏了」更准确、也更难说的话。**

## 一句关于人的话
没有。**产出是一个门、一次 1.9% 覆盖率的自我审计,和一个被自己修掉的假指控。**
