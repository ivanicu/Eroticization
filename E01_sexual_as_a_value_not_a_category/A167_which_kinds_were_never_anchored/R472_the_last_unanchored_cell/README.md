# R472 · 同一个子串弱点,四次造成假指控,这次造成**假释放**

⚠ **FRONTIER**。`#427` 的 NEXT:`OTHER`,83 列,`#423a` 名单上最后一格。

## ① 边界先写,问题也换对
`OTHER` 按构造就是异质的 -> **「整族一个方向」是错的问题**。
对的问题:**83 列里有多少是**有序**的量、**用过**、而方向**从未检查**?**
不在范围内:无序类别(没有方向可言)· 方向由构造固定(计数、年龄)。

| 判据 | 剩下 |
|---|---:|
| `OTHER` 全部 | **83** |
| 有序(数值 ≥3 值,n≥200) | **14** |
| 项目里用过 | 22 |
| **在范围内(有序 且 用过)** | **8** |

## ★★ ② 而分类器把 8 判成 6,两个假释放,两个**独立**的缺陷
| 列 | 被什么放过 | 机制 |
|---|---|---|
| `bondageaverage` | `age` | **子串**:bond·**age**·aver·**age** |
| `neuroticismvariable` | `^n[A-Z_]` | ⚠ **`re.I` 把 `[A-Z_]` 悄悄变成 `[A-Za-z_]`** |

第二个值得单独记:那个模式是写来认 camelCase 前缀(`nCat`)的,
**加上 `re.I` 之后它匹配每一个以 n 开头的词**。
**⇒ 同一个子串弱点已经四次造成假指控(`#382c` `#394c` `#407a` `#422b`),这次造成假释放 ——**
**而假释放是永久的,因为没有人会去重审一个已经被放掉的东西(P6)。**

手判 8 列的值集后:**8/8 全部方向自由**(Big Five ×5 是对称的 −6..+6,
`powerlessness` −9..+9,`pornhabit` 0..9,`bondageaverage` 0..5)。

## ⚠⚠ ③ 而其中一部分**已经在公开页面上带符号**
- 「**less extraverted, more neurotic, less agency**」
- 「openness **+0.083** · extroversion **+0.088** · neuroticism **−0.064** · powerlessness **−0.059**」
- 「Openness(**+0.0593**)与 powerlessness(**−0.0441**)只锚住晚发家族」

**这些都是方向性读法,而变量的数值方向从未被检查。**
⚠ 两个计数锚在这里**没有功率**(|r| ≤ 0.083)-> **不能用 `#424b` 的办法标定** -> 见 NEXT。
`pornhabit` 例外:**+0.181 / +0.196**,两锚同号且够大 -> **方向确认(高 = 用得多)**。

## IMPOSSIBLE
① 「用过」用的是**列名点名**,漏掉经派生表使用的列(`#423b` 同一个坑);
② 手判是我一个人判的;③ 本轮**不判定页面上的符号对不对** —— 只判定它**没被检查过**。
