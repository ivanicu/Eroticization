# R467 · 一个没被用过的变量,方向不是安全的假设,是没被检查过的假设

⚠ **FRONTIER**。`#422` 的 NEXT:把「换成可判定的存在性检查」这一招用在**方向风险**上。

## ① 先把 9 个 `kind` 列全(可判定,不是普查)
`MULTISELECT` 101 · `OTHER` 83 · `RATING_0_5` 68 · `TOTAL` 37 · `AGE_ONSET` 32 ·
`LIKERT_PM3` 22 · `FORCED_CHOICE_MOST` 10 · **`RATING_BINNED_FIB` 7** · `RATING_NEG_FIB` 5

方向状态:`MULTISELECT`/`TOTAL`/`AGE_ONSET` **由构造固定** ·
`RATING_NEG_FIB` **`#392c` 标定(且当时是反的)** · `LIKERT_PM3` **`#419b` 标定** ·
`FORCED_CHOICE_MOST` 是类别、无方向 · **`RATING_BINNED_FIB` 从未标定**。

⚠ **而我第一个仪器是坏的,方向是「少算」**:用「代码里出现列名」判断「用过」,
对 `MULTISELECT` 算出 **0** —— 因为管线走 `endorsements_long.parquet` 而不是列名。
**六个坐标全建在它上面。** 换仪器后才看清。

## ★★ ② 两个斐波那契族**互为镜像**
`RATING_NEG_FIB` = `{0,−1,−2,−3,−5,−8}` · `RATING_BINNED_FIB` = `{0,1,2,3,5,8}` ——
**绝对值完全相同**。`#392c`:**−8 = 最色情**。

| 族 | 锚 | n | r | 零 sd | z |
|---|---|---:|---:|---:|---:|
| BINNED_FIB | 性行为计数 | 2,044 | **+0.1659** | 0.0223 | **+7.4** |
| BINNED_FIB | 恋物类别数 | 2,061 | **+0.1136** | 0.0233 | **+4.9** |
| NEG_FIB(**正对照**) | 性行为计数 | 15,253 | **−0.4502** | 0.0084 | −53.6 |
| NEG_FIB(**正对照**) | 恋物类别数 | 15,503 | **−0.1863** | 0.0082 | −22.8 |

**正对照精确复现 `#392c` 的 −0.4502** -> 锚在这个脚本里是活的。
⇒ **幅度即唤起,符号是装饰的。按原始数值直接用,会对同一件事给出相反的符号。**

## ⚠ ③ 而它**一处都没用过**(代码 0/7 · 页面 0/7)
**这正是它没被发现的原因。** 一个没被用过的变量不会报错、不会给出可疑的数,
它只是**在那里等着**。`#392` 花了四轮才发现方向反了;这一个连「被用」都还没发生。

## 一句关于人的话
这七题问的是**觉得自己的身体**色情。方向标定之后:
**与自己身体的色情关系,伴随的是更宽的性生活,不是更窄的**(+0.166 / +0.114)。
⚠ **范围**:只有 **2,061** 人答了这一块(13%,且题面在身体类型上筛选)。

## IMPOSSIBLE
① 只有两个锚,且都是计数 -> 标定的是「与整体性卷入同向」,不是「与唤起同向」;
② n=2,061 是一个**被题面选出来的**子样本,不能外推;
③ `RATING_0_5`(68 列)与 `OTHER`(83 列)本轮**未标定** -> 见 NEXT。
