# A27 · 去重删掉的是重复,还是内容

`#170c` 排出三层 —— 守卫没调用(`#156`/`#166c`)· 输出没人读(`#169c`)· 记了没人修(`#170a`)。
**第四层没查:修得对不对。** `#170a` 把 R06 正文段的数字删掉只留指针,
而没有任何检查能证明删掉的那些数与声明表里留下的是**同一批**。

**这个弧要让哪个决定变安全**:「一个事实一个家」这个动作,能不能在没有检查的情况下做。

| 子轮 | 目录 | 信念更新 |
|---|---|---|
| R216 | [`R216_numbers_that_left_the_page`](R216_numbers_that_left_the_page) | 不能:上一轮的去重把 `+0.2523` 整个删出了这一页,它在表里没有家 |
