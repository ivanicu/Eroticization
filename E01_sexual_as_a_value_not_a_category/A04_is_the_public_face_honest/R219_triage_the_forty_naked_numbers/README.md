# R219 · 那 40 个裸数,缺的是证据还是搬运

逐行判每个裸数在账本里有没有一个**带限定语的家**:

| 类 | 行数 | 含义 |
|---|---:|---|
| **① 搬运缺口** | **24** | 账本里有限定语,README 没搬过来 —— **可直接补** |
| ② 证据缺口 | 12 | 账本里也没有(`#168` 那 96 条的子集) |
| ③ 不需要限定语 | 0 | **构造性的空,不是测量结果** —— 见下 |

**`①/(①+②) = 67%`。公开面缺的主要不是证据,是搬运。**

## 三个对照

- **正对照**(注入):`±0.0164` —— `#167` 正文里同一行带限定语 → 判进 **①** ✅
- **负对照**(注入):`+9.9991` —— 账本里不存在 → 判进 **②** ✅
- **③ 类的 0 是构造性的**:`_MAGNUM` 只抓带符号小数 · 百分比 · 比值,**计数与版本号本来就进不来**,
  所以这一类在本设计里恒为空。**不得读作「没有不需要限定语的数」。**

⚠ 第一版的正对照要求 `+0.432` 判进 ①,而它**根本不在裸数行里**(README 上它已带 ±)——
条件与它自己的说明文字自相矛盾,是 `#139` 那一类的装饰。已换成注入式。

## 产出:搬运清单 + 两处已落地

`results/transport_list.csv`,37 个 token 配好账本里那句限定语。
**清单是候选,不是判决** —— 按**串**匹配不按**所指**(`#170b` 的见证):
`10%` 被配到了一张无关表格的 `±10%`。

已落地两处(中英各一):

    改前:真实曲率 **0.3×**,推到 2× 需约 48 倍样本
    改后:真实曲率是 **0.3× 自身展布** —— 它**根本没通过可分辨性**,
          所以这是**能力边界,不是零**;推到 2× 需约 48 倍样本

**`0.3×` 原来没说「相对什么的 0.3 倍」** —— 而账本里那句话说的是它**根本没进可分辨区**。
一个读者看到 "0.3×" 会读成"效应很小";看到"0.3× 自身展布"才会读成"这台仪器答不了"。

改后跑 `tools/readme_gate.py`:`numbers_that_left = 0` —— 这次搬运没弄丢内容。

## 结构上做不到

"带限定语的家"按**同一行**判。账本里限定语写在下一段的,这条规则看不见 → 会**高估 ②**。
