# A23 · 那些「去掉 X 之后还保留 Y%」的百分比,经不经得起泄漏校正

`#165` 发现:回归掉一个**有噪声**的协变量会留下衰减残留,而下游的读数会把残留当成信号。
本项目至少有三处「保留率」用的是同一个动作 —— `#104` 去掉勾选数(67%)、`#164` 去掉性别(102%)、
`#165` 去掉位置。**只有第三处量过它的泄漏底噪。** 而 `#164` 的 102% 现在挂在两份 README 上。

**这个弧要让哪个决定变安全**:那两条声明是留在 README 上,还是降级。

| 子轮 | 目录 | 信念更新 |
|---|---|---|
| R211 | [`R211_does_leakage_need_a_noisy_covariate`](R211_does_leakage_need_a_noisy_covariate) | 泄漏是**泛函不匹配**不是噪声;平均不引入(±0.046),单次担保不了(±0.116) |
