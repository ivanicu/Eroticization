# R515 · 塌陷是噪声,而这一问到此为止(第三轮,按规矩换方向)

⚠ **FRONTIER**。`#470` 的 NEXT:k=12 的塌陷,是某一块进来造成的,还是随机游走?
⚠ **跑之前就写明:这是同一问题第三轮,若判 RANDOM_WALK 就换方向,不开第四轮(frontier §3)。**

## ① 逐步扫描,点名新进入的块
| k | n | 差 | 新进「罕见」组 | 新进「常见」组 |
|---:|---:|---:|---|---|
| 10 | 3,699 | +0.0581 | — | — |
| 11 | 4,164 | +0.0435 | nonconsent scenarios | roles are erotic |
| **12** | 5,140 | **+0.0202** | *most erotic*(强制选择) | erotic, in the context of… |
| 13 | 5,400 | +0.0536 | types of sadomasochism | For saliva… |
| 14 | 5,890 | +0.0610 | (vq…) | (88…) |

**相邻的 k=11 与 k=13 都回到 +0.044 / +0.054** -> 不是「进来一块就一直低」。

## ★★ ② k=12 上留一块(24 块逐个抽掉),多重性用**最大值零**(`#440b`)
**基线 +0.0202** · **最大位移 0.0312**(抽掉「bodily secretions」)·
**最大值零的 95 分位 0.0741** -> **落在零里。**

**判决 = RANDOM_WALK。** 没有任何单独一块解释得了这个塌陷。

## ③ 于是按预注册规则收手
`#470b` 的「大小估不到比四倍更细」**已经涵盖它**。**换方向,不开第四轮。**

## ⚠ ④ 副产物:第五次,于是它变成了接口
`L.shift` 又取到 pandas 的方法(`dtype` `#429b` · `b.round` `#431e` · `r.sub` `#453d` ·
`mid.diff` `#469a` · 本轮)。**五次就不是失手,是一个类。**
-> `lib/bounded.show()` 现在**每次都检查列名是否撞 DataFrame 属性并喊出来**;
正对照(`shift`/`diff` 被喊)与负对照(普通列名不喊)都在。

## 一句关于人的话
没有 —— **产出是一个被自己的零挡下来的解释,和一条从「要记住」搬进「有接口」的教训(本会话第六次)。**

## IMPOSSIBLE
① 零只有 200 次自助 × 12 块的子集;② 「随机游走」= 在这个分辨率下无法归因,不是证明无因;
③ 逐步扫描的块名是描述,**不是构念命名**(`R372` 的规矩)。
