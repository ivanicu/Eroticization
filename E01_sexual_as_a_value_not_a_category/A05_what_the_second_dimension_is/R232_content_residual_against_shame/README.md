# R232 · 内容残差,对上一个外部结局

| 项 | r(·, 羞耻) | sd | 比 |
|---|---:|---:|---:|
| **S(复现 `#179`)** | **+0.1155** | 0.0117 | **9.9** |
| 内容残差 raw | −0.0230 | 0.0127 | 1.8 |
| 内容残差 +S | +0.0029 | 0.0131 | 0.2 |
| 正对照:把 S 当残差塞进来 | +0.1155 | 0.0130 | 8.9 |

**可判前提通过**:S↔羞耻 在同一批人上复现 `#179` 的 `+0.1155`,**逐位相同** ——
管道没变,所以内容残差那一格可读。

**跨人打乱羞耻:内容残差 +0.0121 —— 是它 raw 值的 53%。**

## 判定:NOISE

    raw 1.8×(不可分辨)· 控制 S 后 0.2×  ->  NOISE

**`#165` 的那个残差,对一个与它零 item 重叠的外部结局毫无预测,
而同一条管道在 S 上读到 9.9×。** 那条悬了 22 轮的 UNVERIFIED,现在降为 **NULL**。

`#129` 守卫已跑:残差与 S 的相关在构造上为 0(`check_residualized` 通过)。

## 结构上做不到

内容分数由**跨人主成分**定义(`#165` 的 IMPOSSIBLE 原话)——
**完全特异的组合对它不可见**,所以这个 NULL 只覆盖**共享的**内容维度,
不覆盖"只有他一个人这么勾"的那一部分。
