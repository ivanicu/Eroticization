# R305 · c3 里扣掉 D 之后,还剩什么

`#259a`:c3 与 D 的剖面差 14% 可分辨,**但没说差在哪里**。

## ① c3 比 D 多说的
| 结局 | c3 | D | 差 |
|---|---:|---:|---:|
| `age` | −0.097 | −0.001 | **+0.096** |
| `I find it erotic when two people of the opp…` | −0.116 | −0.023 | +0.093 |
| `I find the thought of masturbating alone as…` | −0.079 | −0.021 | +0.057 |
| **`written`** | **+0.112** | +0.056 | +0.056 |
| `If my partner is aroused by something, I ca…` | −0.097 | −0.043 | +0.054 |

反向(D 比 c3 多说的)最大只有 −0.038。

## ② `c3⊥D`:把 D 整个扣掉之后,c3 几乎没损失
**越阈 15.5 ± 0.5(种子)/ ±0.2(阈值) → 区间 15–17**,而 c3 自己是 18。
正对照完美:把 D 从**它自己的带噪声复制**里回归掉,残差越阈 **0.0 ± 0.0**。

`c3⊥D` 最强的几格:**`animated` +0.223** · `I am aroused by bei…` −0.136 ·
**`written` +0.128** · **羞耻 +0.124** · `I find it erotic wh…` −0.116。

> **c3 不是「更灵敏的 D」。把 D 整个减掉之后,它保住了几乎全部的射程 ——
> 总表两行保持,而 c3 携带着 D 完全没有的东西。**

## ⚠ 而那个东西有一个很像名字的形状,我这次不给它命名
残差最强的两格是 **`animated` 与 `written`** —— 正是 `#201` 试图命名为「媒介偏好」
而**被数据杀掉**的那一对(`corr(animated, written) = +0.2507`,它们不是一条轴的两端)。
`#201` 当时的结论是:这个维度预测的是两者**共有**的东西,而那个东西**尚未命名也未检验**。
**本轮把同一个东西又推到了台前,但这次它更干净(D 已被扣掉)。**
⚠ **不命名** —— `#201`/`#202` 两次的教训是:**名字必须先写成能被杀死的预测,再去测。**

## 一句关于人的话
「你把开放花在哪儿」还不是全部。
扣掉它之后,c3 剩下的那部分**最强地指着「画的」和「写的」** ——
也就是**不是真人的那些**。而那个东西这个项目试过两次命名,两次都被数据拒绝。
