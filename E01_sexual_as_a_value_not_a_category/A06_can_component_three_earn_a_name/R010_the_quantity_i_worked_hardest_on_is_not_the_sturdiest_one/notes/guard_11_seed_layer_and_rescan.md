# R301 · 守卫 11 的种子层硬规则,并回头扫

**类型:CLOSURE**。

`#255c`:`#233` 只量了**阈值重抽样**那一层,而 `#300` 实测**数据劈分种子**那一层
可以比它大 40%(±2.5 vs ±1.8),并且正是它让 `#254a` 得出了一个错的结论。

## ⚠ 我把注册的「降级为 WARN」改成了 `FAIL`
**严于注册,方向安全**:一个未被测量的**主导**不确定性来源,等于**精度未知**,不是精度尚可。

新规则:`spread_source == 'threshold_resample_阈值重抽样'` 且未给 `seed_spread` → **FAIL**;
两层都给 → 放行,并按**较大**的那层给区间。

## 四端自检(独立 `Gate` 实例,`#249c`)—— 全对,且新规则没有吃掉旧规则
| | 情形 | 期望 | 实测 |
|---|---|---|---|
| ① | 只给阈值层 | **FAIL**(新) | FAIL |
| ② | 两层都给 | 放行,按较大的给区间 | PASS |
| ③ | 来源是零臂 | 仍 FAIL(旧) | FAIL |
| ④ | 展布为 0 且重抽 ≥10 | 仍「钉死」放行(旧) | PASS |

## 普查:293 个 `run.py`,只给阈值层的 **4 处**(+1 处是我自检用例的自指)
| 轮次 | 状态 |
|---|---|
| `R278`(`#233`) | **不是错**:那一轮的 estimand 本来就是阈值层,只是**不完整** |
| `R295`(`#250`) | rate 的 6/31 —— 待重报 |
| **`R297`(`#252`)** | 三个成分控制后的 8–10 / 6 / 20 —— **正挂在公开页总表上,最该先重报** |
| `R299`(`#254`) | 已由 `#255` 撤回并重报(3–13) |

## 一句关于人的话(Closure 轮,没有)
产物是一条**结构上拒绝低估精度**的规则,和一张 4 行的重报清单 ——
其中一行的数字此刻正印在公开页上。
