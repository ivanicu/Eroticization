# R296 · 人层维度总表

**类型:PRODUCTION**(`§0` 三类动作)。`§0.2`:不能只交账本。
这一轮不产生新数字,产物是**一张能被没读过账本的人直接使用的表**。

## 门:每一个数字都必须能在账本原文里逐字找到
脚本逐格 grep `RETRACTIONS.md`。**命中率 100.0%,未命中 0 项**;
第一版的分词把「≥2,起始仪器」当成一个数去 grep(那是短语不是数),已收紧为只取数值 token。
**8 个格子是 `UNCOMPUTED`** —— 不留空、不从记忆写,它们是这张表诚实的部分。

表见 `results/dimension_table.csv`,并已进入两个 README。

## 表本身说的一件事
**先读信度那一列。** 三个宽度类型(0.56–0.72)是这份数据里**最可靠**的人层量,
而它们是**最后才被找到的**(`#230`,第 230 条);
承载这个项目最老、被引用最多那条主张的**位置分(0.432)**,
是仍然站着的里面**最不可靠**的一个。

## 一句关于人的话(Production 轮)
把八个维度并排放在一张表上,第一次看得出来:
**这个项目花最多力气去证明的那个量,不是它手里最结实的那个。**
