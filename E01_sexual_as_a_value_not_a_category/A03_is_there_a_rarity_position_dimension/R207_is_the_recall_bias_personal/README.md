# R207 · `#114` 的回忆偏差,是人群的还是个人的?

`#160c`/`#161b`:性偏好的「什么」是个人的(0.62),「何时」不是(贴合 0.16 · 偏离 0.11)。
那 `#114`(**人把最爱的性兴趣记得更早,约九个月**,19.8×)必须被重新定位。

`b_i` = 这个人**人内**的「起始年龄残差 ~ 该类别自己的评分」斜率;
量它的分半信度,放进 `#160`/`#161` 的**同一台机器**。

## 结果

| 每半单位数 k | n | 分半 r | **SB 信度** | 人内置换零(5 抽) | 人特异种植 |
|---:|---:|---:|---:|---:|---:|
| 4 | 4,467 | +0.0334 | +0.0646 | −0.0060 ± 0.031 | **0.4121** |
| 5 | 3,086 | +0.0186 | +0.0365 | +0.0265 ± 0.071 | **0.4347** |
| 6 | 1,823 | +0.0317 | +0.0614 | −0.0162 ± 0.073 | **0.5471** |

人群层斜率 **−0.1975**(复现 `#114` 的 −0.2000)。

同一台机器上的参照:**S(喜欢什么)0.62 · a(时间表贴合)0.16 · z(偏离多远)0.11 · b 0.054。**

## 而 `#153` 的规则反过来抓到了本轮最硬的一句

**零的实现 sd ÷ 效应 = 1.1×** —— **效应(0.054)比它自己零的实现展布(0.058)还小。**

> **所以「b = 0.054」不能当一个数来引用。可以说的是:它在它自己零的噪声之内。**

而**种植臂开到 0.41–0.55**,所以仪器**测得到**人层的回忆偏差 —— **这是一个真的缺席,不是没检出力。**

## 判定

**POPULATION。**

> **「某些人比别人更会把最爱的记早」这句话不成立。回忆偏差是所有人共有的。**

而 `b` 与 `z`(偏离多远)的等价第一次**真的过了预设边界**:
|差| + 2sd = **0.0906 < 0.10**。两者是同一个量级的「无」。
`b` 与 `S`(喜欢什么)差 **36.9×**。

## 我在这一轮里犯了自己上一轮写下的错

第一版每个 k 只抽**一次**置换零,k=5 那一抽给 **+0.106**(而 k=4/6 给 −0.03/−0.05),
门当场 FAIL。**这正是 `#153c` 的规则** ——「单次抽样的零不自动致命,伤害 = 零的实现 sd ÷ 效应」——
**我写下它一轮之后,自己踩了它。** 改成 5 抽后零回到 ±0.03,而那个比值 **1.1×** 是本项目至今最大的。
