# R04 · 66.852% 是关于人的,还是关于我选了哪个汇总量的?

**【CLOSURE,明确标注】** 不开新战线。保护本项目最老也最大的一个数字。

`#130e` 发现同一批 31 个题目上,Spearman(稀有度, 起始年龄) 在**均值**上是 +0.011(p=0.954)、
在**中位数**上是 +0.437(p=0.014)。而 `#75` 的时间表(66.852%)与 `#63` 的上界都是从
`np.nanmean(V[tr],axis=0)` 排出来的。

## 结果

| 汇总量 | 跳过并列(原做法) | 打分对数 | **同一批对** | 种子 sd | vs mean | 类别并列 |
|---|---:|---:|---:|---:|---:|---:|
| **mean** | 66.699 | 35,486 | **66.699** | 0.298 | +0.000 | 0 |
| median | 76.233 | 18,001 | **63.298** | 0.366 | −3.401 | 580 |
| q25 | 76.511 | 16,708 | **62.481** | 0.587 | −4.218 | 618 |
| q40 | 74.098 | 18,614 | 62.635 | 0.278 | −4.064 | 473 |
| q60 | 71.200 | 22,162 | 63.240 | 0.278 | −3.459 | 426 |
| q75 | 71.372 | 23,634 | 64.203 | 0.145 | −2.496 | 430 |
| trimmed20 | 66.782 | 35,486 | **66.782** | 0.308 | +0.083 | 0 |
| random | 47.590 | | 47.590 | 4.300 | | 0 |

**`#75` 站住。** 在同一批对上,均值是最好的汇总量;唯一能与它打平的 `trimmed20`(它与均值的
顺序 Spearman = 0.993)只高 **+0.083 个百分点 = 0.3× 种子展布**。

## 这一轮真正的产出

**跳过排序量并列的对,值 +12.9 个百分点。**

`median` 跳过并列时 76.233%,在同一批对上 63.298%。分位数汇总把 31 个类别里 580 个有序对
压成并列,于是它**只在一半的对上打分** —— 而剩下的那一半正是起始年龄差距最大、最容易的那些。

> **跳过模型排不出来的对,是弃权,而弃权调整后的准确率不是准确率。**
> 这是 `#101b same_scale` 在排序任务上的形态,而 `same_scale` 必须是**第一个**门。

## 它回过头来打了 `#130f`

`#130f` 用 Spearman(稀有度, 起始年龄中位数) = +0.437 修正了 `#128` 的一句话,写成
「人群时间表**确实**按常见→罕见排列」。

**这一句要降级。** 中位数排出的顺序**确实**与稀有度对齐(+0.437),但它是一个**更差的时间表**
—— 在留出的个人成对顺序上 63.30% vs 均值的 66.70%。所以「人群时间表按常见→罕见排列」是
**那个输给均值的汇总量**的性质,而表现最好的时间表(均值)与稀有度无关(+0.011)。

`#130a` 的 Δ = −0.2345 作为**测量**不变(49×,种植对照,人内零),但它的**机制**回到 UNVERIFIED:
我的左尾解释在 `#130d` 已死,中位数解释在这里被削弱。
