# R497 · 引用审计这件事本身,不可机械化 —— 而唯一真的缺陷是读出来的

⚠ **CLOSURE**。`#452` 的 NEXT:页面上每一个 `#NNN`,指向的条目与它被引用的语境相符吗?

## ① 可判定的部分
| | |
|---|---:|
| 台账可解析标题的条目 | **436**(修正解析器后更多) |
| 页面上的 `#NNN` 引用 | **631**(不同条目 **216**) |
| **悬空指针** | **0** |

## ⚠⚠ ② 而「4 处悬空」是我自己造的第七次过度指控
第一版正则只认 `## Entry NNN` 一种表头。**读文件才看见它漏掉的两种**:
- **`#1`–`#14` 在文件开头的一张表格里**(`| 11 | … |`);
- **`#15`–`#16` 共用一个合并表头**(`## Entries 15–16`)。

**是读对象抓住的,不是我写的任何检查抓住的。**

## ③ 排序判据**没有判别力**,如实报出
- 英文页:语境是英文、标题是中文 -> **重叠必然为 0**,与对错无关;
- 中文页:重叠最低的 10 条**全部正确** —— 它们都落在**章节标题**处,那里本来就不重复标题的词。

**⇒ 「页面上没有别的错引用」判 UNVERIFIED,不是无罪。**
而 `#452a` 那一条真的错引(`#289`),是**读出来的**,不是任何自动判据找到的。

## 一句关于人的话(方法轮)
**引用是否正确,属于 `#431b` 那个「不可机械检查」的类 ——**
**而这一类正是会复发的那一类,所以它值得被写下来,而不是被假装解决。**

## IMPOSSIBLE
① 排序判据无判别力 -> 只做了可判定的那一半;② 手判是我一个人做的;
③ 631 条引用**没有逐条手读**,只读了排序最靠前的 22 条。
