# R05 · 最后一个不带出处的数字,挂在一句账本说是假的话上

`#144` 的 NEXT:把前页那 **14 个不带出处的数字**逐个接上账本或删掉,完成条件是
`uncited_numbers()` 的输出降到只剩 DOI 与版本号。

## 先撤回 `#144f` 自己:那 14 个是我的粒度伪影

`uncited_numbers()` 第一版**按行**判,而引用往往在同一**段**的别处。改成按段:

| 粒度 | 带数字却不带出处 |
|---|---|
| 按行(`#144f`) | **17 行** |
| **按段(正确)** | **2 段**,其中一段是 `python 3.14` |

**盲区被高估了 14 倍。检查的单位,必须与"出处"的单位一致。**

## 而那唯一真的一个,比 14 个加起来更值

剩下的那一段是覆盖度定律:

> 任何在这个 release 上的分组比较,若没有按**块数**匹配,有一大半在测**问卷覆盖度**
> 而不是性癖差异(`corr = +0.815`,9 次切分)。~~我没在任何已发表分析里见过有人提这一条。~~

**最后那句话,`Entry 15` 明说是假的:**

> 数据发布者自己的 `analysis/swarm/14-missingness.md`,日期**早五个月**,写着
> *"if missingness differs systematically by gender, orientation, or politics, then group
> comparisons on gated columns are comparing different subpopulations"*,附带按性别/取向/政治的
> 逐列缺失率、应答者与非应答者在 18 列里 17 列 Cohen's d > 0.7,以及
> *"any 'gender difference' in these kinks is partially an artifact of who answered"*。

**而这句假话在两套叙述里各有一份**(中文 段 18、英文 行 123),两处都在。都已改。

## 剩下的是真的,而且很大

| | 值 |
|---|---:|
| corr(子空间一致性缺口, 组间覆盖度缺口) | **+0.815**(9 次切分) |
| pornhabit:原始 → 块数匹配 → 再匹配 θ 与性别 | **0.2285 → 0.0871 → 0.0439** |
| 与性别的排序 | **反转**(性别可比测量下是 0.0778) |

> **危害是他们记录的;把它量化并因此翻掉一条自己的结论,是我做的。**
> 「看多少色情重组情欲语法比性别多 2.4 倍」正是死在这条上(`#11`)。

## 前页不变量现在是绿的

`readme_ledger_audit.py` 三条规则:**撤回语境**(29 命中,已分诊)· **内部一致**(5 命中,
均为英文正文是中文表的子集)· **无出处**(现在只剩 `python 3.14`)。

## 两轮下来的图样

`#144b` 是一个**虚构的数**,`#145b` 是一句**虚构的优先权**。两者都在前页,都被账本杀过,
都活了下来。**前页最可引用的那些句子,恰恰是账本杀掉的那些** —— 因为可引用的句子是为效果写的。
