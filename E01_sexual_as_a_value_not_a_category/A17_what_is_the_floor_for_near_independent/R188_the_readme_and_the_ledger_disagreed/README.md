# R03 · README 和账本,已经互相矛盾了 118 条

`#142` 的 NEXT 是给另外四处写死的常数各量一次距离。量到 `A02/R040`(`mx<50`)时看到它的
结果是 **101% / 86%**,而它的预注册判定写着 `mx>=80 → ONE CONSTRUCT, three-axis claim reduced`
—— 那条分支才是会开火的那条。

## 但那不是发现,那是我上一轮的错误

顺着往下读:`R18` 已经诊断出 `R16`/`R17` 的仪器**在构造上就欠功效**
(14 变量的 profile-profile 相关,零分布 sd ≈ 0.30),那条线被**冻结**并写下了解冻条件。
然后 `Entry 24`(`A02·R19`)用一把验证过的信度阶梯把它settled:

| | r_obs(低→高信度) | r_true |
|---|---|---:|
| SUBSTANCE 对自身(正对照) | 0.393 → 0.516 → 0.516 | **+1.018** |
| **POWER–SUBSTANCE** | 0.298 → 0.341 → 0.381 | **+0.605** |
| POWER 对 sham(非角色) | 0.002 → 0.026 | +0.023 |

**`Entry 24` 明确写着:「2.95 of 3」withdrawn;POWER 与 SUBSTANCE 共享 37% 方差,不是 5%;
预注册判定落在中带 → UNVERIFIED。**

**而 README 从 `Entry 24` 起就没被改过 —— 它带着「去衰减 ≤ 0.362,有效维度 2.95/3」
又走了 118 条。**

## 所以 `#141` 整条撤回

`#141` 花了一整轮"重新定价"这条声明:重跑 `A02/R034`,自助它的去衰减值,发现 30.2% 跨过
写死的 0.4,然后**把「有效维度 2.95/3」写回 README**,并推荐改引**原始**相关(≤0.112)。

三处都错:

1. **那条声明 118 条之前就被 `Entry 24` 杀掉了**,我审计的是一个已经作废的版本。
2. **我把已撤回的「2.95/3」写回了前页。**
3. **"改引原始相关"的方向是反的** —— 原始相关正是**被衰减压平**的那个;
   `Entry 24` 的全部要点就是一把**验证过的**去衰减给出 +0.605。

> **我用重跑那一轮的方式去审计一条声明,而没有读那条声明后来的账本条目。**
> **一个轮次自己的输出不是那条声明的当前状态;账本才是。**

## 产出:两个工具

- **`tools/readme_ledger_audit.py`** —— README 上的每个可辨识数字,是否出现在账本的
  撤回语境里。127 个数字里 **29 个命中**(大多良性:那个数是撤回行里的**更正值**)。
  输出是**待读清单,不是判决**(`P6` 安全侧)。
- **`lib/gates.py::threshold_outside_noise`** —— 第八个守卫,`#142` 的 NEXT。
  预注册阈值必须离开被比较量**自身的噪声带**。回放:`#118` 的 `r250=3.0484` vs 写死的 `3`,
  噪声 0.035 → **1.4×,FAIL**;一个 3.60 的例子 → 17.1×,PASS。
