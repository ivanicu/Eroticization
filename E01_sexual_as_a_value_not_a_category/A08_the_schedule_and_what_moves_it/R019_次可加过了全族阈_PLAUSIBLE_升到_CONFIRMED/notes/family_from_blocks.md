# R433 · 用问卷自己的块结构重定义家族 —— **变号**,`#388a` 的家族读法退回

`#388c` 已经写着:「家族是**看过头名之后**定义的,+0.1159 是**描述**,不是检验。」
**本轮做那个欠下的复制,而它失败了 —— 并且是变号。**

## 结果
| 家族的定义方式 | r(与 `BELIEF` 残差) |
|---|---:|
| **我手挑的五题**(`#388a`) | **+0.1159** |
| **问卷自己的块结构**(`#327` 用块载荷定出的 `c3` 普通端:`sex acts` · `sensations`) | **−0.0259** |

单变量零 0.0101 ± 0.0075(95 分位 **0.0253**)· 阳性参照 `ACTED` **0.0382 开火** ·
负对照(纯噪声)0.0186 未越阈 · guard 26 MDE 0.05 ≤ 争议 0.116。**注册的 kill 反向开火。**

## ⇒ 退回什么,保留什么
- **退回**:`#388a` 的**家族读法**(+0.1159)。它本来就标着「描述,不是检验」,**现在它连描述都不稳。**
- **保留**:预注册的量 —— **扫描的最大值 +0.0888 vs 族内取最大零 0.0419**。
  **那一部分从不依赖我的选择**,不受本轮影响。

## ⚠ 但两种定义**测的不是同一个东西** —— 这是限制,**不是救援**
我手挑的五题是**「常规性行为有多色」的评分**;块版本是**那两块里勾了多少项**(一个计数)。
块计数与总类别数相关 **+0.3412**、与 `S` 相关 **+0.2687** —— **它掺着广度。**
> **这句话是本轮的限制,不是把结论救回来的理由。**
> 注册的 kill 开火了 -> **claim 下架**。「两种操作化不同」变成**下一个问题**,不是**这一个的辩护**。
> (看过结果之后重新解释操作化,正是预注册要防的事。)

## IMPOSSIBLE
① `#327` 的普通端是在**同一份数据**上定的 -> 这是**不同仪器**的复制,**不是独立样本**的复制;
② 块计数与总勾选强相关(已同轮报出);③ 两块只是普通端的**两个**,不是「普通」的全部。

## 一句关于人的话
没有 —— **上一轮那句「他们有地方可以退」暂时收回。**
**它在我自己挑的题上成立,在问卷自己的分组上变号,而我没有第三种分组去裁决。**
