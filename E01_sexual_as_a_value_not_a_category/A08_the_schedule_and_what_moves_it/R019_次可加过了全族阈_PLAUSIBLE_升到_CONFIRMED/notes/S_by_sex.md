# R425 · `S` 那条路上有没有两性差,这份数据答不了 —— 而拦住我的是一个**真实数据上的**正对照

`#296` 的性别非不变性是页面上**唯一停在 D5** 的结论,现在被夹在两个已知之间:
`#378a` 它**不在** `c3⁻` 那条路上(|t| 0.10)· `#380a` 它**在** `EARLY` 那条路上(|t| 3.69)。
**-> 那 `S` 那条呢?**

## 三条路,窄口径(`S` 需 ≥8 块),n=6,717,结局 = 组内秩变换的羞耻
| 路 | 组 0 | 组 1 | 差 | \|t\| | 角色 |
|---|---:|---:|---:|---:|---|
| **`S` 位置** | +0.0771 | +0.1105 | +0.0334 | **1.160** | ← 本轮的问题 |
| `c3⁻` | +0.1107 | +0.1063 | −0.0044 | **0.178** | ← **阴性参照**(`#378a`:应当无差)✅ |
| `EARLY` | −0.0952 | −0.0525 | +0.0426 | **1.734** | ← **阳性参照**(`#380a`:应当有差)❌ |

族内阈(随机等大小劈分 400 次的 95 分位)= **2.382**。

## ★★ 阳性参照失败了 —— 而这正是正确答案
`EARLY` 是**已知有差**的(`#380a`,宽口径 |t| 3.69,三个摘要一致)。
**在这个窄口径配置下,它只有 |t| 1.734,过不了线。**
**⇒ 这个仪器测不到它已知拥有的东西,所以它对 `S` 的沉默什么也不说。UNVERIFIED。**

## ★★★ 而合成正对照**会**通过 —— 这是本轮真正的产出
| 正对照 | 幅度来自 | \|t\| | |
|---|---|---:|:--|
| **合成**(种植 0.25) | **我随手选的方便幅度** | **6.816** | **通过** |
| **真实**(`EARLY`) | **数据决定的真实幅度** | **1.734** | **不过** |

**⇒ 合成对照会让我报「`S` 没有两性差」。真实对照拦住了它。**
> **一个种在方便幅度上的合成正对照,只说明仪器在**那个**幅度上工作,不说明它在**真实**幅度上工作。**
> `P5★` 说「一个从未返回过非零的仪器给出的 0 是沉默」。**这一轮加一句:**
> **一个只在比争议幅度**更大**的幅度上返回非零的仪器,同样是沉默。**

## ⇒ 一条可命名的结构性限制
`S` 的两性差要判,需要**宽口径的功率**;而 `S` 的定义**需要 ≥8 个块**。
**两个要求互相排斥 —— 和 `#379b` 是同一堵墙。**
**⇒ 「`S` 的斜率对两性是否相同」在这份数据上,用这套方法,答不了。** 不是没做,是做不到。
(MDE(S)= **0.12**,而有意义的差是 0.05 —— 即使抛开参照,这个零也不可发布。)

## IMPOSSIBLE(跑前写下)
① 窄口径功率低;② 族内阈对三条路共用,**保守**不精确;
③ 「`S` 无差」若成立也只说**斜率**一样,**不说分布一样**(`#293` 已证明分布不一样)。

## 一句关于人的话
没有。**本轮的产出是一堵被命名的墙,和一条关于对照的规矩。**
