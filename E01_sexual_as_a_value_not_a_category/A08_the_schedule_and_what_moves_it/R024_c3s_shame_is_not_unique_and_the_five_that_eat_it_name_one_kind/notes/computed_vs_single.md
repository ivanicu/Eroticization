# R452 · 我那个读法不成立 —— 而这一轮的价值就是拦住它

`#406a`:`S` 在 29 个结局里 **8 个反号**,而最负的三个是 `highenergy` · **宜人性** · **尽责性**。
**读起来像「关于自己性的陈述 vs 一般人格」—— 但那是我读出来的。**

## ⚠ 分组由**卡片自己的** `COMPUTED COLUMNS` 一节给出,不由我给出
卡片逐条点名:`opennessvariable` · `consciensiousnessvariable` · `extroversionvariable` ·
`neuroticismvariable` · `agreeablenessvariable` · `powerlessnessvariable` · `totalfetishcategory` · `bondageaverage`。
**⇒ 29 个结局里:合成量 6 · 单题 23。**

## 结果:世界 B —— **分类解释不了它**
| | |
|---|---|
| 分组后加权一致性 | **0.7241** |
| offset 零(**随机等大小分组** 2000 次) | **0.7264 ± 0.0132** · 95 分位 **0.7241** |
| | **−0.17 sd —— 落在零的正中央** |
| 合成量组自己 | **0.5000**(6 个里 3 正)—— **一个硬币** |
| 单题组 | 0.7826(23 个里 18 正) |

⚠ **零的种类**:`offset_control` —— **任意分组都会把组内一致性抬高**(同号的碰巧分到一起也会)。

## ⚠ 而这个零**也不可发布**
guard 26:**MDE = 60%**,而有意义的分组效应是 **50%** —— **1.2× 偏大**。
**⇒ 既不能说「卡片的分类解释了它」,也不能说「它解释不了」。**

## ⇒ 但**能**说的那一句才是本轮的价值
**我那个读法(「内省 vs 人格」)没有得到支持,所以它不能上页面。**
> **一个读起来很顺的解释,和一个被测过的解释,在页面上占同一个位置 ——
> 而只有一个应该被放上去。这一轮拦住的就是那个差别。**
⚠ 合成量组的 0.500 看着像「人格变量的符号是随机的」,但 **6 个里 3 正本来就是最可能的结果** ——
**那个数几乎不含信息,不能当描述用。**

## ★ guard 26 的新接口第一次真用,而且两条都生效了
本轮**显式声明** `main_quantity='discrete_count'` + `sweep_detection` ->
它先判**没有饱和**(扫描 33% / 63% / 93% / 100%),**再**应用普通的幅度规则并 FAIL。
**⇒ 两条判据各自独立地起了作用**(`#407c` 的设计意图)。

## 对照
负对照**越阈率 3.8%** ✅ · offset 零非退化 ✅ · guard 26 **FAIL(如实记)**。

## IMPOSSIBLE
① 合成量组只有 6 个 -> 一致性在小组上天然更高,**而随机零用同样的组大小,已吸收这一点**;
② 卡片描述的不是这个文件(`#373`),但**合成/删除清单那部分可信**(`#374`);
③ 「分类解释符号」不等于「符号有心理学含义」。

## 一句关于人的话
没有。**产出是:一个很顺的解释被拦在了页面外面。**
