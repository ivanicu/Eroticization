# R386 · `humiliation` 不是概念重叠 —— 对照掉得一模一样

⚠ **Closure(稳健性),如实标注。**

`#338b` 的残差第一名是 **`humiliation` +0.301**,而它是这个项目里唯一一个
既是**兴趣类别**、又直接是**羞耻的近义概念**的东西。
**它可能不是「这类人更羞耻」,而是「这道题和羞耻那道题问的是同一件事」。**

## 结果:去掉它,和去掉对照,掉得一模一样
| | 残差 sd | 相对 se 中位 | `corr(mu, rar)` | 四量 R² |
|---|---:|---:|---:|---:|
| 全部 31 类 | 0.1450 | 4.94× | +0.7583 | 0.0236 |
| **去掉 `humiliation`** | **0.1364** | 4.70× | +0.7817 | 0.0200 |
| **去掉 `gender`(对照)** | **0.1364** | 4.70× | +0.7768 | 0.0307 |

**留一每一个类别(31 次)的基线:0.1448 ± 0.0034,范围 [0.1346, 0.1474]。**
**去掉 `humiliation` 落在 −2.45 sd;去掉 `gender` 也落在 −2.45 sd。**

> **⇒ 那个下降不是概念重叠,是「少了一个大残差的格」。**
> **如果是概念重叠,`humiliation` 该比 `gender` 掉得多。它没有。**

正对照(给 `gender` 的人 +1.0):留一正确挑出它(+0.947)。

## ⚠ 而这一轮真正做事的是**对照**,不是判据
我注册的 kill 是「去掉 `humiliation` 后残差 sd 是否低于随机留一分布 −2 sd」——
**它通过了。但对照也通过了,数值一模一样。**

> **一个只作用在处理组上的门槛,分不开「机制」和「任何一个大格」。**
> **是那个配对的对照做出了判决。**

## ⇒ `#339b` 的开放问题照旧
去掉 `humiliation` 之后,残差仍是抽样底的 **4.70 倍**。
**那 42% 不是一个近义词造出来的。**

## 一句关于人的话
没有 —— **但那个「量不到的东西」现在少了一个最容易的解释。**
