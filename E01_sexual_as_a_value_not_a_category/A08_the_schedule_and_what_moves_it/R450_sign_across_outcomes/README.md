# R450 · `S` 只是**勉强**朝一个方向 —— 而 8 个结局上它是反的

页面上关于 `S` 的每一句话都来自**羞耻**那一个结局。**没有一句检查过它在别处是否同号。**

## 结果:21/29 为正,一致性 **0.7241** vs 零的上侧 **0.6897**
| | |
|---|---|
| `S` 系数为正 | **21 / 29** |
| 符号一致性 | **0.7241** |
| offset 零(**打乱 `S`**,保住结局间相关) | **0.5788 ± 0.0588** · 95 分位 **0.6897** |
| | **+2.47 sd —— 越阈,但擦线** |

⚠ **零的种类**:`offset_control` —— **29 个结局里,即使 `S` 与它们全无关,多数符号也会占到 58%**。

**最正的**:`inducefetish` +0.096 · **羞耻 +0.090** · 「非性场景下作为生理女性存在很色」+0.086 · 实践量 +0.084
**最负的**:`highenergy` −0.075 · 宜人性 −0.058 · 尽责性 −0.053 · 神经质 −0.033 · `written` −0.029

## ⚠ guard 26 通过了,而那个通过是**空的**
MDE 扫描**每一级都 100% 检出**(0.02 / 0.04 / 0.06 / 0.10)。
**符号一致性几乎没有动态范围** —— 任何**统一方向的**推动都会把接近零的系数一起翻过来,统计量立刻饱和。
> **⇒ 这个设计的 MDE 不可测,而不是「很小」。guard 26 的 PASS 在这里不算数,我不拿它当证据。**
> (本会话第三次 MDE 扫描饱和/量错:`#387c` · `#402b` · 本轮 —— 但**这一次的原因在统计量本身**,不在我的构造。)

## ⚠ 而偏差方向是**不保守**的
**IMPOSSIBLE ①**:29 个结局彼此相关 -> 一致性被**抬高**,**偏向「朝一个方向」**。
**⇒ 一个擦线的通过,加上一个偏向通过的偏差 —— 这不足以支持「`S` 是一个有统一方向的量」。**
**IMPOSSIBLE ③**:结局的**编码方向**由问卷决定,不由我 ——「一致性低」也可能只是编码方向不统一,本轮无法排除。

## ⇒ 页面上关于 `S` 的话,是**结局特定**的
**8 个结局上 `S` 是反号的**,其中包括 `#405a` 撞见的「能不能改」。
**⇒ 每一句「`S` 高的人如何如何」都必须带上「在**羞耻**这个结局上」。**

## 对照
负对照**越阈率 2.0%** ✅ · offset 零非退化 ✅ · guard 26 **PASS 但空** ⚠。

## 一句关于人的话
**「口味有多冷门」不是一个朝着一个方向的东西。**
**它与羞耻的关系,不能被读成它与「一个人的性生活整体」的关系 —— 在 29 件事里有 8 件它是反的。**
