# R358 · 把 guard 21 用到公开页面上的每一句「没有」

`#312a` 写好了守卫,**但它还没判过任何一句已发表的话。**

⚠ **这是 Closure**,不是 Frontier。

## 计数
| | |
|---|---:|
| README 上的零式声明(含数字 · 含否定词 · ≥60 字符) | **82** |
| 三件套齐全 | **69**(84%) |
| **缺件** | **13** |
| 缺 MDE/CI | **12** |
| 缺置换分位数 | 5 |
| 缺正对照灵敏度 | 2 |

## ★ 不只列缺口 —— 现场补上了 `#308` 的 MDE
`#312` 的 NEXT 点名要补的那一条:
置换零 sd **0.0135** → **MDE = 2.8×sd = 0.0378**;
有意义的交互(两条路主效应各约 0.10,取一半)**0.0500**。
**MDE < 有意义量,所以 `#308` 那个零有内容。**
⚠ 而灵敏度是**观察到的**,不是算出来的:植入 0.15 抓到 **+0.1502**。

**`#308` 与 `#311` 现在都过 guard 21。**

## ⚠ 这台仪器已知的两个盲区,写在结果旁边
1. **它不区分 `UNVERIFIED` 与「零」。** `#296`(「结局侧还没被控制」)被它匹配了 6 句 ——
   那是「我不知道」,不是「没有」。**一台把 UNVERIFIED 当零的仪器,会把无知记成发现。**
   逐条读时必须人工剔除。
2. **引用按「块」归属,所以「齐全」被高估。** 一句话继承了它整段所引账本条目的关键词,
   而那条目里的置换零未必是**这一句**的。
   **⇒ 84% 是上界;13 句缺件是下界。** P6 安全侧:**检出 = 待读;未检出 = 确定要补。**

## 缺件集中在哪
12 句缺 MDE 里,4 句在**头段**(`#264 #301 #302` 的 1.4% / 0.14% / 49%),
其余在 `#20`/`#169`(已撤回的那条定律)与几段没有账本引用的旧文本。

## 一句关于人的话
没有 —— **但这一页现在知道自己有 13 句「没有」还欠着一张视力表。**
