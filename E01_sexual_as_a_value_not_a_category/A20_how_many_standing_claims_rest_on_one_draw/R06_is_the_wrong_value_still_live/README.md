# R06 · 账本判为错的那个数,还在不在新输出里?

**【CLOSURE,明确标注】** 不产出关于人的新事实。它保护的是 `#156`/`#157` 刚修好的两条声明。

`#157` 的 NEXT:`repro_audit` v1 的方向是反的 —— 它查「账本的数**在不在**新输出里」,
所以对 `#90` 的 ±30.8 完全无感,**而那个数本该消失**。

## 抽取器:它连 `#90c` 自己那句话都抓不到

v1 的正则是 `(\d+\.\d+)\s*(?:->|→|到|to)\s*(\d+\.\d+)`。而 `#90c` 的原句是
`falls from ±30.8 to **±23.7 pp**` —— markdown 的 `**`、`±`、`%` 把匹配打断了。
放宽后,全账本 **7 条**条目含校正对。

## 反向检验先证明了上一轮的修复

| | 新输出里 |
|---|---|
| `#90` 的旧值 **30.8** | **0 次** ✅ |
| `#90` 的旧值 **22.4** | **0 次** ✅ |

**上一轮的修复被一个独立检查确认。**

## 而跑它抓到三类假阳性,每一类都是实测踩出来的

| 类 | 实例 | 处理 |
|---|---|---|
| ① **参数 → 结果** | `Entry 69`:*"the control at **g=0.15** moves ρ to −0.314"* —— 0.15 是种植强度,不是旧值 | 在 `stale_pairs` 里排除赋值/参数前缀 |
| ② **一条条目覆盖多轮,旧值在前一轮** | `Entry 126`:`R20` 打 **+0.1822**、`R21` 打 **+0.1449**,两轮同属一条条目 | 判据改为「新值在该条目的**任何一轮**里出现即良性」 |
| ③ **一轮本来就该同时报两个臂** | —— | **不可机械区分,留在必读清单里**(`P6` 安全侧) |

## 结果

**7 条含校正对,只有 `#90` 是现存声明,而它已修并已验证。**
`#126` 在修好的判据下正确读作良性(`R21` 输出里有 +0.1449)。

**没有其它现存声明带着一个还活着的、被账本判为错的数。**
