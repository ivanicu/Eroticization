# R04 · 账本声称的东西,那一轮的代码现在还跑得出来吗?

第四条边(前三条:README→账本 · 轮次→账本 · 轮次→守卫)。
`#154a` 正是靠"跑一遍轮次本身"才分清「我复现错了」和「README 错了」—— **而那一步是手工的**。

`tools/repro_audit.py`:重跑一轮,把账本条目里的**可辨识数字**与新 stdout 比对(容差 2%)。

## 17 条现存声明,16 条跑通,**1 条崩溃**

| 声明 | 退出码 | 账本数字 | 对不上 |
|---|---:|---:|---:|
| #63 | 0 | 10 | **0** |
| #116 · #69 · #100 | 0 | 14 / 17 / 16 | 2 |
| #99 · #104 · #107 | 0 | 20 / 22 / 7 | 3 |
| #114 | 0 | 12 | 4 |
| #90 | 0 | 6 | 5 |
| #75 · #118 | 0 | 41 / 13 | 7 |
| #110 | 0 | 22 | 9 |
| **#117** | **1 崩溃** | 11 | 8 |
| #102 | 0 | 19 | **14** |
| #88 | 0 | 30 | **18** |
| #101 | 0 | 40 | 21(条目覆盖 R14+R15,我只跑了 R15) |

## `#117` 的崩溃,是它自己记录的那个 bug

```
File ".../A12/R13/run.py", line 129
    sh=G.shift.values
AttributeError: 'function' object has no attribute 'values'
```

`shift` 是 pandas 的 **DataFrame 方法**。而 `#117e` 的原文是:

> **第五次 pandas 访问器撞名,而这次撞的是我自己禁用名单上的第一个词。**
> `G.shift` → `DataFrame.shift`。`#80d` 我写下"任何列名不得与 DataFrame/Series 方法同名",
> 并把 **`shift` 列在第一位**;… **写下规则第三次没有阻止我违反它**。

**我诊断了它、为它写了 `check_columns` 守卫、把结论记进账本 —— 却没有把那一行改掉。**
轮次从那时起一直崩,而崩溃在**结论打印之后**,所以退出码没人看。

### 两个工具在同一个具体缺陷上会合

`guard_lint`(`#128a`)早就把 `A12/R13` 标为**缺 `check_columns`**;
`repro_audit`(本轮)显示它**确实**崩在那一类 bug 上。**这是四个工具第一次相互印证。**

### 而声明本身完好

修好两处(`G['shift']`、`G['shift']-G['shift_null']`)并**把守卫装上**之后,
那个从没跑到过的段落给出:位移 **+0.0266 ± 0.0127**,分层置换零 **+0.0067**(25%),
**29/45** 为正,**2.1×** —— 与 `Entry 117` **逐字相同**。

**这是代码卫生的缺陷,不是声明的缺陷。**

## 范围(`P6` 安全侧)

"对不上"是**必读清单**,不是判决:账本条目会引用**前面条目**的数字、散文里的数字、
以及本轮没跑到的臂。`#101` 的 21 个就是这样(条目覆盖 `R14`+`R15`,我只跑了 `R15`)。
