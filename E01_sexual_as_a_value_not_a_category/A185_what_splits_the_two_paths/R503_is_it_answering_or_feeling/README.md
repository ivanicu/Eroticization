# R503 · 不是「答得多」,是「进过多少个不同领域」

⚠ **FRONTIER**。`#458` 的 NEXT:「答题类别数只推能不能改」是不是答题行为的影子?

## ★★ ① 读完发现**两件事**,都在计算之前 —— 而两件都推翻我自己写的路标
**(1) 出处引错了。** 我的 NEXT 写「`#100` 系列用过卡钳匹配」;
项目的记录是 `#129e`/`#226a`/`#228a`,**而且 `#226a` 那次卡钳「没成」**(中位 49/62/69 仍递增);
**成的是 `#234a` 的十分位分箱重抽样。**
**(2) 更要紧:匹配掉答题量就是把 focal 本身拿掉。**
**匹配对这个问题不适用**,无论哪个版本 —— 写下来,不悄悄换。

## ② 换成合适的分离器:`#421` 已列举的五个卷入量
它们**不可互换**(本轮实测的相关矩阵印在 `results/`,+0.26 ~ +0.86)。

| 量 | b 羞耻 | b 能不能改 | 推谁 |
|---|---:|---:|---|
| **起始类别数 `ncat`** | +0.0101 | **−0.0353** | **只推能不能改** |
| **块覆盖数 `COVB`** | +0.0094 | **−0.0421** | **只推能不能改** |
| 总勾选项数 `PICKS` | −0.0221 | −0.0227 | 都不推 |
| 恋物类别数 | −0.0096 | −0.0140 | 都不推 |
| 认可的性行为数 | −0.0066 | −0.0309 | 都不推 |

族内阈 **0.03352**。**对照**:`ncat` 精确复现 `#458b`(+0.0102 / −0.0354)。

## ★★ ③ 分野很干净:**领域数** 推,**项数** 不推
越阈的两个(`ncat` · `COVB`)**都是「进过多少个彼此不同的领域」**;
三个数「领域**之内**」的量(总勾选项、恋物类别、性行为)**一个都没越**。

## 一句关于人的话
**不是「答得多的人觉得更能改」—— 单纯勾了多少项,什么也不说。**
**跟着它走的是:一个人到底踏进过多少个彼此分开的领域。**

## IMPOSSIBLE
① 领域数**仍然**是一种答题行为 -> **收窄了**假象的担心,**没有消除**;
② 五个量共同可用的样本比 `#458b` 小;③ 横断面。
