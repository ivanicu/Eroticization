# R324 · `form_i` 通向什么

`#278a`:它不通向羞耻,**但它打中 27 个结局里的 9–11 个,而没人看过那是哪几个。**

## 剖面相似度:它指的地方,和 D 指的地方大体重合
| | 与 `form_i` 剖面的相似度 |
|---|---:|
| **跨块对比 D** | **0.7105** |
| c3 | 0.4958 |
| 位置分 S | 0.2289 |
| **offset**(同一件事的两次带噪声读数) | **0.9124 ± 0.0122** |

**最像的 D 只到上限的 78%** —— 注册的 kill(三条都低于上限一半)**没过**。
正对照两端都过(已知不同 0.1235 · 同一变量两次复制 0.9981);
负对照(只在有限值内置换)0.1703 ± 0.1209。

## ⚠⚠ 而这是同一个分离的第三次,也是最干净的一次
**`form_i` 与 D 在分数层是**精确正交**的**(`form_i` 是六坐标回归后的残差,D 是六坐标之一)——
**而它们的结局剖面相似 0.71。**

> **两个相关**恰好为零**的量,可以在 27 个结局上给出几乎同一张脸。**
> **`#259a`(分数 +0.16 / 剖面 +0.78)· `#263a`(分数 −0.10 / 剖面 +0.71)· 本轮(分数 **0** / 剖面 +0.71)**
> —— **三次,而这一次分数层的零是代数上保证的。**
> **剖面相似作为「是不是同一个东西」的证据,力量为零。**

## `form_i` 的剖面本身
最强几格:`aroused by being…` −0.122 / +0.118 · `extroversion` −0.099 ·
`I can get aroused by a partner doing what I like…` −0.054 · **羞耻 +0.045**(第 3 强,但增量不可分辨,`#278a`)。
最弱:`关系风格` +0.000 · `allrollidentity` −0.001 · `Engaging with or…` +0.002。

## 一句关于人的话
那个没有名字的东西,**指的地方和「你在哪些领域敞开」大体是同一片** ——
尽管按算法它与后者**一点关系都没有**。
**它不是一片新大陆,是同一片大陆上,用另一种方式量出来的一块。**
