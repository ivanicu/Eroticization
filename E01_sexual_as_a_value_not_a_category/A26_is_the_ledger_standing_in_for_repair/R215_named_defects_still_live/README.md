# R215 · 账本点名过的 README 缺陷,现在修好了没有

**新规则** `readme_ledger_audit.named_defects()`:把账本正文里**点名 README 某处有缺陷**的条目
所引用的**具体串**抽出来,在当前 README 里 grep —— **仍原样命中且邻近无撤回标记 = 未修**。

**正对照**:`#169b` 上一轮刚把 `+0.815` 三处全部改写并加标记。规则必须判它**已修**。
判到了(3 处命中,`marked=True` 全中)—— 标记检测没失效,整轮可读。

## 注册的 kill 开火

| | 命中 | 无标记 | **同文件内重复** |
|---|---:|---:|---:|
| **修前**(`HEAD`) | 25 | 18 | **4** |
| **修后**(工作区) | 20 | 13 | **1**(已确认假阳性) |

修前的 4 处,**全部出自 `#144d`,记于 25 条条目之前**:

    #123 #144  `+0.2922`  README.md 的 57 与 134 行各一遍
    #144       `82.7%`    README.md 的 57 与 127 行各一遍
    #154       `+0.023`   README.md 的 50 与 111 行各一遍   <- 假阳性,见下

**`#144d` 记的是「README 有两套并行叙述」——** 一张声明表 + 一段正文走查,同一条声明各写一遍。
它不是排版问题:**它就是 `+0.815` 能在行 122 活下来、而行 88 已更新的那个机制。**
按「一个事实一个家」(§P16)重写 R06 正文段:数字只留在声明表里,正文只写表里没有的东西。

## 这条规则的两个见证(P6 代理账)

- **① 改写后仍保留同一个数字**:`+0.815` 现在活在删除线里带 `KILLED — #20`。
  所以**必须查邻近的撤回标记**,否则修好的会被判未修。
- **② 同一个字符串可以有两个所指**:`+0.023` 在行 50 是**开放性相关**,在行 111 是
  信度阶梯里的 **sham 值**。代理按**字符串**匹配,不按**所指**匹配 ——
  **「同文件内重复」的判定必须人工确认所指相同,这一步不是自动的。**

## 结构上做不到

未命中 **≠** 已修(措辞可能已经变了)。规则只在**命中且无标记**方向可读。
