# R501 · 一个中介能不能出信息,三个相关系数就够了 —— 不必跑那一轮

⚠ **CLOSURE**(造仪器并校验它)。`#456` 的 NEXT。

## ① 事前上界 vs 事后实测
| 案例 | 事前上界(占总效应) | 事后实测移动 |
|---|---:|---:|
| **负对照** `#500` 起始→能不能改(控羞耻) | **0.74%** | **0.7%** |
| **负对照** `#500` 起始→羞耻(控能不能改) | **0.36%** | **0.5%** |
| **正对照** `#434` 用量→实践(控羞耻) | **有空间**(−20.2%) | 已发布 |

**对上了。** 而上界只用了 `r(x,y2)` · `r(y1,y2)` · `r(y1,x)` **三个数**,**没有跑任何拟合**。

## ② 工具的形状照 `share()`:拒绝时**拿不到那个数**
`lib.bounded.mediation_headroom(x, y1, y2, mask, min_move=0.05)` ->
`(ok, indirect, share, why)`,**`ok=False` 时 `indirect` 是 `None`**。
自检 **3/3**(正交结局被拒 · 真中介放行 · 拒绝时不返回数值)。

## ⚠ ③ 一条必须一起说的边界
工具用的是**原始相关**,没有本轮的完整控制集 ->
**它回答的是「有没有空间」,不是「实际拿走了多少」。**
`#434` 那一行的 −20.2% 是**上界**,不是它拟合出来的份额(那一条 `#435c` 已判不可估)。

## 一句关于人的话
没有,这是仪器轮。
**产出:一个中介检验能不能失败,现在在跑之前就知道 —— 而 `#456` 是跑完之后才发现的。**

## IMPOSSIBLE
① 上界用原始相关,是「有没有空间」不是「拿走多少」;② 只在两个已知案例上校验;
③ `min_move=0.05` 是我选的,**但两个负对照都远低于它(0.74% / 0.36%),结论不依赖这个值**。
