# R477 · 色情使用量多解释的,是羞耻,而且只有羞耻

⚠ **FRONTIER**。`#432` 的 NEXT:方法线连跑 7 轮,回到心理学。
`pornhabit` 是 `#428` 名单上**唯一方向已被测量**的变量(`#428c`:高 = 用得多),
而它从没被问过任何心理学问题。

## ⚠ ① 先修一个单位错误(首跑给出 4/4,是构造出来的)
族内阈由零的**最大值**取 95 分位,但零的抽样是**系数**,而 `sig` 拿它去比 **z 分数** ->
**单位不匹配,于是四个全部「越阈」。** 把零先按各自的零 sd 标准化后,阈从 `0.03` 变成 **2.44**。
**与 `#429b`(`dtype==object`)、`#431e`(`b.round`)同一类:一个安静地取到别的东西的表达式。**

## ② 结果:1/4,而这一个很干净
控制集沿用 `R449`(逐字拼接,不是重写):`z(S)` · `−z(五题)` · `c3⁻` · 类别数,**另加年龄**。
n = **6,715** · `pornhabit` 值集 0..9 · 众数 **7**。

| 结局 | b | z | 越族内阈 2.44 |
|---|---:|---:|---|
| **羞耻** | **+0.0611** | **+5.19** | **是** |
| 能不能改 | −0.0179 | −1.48 | 否 |
| 治疗性 | +0.0165 | +1.39 | 否 |
| 实践了多少 | +0.0122 | +1.02 | 否 |

**那三个是有功率的零**:同一设计能测到 **0.029**(= 2.44 × 零 sd),**不到羞耻系数的一半**。
**对照**:置换后的 focal 越阈率 ≤ 6%(零说得出「不」)· 未控制时的原始关联非零(否则「塌掉」是空话)。

## 一句关于人的话
**在「口味有多不寻常、有多宽、普通的东西管不管用」都固定之后,**
**色情使用量多解释的是羞耻,而且只有羞耻 ——**
**不是「它失控了」的感觉,不是「它疗愈」的感觉,也不是更多地去实践。**

## IMPOSSIBLE
① 横断面;② 原始关联自始至终都小(0.030–0.077)-> 这是「谁在控制之后活下来」,不是大效应;
③ 四个结局是 `R449` 的,**不是为这个问题挑的**(这是优点,也意味着不覆盖别的结局)。
