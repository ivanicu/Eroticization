# R417 · 数据集卡片描述的不是我手上这个文件

`#372` 的 NEXT 要求 **L3 先跑**(读数据集自己的文档)。跑了,**然后没走到原定的问题**。

## ① 形状就对不上
| | 行 | 列 |
|---|---:|---:|
| 卡片第 3 行 | 15,511 | 376 |
| 文件 | **15,503** | **365** |
| 差 | **−8** | **−11** |

## ② 逐条核对卡片明写的「Now: N levels」
| | 列 | 卡片 | 文件 |
|:--:|---|---|---|
| ✅ | `politics` | 3 档 | 3 档 |
| ✅ | `bmi` | 2 档 | 2 档 |
| ❌ | `straightness` | **5 档** (Gay · Leaning gay · Bi · Leaning straight · Straight) | **2 档** (Straight · Not straight) |
| ❌ | `childhood_adversity` | **2 档** (None · Any) | **1 档** (Any) |
| ❌ | `TotalMentalIllness` | **2 档** (None · Any) | **1 档** (Any) |

**符合 2 · 不符 3。⇒ 卡片与文件是两个对象。**
⚠ 正对照通过(有 2 条相符)—— **一个「全部不符」的解析器,坏掉的概率远高于数据。**
⚠ 解析只覆盖卡片里措辞规整的那些块;**漏掉的方向是保守的**(少报不符)。

## ③ ★ 而两条不符**恰好推翻我自己在 `#366` 里加的一条警告**
我写的是:「`TotalMentalIllness`/`childhood_adversity` 的 NaN 被读成 0 —— 把「没有」与「没作答」并成一档」。
**卡片说这两列本该是 `None` / `Any` 两档。** 那么文件里的 NaN 就是 `None`。

两个世界,用**行数**分辨:
- 若 `None` 的**行**被删 -> 全表最多剩 6,196 行。
- 实际全表 **15,503** 行,与卡片只差 **−8**。

**⇒ 被删的是标签,不是行 -> NaN 就是 `None`(D6)。`#366` 那条警告多余,撤回。**

## ④ 这改的是别处的 scope
`#357` 是**整条页面注记**建在卡片上的(成长环境 7→3 · 关系风格 3→2 · 匿名化让相关弱约 25%)。
**卡片落后于文件,所以那三条是「卡片这么说」,不是「这个文件如此」。**
而 `straightness` 5→2 证明**实际的粗化可以比卡片写的更狠** —— 所以 `#357` 的方向没错,**幅度是下界**。

## 一句关于人的话
没有。**但这一轮把页面上一整条注记的地位,从「数据的事实」改成了「文档的说法」。**
