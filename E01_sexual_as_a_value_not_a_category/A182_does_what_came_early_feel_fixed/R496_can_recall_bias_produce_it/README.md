# R496 · 读出来的两件事,比跑出来的那一件更值钱

⚠ **FRONTIER**。`#451` 的 NEXT:回忆偏差是那条新结论唯一的严重威胁 ——
而规矩写在前面:**先去读 `#289` 用的到底是什么控制,不要凭记忆重造。**

## ★★ ① 读完发现两件事,都在任何计算之前
**(1) 页面上那条引用是错的。** 页面写「回忆偏差已控(`#289`)」——
**而 `#289` 讲的是「S 不是性别,但 S 在两个人群里意味着不同的东西」**,按性别的测量不变性。
项目里真正测过回忆偏差的是 **`#114`**(经 `R166`)。**已在页面上更正。**

**(2) `#114` 的构造本身就回答了这个威胁。**
它的量是:报告起始年龄 − 人群该类别中位 − **该人自己的整体早熟度**。
**⇒ 人层均值按构造被扣掉了,而 `EARLY` 正是人层均值。**

## ② 把「按构造分开」量出来,而不是只论证它
| | |
|---|---:|
| 整体早熟度 与 `EARLY` | **+0.9932** |
| `#114` 扣掉早熟度后的人内倾斜展布 与 `EARLY` | **+0.2733** |

**⇒ 被测出来的那个偏差,住在一个几乎正好已经被这个 focal 扣掉了的层里。**
⚠ **分离不是彻底的**(+0.27),如实报出。

## ③ 替代品,**明确标注**(`#451` NEXT 的自订规则)
> 若既有控制在本轮口径上不可用,**写下来并停,不要换一个近似的冒充它。**

所以:**整体评分强度**(`RATING_0_5` 67 列人均)作为**替代**协变量,
**它被标注为替代品,不被当成 `#114` 的控制**。
**对照**:它与 `EARLY` 相关 **−0.1046** -> 控制不是空的。

| 臂 | b | 阈 | 越阈 |
|---|---:|---:|---|
| 不控评分强度 | **+0.0502** | 0.0222 | 是 |
| **控评分强度(替代品)** | **+0.0477** | 0.0222 | 是 |

**保留比 0.950 -> SURVIVES。**

## 一句关于人的话
**「早来的东西更像是定死的」不是记忆造出来的 ——**
**这份数据里被量出来的那种记忆偏差,发生在一个已经被扣掉的层上。**

## IMPOSSIBLE
① 分离 +0.27 不彻底;② 替代品**不是** `#114` 的控制,如实标注;③ 横断面。
