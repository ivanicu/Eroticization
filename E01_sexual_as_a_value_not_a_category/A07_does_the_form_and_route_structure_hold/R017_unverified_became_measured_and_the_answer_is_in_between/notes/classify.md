# R367 · UNVERIFIED 分三类 —— 29 条只有点估计,而 NEXT 段一直在污染我的审计

⚠ **Closure。⚠ 而 `#364` 刚证明自评的结论不可信**,所以只报计数 + 人工抽读。

## 计数(账本 305 条,含 UNVERIFIED 式判决 88 条,29%)
| | | |
|---|---:|---:|
| ① **带区间 / MDE**(能说出自己属于哪一种) | **49** | 55.7% |
| ② **只有点估计**(说不出) | **29** | **33.0%** |
| ③ **仪器失败**(正对照没过,另一回事) | **10** | 11.4% |

**② 的 29 条是一份可执行的补测清单**,而且它们**集中在早期**:
`#24 #25 #26 #28 #33 #34 #41 #46 #48 #74 #92 #93 …`
—— **区间纪律是后来才有的,而早期的 UNVERIFIED 至今说不出自己是哪一种。**

## ⚠⚠ 正对照抓到一个影响多轮的缺陷
第一版把 `#321` 判成③。触发词在它的 **NEXT 段** —— 那描述的是**下一轮**的设计
(「③ 仪器失败(正对照没过」)。

> **每条账本的 NEXT 属于下一轮,不属于这一条。**
> **任何读整条的分类器,都会把未来那一轮的性质算到当前这一条头上。**

剥掉 NEXT 后正对照 **3/3**,全部门通过。

## 而这个缺陷不止影响本轮 —— 重跑了 `#319`
`#319`(撤回风险比)**也读了整条**。NEXT 污染会让「NEXT 里提到网格」的条目被判为有网格,
**把 RR 推向 1** —— 那个零可能是伪影。

| | RR | 2×2(无网格撤/未撤,有网格撤/未撤) | 网格检出率 |
|---|---:|---|---:|
| 读整条(`#319` 的做法) | 0.88 | (48, 149, 30, 78) | 35% |
| **剥掉 NEXT** | **0.76** | (49, 168, 26, 62) | 29% |

**结论不变**(仍在 1 以下,而置换零展布是 ±0.23)——**`#319` 站得住,但它的数字该按 0.76 记。**

## 人工抽读 6 条
`#93` ② 无证据 ✓ · `#145` ② 无证据 ✓ · `#309` ①(±0.4 / 95% CI / MDE)✓ ·
`#310` ①(95% CI ×2 / MDE)✓ · `#313` ①(MDE / 分位数)✓ · `#314` —— 第一版判③,
**而它的正对照失败已在同一轮内修好**,剥 NEXT 后归位。

## 一句关于人的话
没有 —— **但这个项目里有 29 条「不确定」,至今说不出自己是「仪器不够」还是「答案在中间」。**
