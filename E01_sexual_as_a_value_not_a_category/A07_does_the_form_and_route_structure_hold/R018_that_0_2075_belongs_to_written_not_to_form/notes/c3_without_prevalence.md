# R374 · 扣掉流行度的 `c3`,三项结论全带得动

⚠ 跑之前写下的:`res` 是从 `v0` 减出来的,`c3_np` 与 `c3` **必然**高度相关 ——
**相关本身不是证据**,要看**羞耻**与**跨仪器**掉不掉。

## 三项
| | `c3_np`(扣掉流行度) | `c3`(原) | 保留 |
|---|---:|---:|---:|
| ① ↔ `c3`(**不作为证据**) | **+0.9473** | — | — |
| ② **↔ 羞耻** | **+0.1044** | +0.1278 | **81.7%** |
| ③ ↔ 越轨半(起始年龄仪器) | **+0.1371** | +0.1740 | 78.8% |
| ③ ↔ 普通半 | **−0.0923** | −0.1205 | — |
| ③ **越轨/普通 比值** | **1.5×** | 1.4× | **略强** |

**起始年龄仪器与块仪器零列重叠**(脚本内 `assert`)。
正对照(只由 `res` 驱动的合成结局):`c3_np` **+0.7093** > `c3` **+0.6660** ✓;
负对照(打乱人)**−0.0011 ± 0.0137**;guard 20 通过(方向一致且同号)。

## ⚠ 我注册的 kill ③ 门槛设错了 —— 第四次
我写的是「越轨/普通 比值 > 3.0×」,理由是 `#303` 报过 **18.6×**。
**但 `#303` 的 18.6× 不是这两个相关的比值** —— `c3` 自己在同一次运行里也只有 **1.4×**。
**我拿另一轮的一个数当门槛,而没有先在这一次运行的单位里把它复现出来。**

> **一个从别的轮次拿来的门槛,必须先在这一次运行里由**参照臂**复现,才能当门槛。**
> 正确的对照是**同一次运行里的 `c3`**,而按它读:**1.5× vs 1.4×,完全保留。**

⚠ 门槛设错的第四次(`#305d` · `#315b` · `#326b` · 本轮),而**四次都是对照发现的**。

## ⇒ 「`c3` 可能只是冷门度」这个通缩读法,两条路都被堵上
- 块层(`#328b`):扣掉流行度后**载荷图样不变**(残差 ↔ 原载荷 +0.9223,两端同一批块);
- 人层(本轮):扣掉流行度后**羞耻保留 81.7%、跨仪器比值不降反升**。

⚠ **边界**:`res` 只扣了**流行度**一个属性(`#328a` 的选项数与离散度没扣)——
**这是「不是冷门度」的证据,不是「与所有结构属性无关」的证据。**

## 一句关于人的话
把「多少人选」从这条维度里整个减掉之后,**它照样带着羞耻,照样在另一件仪器上分开越轨与普通。**
它不是冷门度的影子。
