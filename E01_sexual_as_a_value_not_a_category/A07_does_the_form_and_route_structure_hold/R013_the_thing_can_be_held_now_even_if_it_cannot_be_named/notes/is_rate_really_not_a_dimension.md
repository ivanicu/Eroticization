# R326 · `#250a` 那一步站不站得住

`#280b`:`#250a` 用「剖面相关 −0.8499 贴着上限」得出「`rate` 不是独立维度」,
并据此关掉整条 A67–A69 线 —— **而它从没报过分数层相关**,那正是守卫 15 挡的方向。

## 结果:它站得住
| | |
|---|---:|
| **`corr(rate, rho_i)` 分数层** | **−0.3283** |
| `#250a` 报的剖面层 | −0.8499 |
| 守卫 15 | **放行**(剖面与分数同时高 → 可以按「同一构念」读) |

**分数层 −0.3283 过了 0.30 的线** —— `#250a` 不是被禁方向上的结论。

## 而补出了 `#250a` 当时没测的一格
| | 29 个结局越阈 |
|---|---:|
| `rate` 原始 | 6.2 ± 0.8 |
| **`rate⊥rho`(把 rho_i 整个扣掉)** | **5.0 钉死**(重抽 12 次,展布恰为 0) |

正对照完美(把 rho 从**它自己的带噪声复制**里回归掉,残差越阈 **0.0**);
负对照(只在有限值内置换)0.0。
`rate⊥rho` 最强几格:`age` −0.072 · `animated` +0.044 · `Engaging with or…` +0.039。

> **`rate` 大体就是 Δ 反过来写(`#250a` 成立),
> 但它**不是被完全吸收**:扣掉 rho_i 之后,残差仍打中 29 个结局里的 5 个。**

## 一句关于人的话
「你积累得多快」这件事,九成是「你罕见的兴趣排在前还是后」换一个方向说 ——
**但还剩一成不是**,而那一成最强地贴着**年龄**。
